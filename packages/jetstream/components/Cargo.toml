[package]
name = "poodle-jetstream-components"
version = "0.0.0"
edition = "2021"
description = "Poodle component implementations for Jetstream — token-resolved UI components using the JsEl fluent builder."

[dependencies]
poodle-adapter = { path = "../../contracts/adapter" }
poodle-components = { path = "../../contracts/components" }
poodle-tokens = { path = "../../contracts/tokens" }
poodle-jetstream = { path = "../adapter" }
jetstream-runtime = { path = "../../../../jetstream/crates/jetstream-runtime" }
glam = "0.29"
taffy = "0.9"

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
