[package]
name = "poodle-jetstream"
version = "0.3.0"
edition = "2021"
license = "MIT"
rust-version = "1.95"
description = "Jetstream rendering adapter for Poodle — maps shared contract specs and resolved styles to Jetstream UiTree nodes."
repository = "https://github.com/inflatable-cookie/poodle"
homepage = "https://github.com/inflatable-cookie/poodle"
readme = "README.md"
keywords = ["design-system", "renderer", "ui"]
publish = false

[dependencies]
poodle-adapter = { version = "0.3.0", path = "../../contracts/adapter" }
poodle-headless = { version = "0.3.0", path = "../../contracts/headless" }
poodle-events = { version = "0.3.0", path = "../../contracts/events" }
poodle-layout = { version = "0.3.0", path = "../../contracts/layout" }
poodle-specs = { version = "0.3.0", path = "../../contracts/components" }
poodle-style = { version = "0.3.0", path = "../../contracts/style" }
poodle-tokens = { version = "0.3.0", path = "../../contracts/tokens" }
poodle-node = { version = "0.3.0", path = "../../contracts/node" }
# These types cross the Jetstream adapter boundary and must match the sibling
# Jetstream workspace. Upgrade them in both repositories as one migration.
taffy = "0.9"
glam = "0.29"

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
