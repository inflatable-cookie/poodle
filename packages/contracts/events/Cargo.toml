[package]
name = "flint-events"
version = "0.0.0"
edition = "2021"
description = "Renderer-agnostic semantic event types for Flint multi-renderer targets."

[lib]
path = "src/lib.rs"

[package.metadata.flint]
public-intent = true
channel = "preview"
stability = "pre-release"
