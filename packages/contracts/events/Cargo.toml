[package]
name = "pug-events"
version = "0.0.0"
edition = "2021"
description = "Renderer-agnostic semantic event types for Pug multi-renderer targets."

[lib]
path = "src/lib.rs"

[package.metadata.pug]
public-intent = true
channel = "preview"
stability = "pre-release"
