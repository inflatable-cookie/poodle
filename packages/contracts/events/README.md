# poodle-events

Renderer-neutral semantic event types for Poodle's native targets.

The crate defines component identities, interaction profiles, modifiers,
drag phases, and typed event values. It keeps event meaning stable while each
backend owns platform input translation and dispatch.

This crate is a pre-1.0 source preview and is not yet published to crates.io.
