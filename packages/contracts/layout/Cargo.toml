[package]
name = "flint-layout"
version = "0.0.0"
edition = "2021"
description = "Renderer-agnostic layout intent types for Flint multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
flint-tokens = { path = "../tokens" }

[package.metadata.flint]
public-intent = true
channel = "preview"
stability = "pre-release"
