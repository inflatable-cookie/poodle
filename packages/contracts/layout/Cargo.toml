[package]
name = "poodle-layout"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
description = "Renderer-agnostic layout intent types for Poodle multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-tokens = { path = "../tokens" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
