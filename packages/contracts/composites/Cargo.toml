[package]
name = "poodle-composites"
version = "0.0.0"
edition = "2021"
description = "Contract-backed composite specs for Poodle multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-primitives = { path = "../primitives" }
poodle-tokens = { path = "../tokens" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
