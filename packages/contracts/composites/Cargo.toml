[package]
name = "flint-composites"
version = "0.0.0"
edition = "2021"
description = "Contract-backed composite specs for Flint multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
flint-primitives = { path = "../primitives" }
flint-tokens = { path = "../tokens" }

[package.metadata.flint]
public-intent = true
channel = "preview"
stability = "pre-release"
