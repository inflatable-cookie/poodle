# poodle-style

Renderer-neutral resolved style descriptors for Poodle's native targets.

The crate models color, typography, borders, radii, shadows, opacity, and
cursor intent after semantic token resolution. Target adapters convert the
descriptors into runtime-specific style values.

This crate is a pre-1.0 source preview and is not yet published to crates.io.
