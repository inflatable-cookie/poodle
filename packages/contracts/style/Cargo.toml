[package]
name = "flint-style"
version = "0.0.0"
edition = "2021"
description = "Resolved style descriptor IR for Flint multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
flint-tokens = { path = "../tokens" }
flint-layout = { path = "../layout" }

[package.metadata.flint]
public-intent = true
channel = "preview"
stability = "pre-release"
