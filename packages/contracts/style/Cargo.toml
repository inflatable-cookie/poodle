[package]
name = "pug-style"
version = "0.0.0"
edition = "2021"
description = "Resolved style descriptor IR for Pug multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
pug-tokens = { path = "../tokens" }
pug-layout = { path = "../layout" }

[package.metadata.pug]
public-intent = true
channel = "preview"
stability = "pre-release"
