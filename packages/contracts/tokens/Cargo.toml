[package]
name = "flint-tokens"
version = "0.0.0"
edition = "2021"
description = "Generated Flint token bindings for multi-renderer consumers."

[lib]
path = "src/lib.rs"

[package.metadata.flint]
public-intent = true
channel = "preview"
stability = "pre-release"
