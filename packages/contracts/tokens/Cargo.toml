[package]
name = "poodle-tokens"
version = "0.1.0"
edition = "2021"
description = "Generated Poodle token bindings for multi-renderer consumers."

[lib]
path = "src/lib.rs"

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
