[package]
name = "poodle-primitives"
version = "0.0.0"
edition = "2021"
description = "Contract-backed primitive specs for Poodle multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-tokens = { path = "../tokens" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
