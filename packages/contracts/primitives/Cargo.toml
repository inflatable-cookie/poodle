[package]
name = "pug-primitives"
version = "0.0.0"
edition = "2021"
description = "Contract-backed primitive specs for Pug multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
pug-tokens = { path = "../tokens" }

[package.metadata.pug]
public-intent = true
channel = "preview"
stability = "pre-release"
