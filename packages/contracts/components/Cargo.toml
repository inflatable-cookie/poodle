[package]
name = "poodle-specs"
version = "0.0.0"
edition = "2021"
description = "Contract-backed component specs for Poodle multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-tokens = { path = "../tokens" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
