[package]
name = "poodle-specs"
version = "0.1.0"
edition = "2021"
license = "MIT"
rust-version = "1.95"
description = "Contract-backed component specs for Poodle multi-renderer targets."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-tokens = { path = "../tokens" }
poodle-headless = { path = "../headless" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
