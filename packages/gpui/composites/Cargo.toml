[package]
name = "pug-gpui-composites"
version = "0.0.0"
edition = "2021"
description = "Contract-backed GPUI composite baseline for Pug."

[lib]
path = "src/lib.rs"

[dependencies]
pug-gpui-primitives = { path = "../primitives" }
pug-gpui-tokens = { path = "../tokens" }

[package.metadata.pug]
public-intent = true
channel = "preview"
stability = "pre-release"
