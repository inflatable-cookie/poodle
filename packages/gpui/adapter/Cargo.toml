[package]
name = "poodle-gpui"
version = "0.0.0"
edition = "2021"
description = "GPUI rendering adapter for Poodle — maps shared contract specs and resolved styles to GPUI native elements."

[lib]
path = "src/lib.rs"

[dependencies]
poodle-adapter = { path = "../../contracts/adapter" }
poodle-events = { path = "../../contracts/events" }
poodle-layout = { path = "../../contracts/layout" }
poodle-components = { path = "../../contracts/components" }
poodle-workstation = { path = "../../contracts/workstation" }
poodle-style = { path = "../../contracts/style" }
poodle-tokens = { path = "../../contracts/tokens" }

[package.metadata.poodle]
public-intent = true
channel = "preview"
stability = "pre-release"
