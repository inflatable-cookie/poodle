# Worker Dispatch Ledger

Status: active
Owner: Poodle orchestrator
Updated: 2026-08-11

The orchestrator is the only writer. Workers write their scoped deliverables,
batch log, and papercuts; they do not edit this ledger or roadmap/card status.

| Batch | Card | Branch | Model | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| `g13-b001-authority-inventory` | `g13/batch-cards/001-authority-inventory-and-docs-baseline.md` | `thread/g13-001-authority-inventory` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `251cc858` → merge `a0ca039d`; counts re-measured and confirmed; `svelte:surface-audit`, `docs:lint`, `docs:check`, `git diff --check` all exit 0 on merged main; review log `docs/logs/2026-08/11-g13-b001-b005-review-and-merge.md`; worktree removed |
| `g13-b002-pilot-fixture-metrics` | `g13/batch-cards/002-pilot-fixture-and-metrics-freeze.md` | `thread/g13-002-pilot-fixture-metrics` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `89debbcb` → merge `2368f436`; 32 `FIX-*` fixtures, full quantitative baseline; LOC, glob totals, and verbatim drift-gate quotes spot-verified; `docs:lint`, `svelte:surface-audit`, `git diff --check` exit 0 on merged main; found the corpus §8 classification-count defect (ruled: row marks win, `EXT` = 6); worktree removed |
| `g13-b006-button-tone-parity` | `g13/batch-cards/006-button-family-tone-parity.md` | `thread/g13-006-button-tone-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `22337a31` → merged; Button success + IconButton warning delivered, all 7 tone blocks verified byte-identical to their danger counterparts after substitution; renderer confirmed exhaustive over variant×tone, no Rust edited; correctly stopped on the SplitButton shadow conflict (→ `008`) |
| `g13-b007-value-domain-drift` | `g13/batch-cards/007-value-domain-drift-inventory.md` | `thread/g13-007-value-domain-drift` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `8090521c` → merged; 21 disagreements + 8 unresolved types across 16 components; exit-code behaviour and button-family-clean claim both re-verified by the orchestrator; `docs:check` untouched |
| `g13-b008-split-button-tone` | `g13/batch-cards/008-split-button-tone-parity.md` | `thread/g13-008-split-button-tone-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `f59adac0` → merged; all 6 success/warning blocks verified byte-identical to SplitButton's own danger rules after substitution; primary danger shadow matches contract §8 verbatim; zero baseline files touched, visual diffs enumerated and all expected. Carries the orchestrator shadow ruling (contract right, split-button.css outlier — button.css:193 ships the same shadow); delivers primary danger shadow + success/warning; enumerates visual diffs but refreshes no baseline |
| `g13-b009-dialog-initial-focus` | `g13/batch-cards/009-dialog-initial-focus.md` | `thread/g13-009-dialog-initial-focus` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `e4af527e` → merged; found a real latent bug (`?? ` fallback always ran, so Dialog never focused `focusable[0]`); stopped red on spec drift, ruled web-only pending the IR. Side-quest, outside the IR lane: consumer-driven `initialFocus` for Dialog/FormDialog in Svelte + React with contract updates; branched from `b1a4a5e7` so the owner's focus-guard work is the base |
| `g13-b010-text-input-focus-parity` | `g13/batch-cards/010-text-input-focus-parity.md` | `thread/g13-010-text-input-focus-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `b2aa40ae` → merged; stopped uncommitted needing one WEB_ONLY_PROPS line, finished by the orchestrator. Runtime-parity side-quest under the new Svelte-is-reference rule: ports `autofocus` + `focus()` to React TextInput and documents both; classified web-only/imperative, excluded from TextInputSpec |
| `g13-b011-poodle-ir-schema-core` | `g13/batch-cards/011-poodle-ir-schema-core.md` | `thread/g13-011-poodle-ir-schema-core` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `4a22c8d8` → merged; lib-only, deps verified (`poodle-tokens`+serde only), no forbidden types (the gpui/jetstream strings are serde renames on a `RuntimeTarget` enum), 14 tests, shared types + permitted subsets modelled and tested. Registered in release manifest/operations/notes by the orchestrator — card wording had excluded those. **First g13.002 implementation card.** Creates `packages/contracts/ir/` (lib only, `poodle-tokens` only dep) per the `b003` placement ruling; schema + validation only, no codegen, no macros |
| `g13-b012-ir-expression-vocabulary` | `g13/batch-cards/012-ir-expression-vocabulary.md` | `thread/g13-012-ir-expression-vocabulary` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `911fdfd8` → merged; operator set verified to be exactly the 14 ruled with no arithmetic, 30 tests incl. three exclusion tests. Implements the now-normative bounded expression vocabulary in `poodle-ir`; conformance set drawn from every `$derived` in the three pilots; no evaluator, no codegen |
| `g13-b013-tabs-variant-consolidation` | `g13/batch-cards/013-tabs-variant-consolidation.md` | `thread/g13-013-tabs-variant-consolidation` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `0b347022` → merged. Side-quest component API change: five Tabs variants to four, `text`→`card`, old card deleted, `activeOutline`/`activeFill` opt-in. Rust accessory parity is the bulk. Consumer migration (29 sites, 6 repos) handled separately |
| `g13-b014-app-header-element-access` | `g13/batch-cards/014-app-header-element-access.md` | `thread/g13-014-app-header-element-access` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `b47a2580` → merged; also surfaced the forwardRef parity-sweep hole (8 components ungated). Side-quest: exposes the AppHeader `<header>` element (Svelte `bind:element`, React `ref`) so hosts can attach window-drag behaviour. Rest props and action props rejected by ruling. Also documents the `--poodle-app-header-min-height` override and its 0,2,0 specificity trap |
| `g13-b015-emission-drift-patterns` | `g13/batch-cards/015-deterministic-emission-and-drift-gating.md` | `thread/g13-015-emission-drift-patterns` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `7878c537` → merged; answered all three codegen rulings from local evidence. Codegen precursor replacing `b004`. Mines this repo's own 18 generation/drift selectors as the primary case study, plus vendored ts-rs/schemars; typeshare and specta unevaluated (not vendored, memory-filling forbidden) |
| `g13-b016-active-fill-navigation-menu` | `g13/batch-cards/016-active-fill-shared-type-and-navigation-menu.md` | `thread/g13-016-active-fill-navigation-menu` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `95c78a61` → merged; caught a UA default-border parity diff in its own change. Promotes `TabActiveFill` to a shared `ActiveFill` control type before a second consumer fragments it, and gives NavigationMenu `activeOutline`/`activeFill` with Tabs' semantics. Accepted default-appearance change: the unconditional trigger border becomes opt-in |
| `g13-b017-app-header-center-region` | `g13/batch-cards/017-app-header-center-region.md` | `thread/g13-017-app-header-center-region` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `e63c7f35` → merged; byte-identical default proven by test and by a before/after gate run. Optional `center` region so a header can centre a region symmetrically; presence is the signal, default layout must stay pixel-identical. Narrow-width ruling: reflow to `auto 1fr auto` rather than stack, matching what soundcheck already does and what a titlebar can afford |
| `g13-b019-history-center-web` | `g13/batch-cards/019-history-center-web-reference.md` | `thread/g13-019-history-center-web` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `0c31f2f9` → merged; surface-audit 164/164, no Longhorn import. Six follow-up defects found by inspection (icons, overlapping specimens, width conflict, untokenised text, dropped class prop, empty axis tabs). New component, MessageCenter pattern: data in, commands out, no Longhorn dependency (direction is Longhorn → Poodle, so composing ForkHistoryPanel is impossible). Web reference first because Svelte is the reference implementation; native parity is `020` and is required |
| `g13-b020-active-edge-strip-consolidation` | `g13/batch-cards/020-tabs-active-edge-and-strip-consolidation.md` | `thread/g13-020-active-edge-strip-consolidation` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `2115e5c1` → merged; stalled at 45min with 33 files done and was resumed with `-c` rather than restarted. Found in-repo strip consumers the card missed (DockRegion, AppHeader specimen). Replaces `activeOutline` with a shared `ActiveEdge` (none/outline/underline) so the border axis cannot self-conflict, deletes the `strip` variant into `block` + underline, and flips `bordered` to default false. Absorbs retired `018` |
| `g13-b021-active-fill-none` | `g13/batch-cards/021-active-fill-none.md` | `thread/g13-021-active-fill-none` | `deepseek-v4-flash` (`xhigh`) | dispatched | Adds `none` to `ActiveFill` so the fill axis can be switched off like the edge axis. Without it `block` + underline cannot reproduce the deleted `strip`, which was `020`'s premise. Must land before the consumer sweep |
| `g13-b004-rust-ir-prior-art` | `g13/batch-cards/004-rust-ir-prior-art-and-failure-audit.md` | `thread/g13-rust-ir-prior-art` | `cursor-grok-4.5-medium` | retired | Provider stream stalled twice, including resume; no worktree edits or commit. Retired 2026-08-11 rather than redispatched: half its scope was settled by `b011`/`b012`, and its candidate set (ts-rs/typeshare/specta) mirrors Rust types rather than emitting from IR instances. Recompiled into `b015` |
| `g13-b005-pilot-expressiveness` | `g13/batch-cards/005-pilot-contract-expressiveness-corpus.md` | `thread/g13-pilot-expressiveness-corpus` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `2f8dc5db` → merge `bb3f79ef`; 129 requirement IDs verified unique, no schema recommendation, `UNKNOWN-01`/`UNKNOWN-02` preserved unresolved; review log `docs/logs/2026-08/11-g13-b001-b005-review-and-merge.md`; worktree removed |
