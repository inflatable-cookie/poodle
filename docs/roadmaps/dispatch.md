# Worker Dispatch Ledger

Status: active
Owner: Poodle orchestrator
Updated: 2026-08-11

The orchestrator is the only writer. Workers write their scoped deliverables,
batch log, and papercuts; they do not edit this ledger or roadmap/card status.

| Batch | Card | Branch | Model | Status | Evidence |
| --- | --- | --- | --- | --- | --- |
| `g13-b001-authority-inventory` | `g13/batch-cards/001-authority-inventory-and-docs-baseline.md` | `thread/g13-001-authority-inventory` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `251cc858` → merge `a0ca039d`; counts re-measured and confirmed; `svelte:surface-audit`, `docs:lint`, `docs:check`, `git diff --check` all exit 0 on merged main; review log `docs/logs/2026-08/11-g13-b001-b005-review-and-merge.md`; worktree removed |
| `g13-b002-pilot-fixture-metrics` | `g13/batch-cards/002-pilot-fixture-and-metrics-freeze.md` | `thread/g13-002-pilot-fixture-metrics` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `89debbcb` → merge `2368f436`; 32 `FIX-*` fixtures, full quantitative baseline; LOC, glob totals, and verbatim drift-gate quotes spot-verified; `docs:lint`, `svelte:surface-audit`, `git diff --check` exit 0 on merged main; found the corpus §8 classification-count defect (ruled: row marks win, `EXT` = 6); worktree removed |
| `g13-b006-button-tone-parity` | `g13/batch-cards/006-button-family-tone-parity.md` | `thread/g13-006-button-tone-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `22337a31` → merged; Button success + IconButton warning delivered, all 7 tone blocks verified byte-identical to their danger counterparts after substitution; renderer confirmed exhaustive over variant×tone, no Rust edited; correctly stopped on the SplitButton shadow conflict (→ `008`) |
| `g13-b007-value-domain-drift` | `g13/batch-cards/007-value-domain-drift-inventory.md` | `thread/g13-007-value-domain-drift` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `8090521c` → merged; 21 disagreements + 8 unresolved types across 16 components; exit-code behaviour and button-family-clean claim both re-verified by the orchestrator; `docs:check` untouched |
| `g13-b008-split-button-tone` | `g13/batch-cards/008-split-button-tone-parity.md` | `thread/g13-008-split-button-tone-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `f59adac0` → merged; all 6 success/warning blocks verified byte-identical to SplitButton's own danger rules after substitution; primary danger shadow matches contract §8 verbatim; zero baseline files touched, visual diffs enumerated and all expected. Carries the orchestrator shadow ruling (contract right, split-button.css outlier — button.css:193 ships the same shadow); delivers primary danger shadow + success/warning; enumerates visual diffs but refreshes no baseline |
| `g13-b009-dialog-initial-focus` | `g13/batch-cards/009-dialog-initial-focus.md` | `thread/g13-009-dialog-initial-focus` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `e4af527e` → merged; found a real latent bug (`?? ` fallback always ran, so Dialog never focused `focusable[0]`); stopped red on spec drift, ruled web-only pending the IR. Side-quest, outside the IR lane: consumer-driven `initialFocus` for Dialog/FormDialog in Svelte + React with contract updates; branched from `b1a4a5e7` so the owner's focus-guard work is the base |
| `g13-b010-text-input-focus-parity` | `g13/batch-cards/010-text-input-focus-parity.md` | `thread/g13-010-text-input-focus-parity` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `b2aa40ae` → merged; stopped uncommitted needing one WEB_ONLY_PROPS line, finished by the orchestrator. Runtime-parity side-quest under the new Svelte-is-reference rule: ports `autofocus` + `focus()` to React TextInput and documents both; classified web-only/imperative, excluded from TextInputSpec |
| `g13-b011-poodle-ir-schema-core` | `g13/batch-cards/011-poodle-ir-schema-core.md` | `thread/g13-011-poodle-ir-schema-core` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `4a22c8d8` → merged; lib-only, deps verified (`poodle-tokens`+serde only), no forbidden types (the gpui/jetstream strings are serde renames on a `RuntimeTarget` enum), 14 tests, shared types + permitted subsets modelled and tested. Registered in release manifest/operations/notes by the orchestrator — card wording had excluded those. **First g13.002 implementation card.** Creates `packages/contracts/ir/` (lib only, `poodle-tokens` only dep) per the `b003` placement ruling; schema + validation only, no codegen, no macros |
| `g13-b012-ir-expression-vocabulary` | `g13/batch-cards/012-ir-expression-vocabulary.md` | `thread/g13-012-ir-expression-vocabulary` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `911fdfd8` → merged; operator set verified to be exactly the 14 ruled with no arithmetic, 30 tests incl. three exclusion tests. Implements the now-normative bounded expression vocabulary in `poodle-ir`; conformance set drawn from every `$derived` in the three pilots; no evaluator, no codegen |
| `g13-b013-tabs-variant-consolidation` | `g13/batch-cards/013-tabs-variant-consolidation.md` | `thread/g13-013-tabs-variant-consolidation` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `0b347022` → merged. Side-quest component API change: five Tabs variants to four, `text`→`card`, old card deleted, `activeOutline`/`activeFill` opt-in. Rust accessory parity is the bulk. Consumer migration handled separately — the "29 sites, 6 repos" figure first recorded here was measured across worktree duplicates and pre-migration state. Re-measured 2026-08-11: 17 sites in one repo (`acowtancy/dairy`), which is registry-pinned and so must migrate *with* its Poodle bump, not before. The other consumers migrated themselves as the change landed; `loophole-legacy` is archived. Mapping is in `CHANGELOG.md` (`7b31511d`) |
| `g13-b014-app-header-element-access` | `g13/batch-cards/014-app-header-element-access.md` | `thread/g13-014-app-header-element-access` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `b47a2580` → merged; also surfaced the forwardRef parity-sweep hole (8 components ungated). Side-quest: exposes the AppHeader `<header>` element (Svelte `bind:element`, React `ref`) so hosts can attach window-drag behaviour. Rest props and action props rejected by ruling. Also documents the `--poodle-app-header-min-height` override and its 0,2,0 specificity trap |
| `g13-b015-emission-drift-patterns` | `g13/batch-cards/015-deterministic-emission-and-drift-gating.md` | `thread/g13-015-emission-drift-patterns` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `7878c537` → merged; answered all three codegen rulings from local evidence. Codegen precursor replacing `b004`. Mines this repo's own 18 generation/drift selectors as the primary case study, plus vendored ts-rs/schemars; typeshare and specta unevaluated (not vendored, memory-filling forbidden) |
| `g13-b016-active-fill-navigation-menu` | `g13/batch-cards/016-active-fill-shared-type-and-navigation-menu.md` | `thread/g13-016-active-fill-navigation-menu` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `95c78a61` → merged; caught a UA default-border parity diff in its own change. Promotes `TabActiveFill` to a shared `ActiveFill` control type before a second consumer fragments it, and gives NavigationMenu `activeOutline`/`activeFill` with Tabs' semantics. Accepted default-appearance change: the unconditional trigger border becomes opt-in |
| `g13-b017-app-header-center-region` | `g13/batch-cards/017-app-header-center-region.md` | `thread/g13-017-app-header-center-region` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `e63c7f35` → merged; byte-identical default proven by test and by a before/after gate run. Optional `center` region so a header can centre a region symmetrically; presence is the signal, default layout must stay pixel-identical. Narrow-width ruling: reflow to `auto 1fr auto` rather than stack, matching what soundcheck already does and what a titlebar can afford |
| `g13-b019-history-center-web` | `g13/batch-cards/019-history-center-web-reference.md` | `thread/g13-019-history-center-web` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `0c31f2f9` → merged; surface-audit 164/164, no Longhorn import. Six follow-up defects found by inspection (icons, overlapping specimens, width conflict, untokenised text, dropped class prop, empty axis tabs). New component, MessageCenter pattern: data in, commands out, no Longhorn dependency (direction is Longhorn → Poodle, so composing ForkHistoryPanel is impossible). Web reference first because Svelte is the reference implementation; native parity is `020` and is required |
| `g13-b020-active-edge-strip-consolidation` | `g13/batch-cards/020-tabs-active-edge-and-strip-consolidation.md` | `thread/g13-020-active-edge-strip-consolidation` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `2115e5c1` → merged; stalled at 45min with 33 files done and was resumed with `-c` rather than restarted. Found in-repo strip consumers the card missed (DockRegion, AppHeader specimen). Replaces `activeOutline` with a shared `ActiveEdge` (none/outline/underline) so the border axis cannot self-conflict, deletes the `strip` variant into `block` + underline, and flips `bordered` to default false. Absorbs retired `018` |
| `g13-b021-active-fill-none` | `g13/batch-cards/021-active-fill-none.md` | `thread/g13-021-active-fill-none` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `239e9776` → merge `33a307d3`; exited at the time limit with 28 files uncommitted after finishing the 310-pair sweep, so the orchestrator verified each acceptance criterion directly rather than redispatch a fourth session. Adds `none` to `ActiveFill` so the fill axis can be switched off like the edge axis. Without it `block` + underline cannot reproduce the deleted `strip`, which was `020`'s premise. Also corrects `004`'s "exactly two members" line, which predated `ActiveEdge` and would have forbidden this fix |
| `g13-b004-rust-ir-prior-art` | `g13/batch-cards/004-rust-ir-prior-art-and-failure-audit.md` | `thread/g13-rust-ir-prior-art` | `cursor-grok-4.5-medium` | retired | Provider stream stalled twice, including resume; no worktree edits or commit. Retired 2026-08-11 rather than redispatched: half its scope was settled by `b011`/`b012`, and its candidate set (ts-rs/typeshare/specta) mirrors Rust types rather than emitting from IR instances. Recompiled into `b015` |
| `g13-b005-pilot-expressiveness` | `g13/batch-cards/005-pilot-contract-expressiveness-corpus.md` | `thread/g13-pilot-expressiveness-corpus` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `2f8dc5db` → merge `bb3f79ef`; 129 requirement IDs verified unique, no schema recommendation, `UNKNOWN-01`/`UNKNOWN-02` preserved unresolved; review log `docs/logs/2026-08/11-g13-b001-b005-review-and-merge.md`; worktree removed |
| `g13-b022-poodle-codegen-emitter-core` | `g13/batch-cards/022-poodle-codegen-emitter-core.md` | `thread/g13-022-poodle-codegen-emitter-core` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `143c63a1` → merge `9dab52ac`; clean scope, pushed, zero uncommitted. Orchestrator re-verified rather than trusting the report: byte-identical double generation, `ir:check` 0 clean / 1 on drift with the worktree untouched, content vs whitespace-only vs stale-orphan each reported distinctly, emitted TS type-checks under `--strict --lib es2020`. `check.rs` holds no filesystem write at all, so read-only is structural. One defect fixed in review (`aa86d3e3`): the no-framework assertion tested stderr with inverted boolean logic and was vacuous on success; it now asserts every emitted import specifier is relative, proven by injecting a bare `svelte` specifier. First `g13.003` card and the compiler boundary. Carries the orchestrator's five rulings on `b015`'s evidence: standalone `lib`+`[[bin]]` (only shape with real read-only check), emitter owns every byte (no second formatter — that was the `45caae82` failure), `ir:check` byte-exact + orphan-aware + composed only from checkers, no type-mirroring crate (`ts-rs`/`schemars` mirror types; the IR emits from instances), report gating deferred. Machinery plus the TypeScript target only; the other four emitters are a follow-up |
| `g13-b023-history-center-tree-core` | `g13/batch-cards/023-history-center-tree-core.md` | `thread/g13-023-history-center-tree-core` | `deepseek-v4-flash` (`xhigh`) | merged | Commits `0e1bb49a`+`d0ac9b05` → merge `3580399d` (paired with `b024`). Corrections at review: the package index still exported the removed `isForkPoint`, and `HistoryBranch.recordedAtMs` modelled a field nothing upstream will populate. HistoryCenter v2, core half. Replaces the fork-expander model with a unified history tree: every entry in the fork graph exactly once at its true position, spine plus indented runs. v1 keyed rows off Longhorn's divergence id, which is relative to the current branch, so distinct forks collapsed onto one entry ("6 branches off the root edit"). v2 attaches each run at the last entry the paths actually share and never keys off divergence. Ships the stitcher as a pure tested function so hosts do not reimplement it. Rulings: clean break, no shim (merged today, never published, one consumer); no client-side clocks (verified: Longhorn's history-tree domain has no recorded-at field at all); depth cap 3 with true branchIds preserved. Web rendering, lanes and specimens are `024`; native parity follows that. **Correction to apply at review** (Longhorn thread, 2026-08-11, after dispatch): the agreed `recorded_at` lands on `HistoryEntryMetadata` only, with no branch-level equivalent proposed — so drop `recordedAtMs` from `HistoryBranch` and derive a run caption's relative time from its own run's most recent entry. The same thread confirms `ForkNavigationTarget::Checkout` cannot express an empty branch head, which independently justifies the card's rule that captions rename but never navigate |
| `g13-b025-codegen-remaining-emitters` | `g13/batch-cards/025-codegen-remaining-emitters.md` | `thread/g13-025-codegen-remaining-emitters` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `5d9edc9d` → merge `945f55d2`; machinery verified byte-for-byte unchanged, all six targets deterministic, per-target drift detection confirmed, 31 tests. Schema test has a real negative case; `jsonschema` correctly scoped dev-only as a test oracle. Fixed at review: clippy had never linted the crate's tests (`--all-targets` missing in both this card and `b022`). Closes `g13.003`. Adds the four remaining emitters the milestone names — JSON surface, JSON Schema, registry, conformance vectors, docs fragments (five artifacts; "JSON/schema" is two) — on the machinery `b022` proved. Machinery is explicitly frozen: a target needing a change to `emit.rs`/`check.rs`/`write.rs` is a stop condition, not a refactor. Carries `b022`'s R1–R5 forward, with the JSON Schema hand-emitted rather than reached for via `schemars`, since these targets emit from model instances and type-mirroring derives emit from Rust type structure. Proves the milestone's own acceptance directly: one fixture change updates every declared artifact in one `ir:build` |
| `g13-b026-dismiss-outside-interact` | `g13/batch-cards/026-dismiss-on-outside-interact-prop.md` | `thread/g13-026-dismiss-outside-interact` | `deepseek-v4-flash` (`high`) | merged | Commit `4418eb58` → merge `c468b434`; first attempt died on a provider thinking-loop with zero output, redispatched at `high` with an explicit one-component-at-a-time instruction. Every default verified against the merge-base rather than assumed; all fifteen preserved, Svelte and React agree. Tests fire a real outside mousedown in both directions. 908 → 954. Accepted with debt: twelve `OPEN_GAPS` entries in `contract-spec-drift` (previously empty) — the right register over `WEB_ONLY_PROPS`, since refusing dismissal is a real capability rather than a web-platform artifact. Fourteen components hardcode `dismissOnOutsideInteract` in their layer registration; only `Popover` exposes it, so outside-dismissal is refusable in exactly one place. Defaults must equal today's shipped behaviour — verified per component, and `Dialog`/`Drawer` already register `false`, so a blanket `true` would have made two modals dismissable. Found while scoping: `contract-prop-drift` is one-directional, so an implemented-but-undocumented prop can never fail the gate — that is why this went unseen. Recorded as a papercut with its parser bug; not fixed here |
| `g13-b027-dismiss-native-and-drift-direction` | `g13/batch-cards/027-dismiss-native-parity-and-drift-direction.md` | `thread/g13-027-dismiss-native-and-drift-direction` | `deepseek-v4-flash` (`high`) | merged | Commit `2eb94a16` → merge `ada91cae`; hit its deadline having committed all three parts cleanly. `OPEN_GAPS` back to `{}`, twelve specs carry the field with web-matching defaults, all twelve renderers read it (render 167→179). The 54-component reverse backlog was mostly snippet slots — separating them by `Snippet` type did the work, not an exemption list; four components / seven props remain, each justified. Parser fixed with the real leaking line as its fixture. Discharges the debt `b026` recorded and fixes the gate that hid it. Part 1: rule on the twelve `OPEN_GAPS` entries — add the field to the specs, or state in code what distinguishes them from `Dialog`/`Drawer`, which already model dismissal. Part 2: `contract-prop-drift` never checks implemented→documented, which is why the whole `b026` gap was invisible; it also lifts `and`/`time` out of a placeholder string. Fix the parser, separate snippets from props, enforce. Explicitly forbids inventing a second empty escape list to make the gate pass |
| `g13-b028-history-center-v3-core` | `g13/batch-cards/028-history-center-v3-core.md` | `thread/g13-028-history-center-v3-core` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `680f3c64` → merge `2a6d3af9` (paired with `b029`). Ran at `xhigh` after two restarts that cost only reading time. Regression test pins both row sets, so two forks at one entry and a fork off a fork differ by data. Found in review: `check:svelte` caught 17 type errors that 482 passing core tests could not see — `HistoryCenterOpenFork.inner` declared as a single fork while its comment and every call site treated it as a keyed map. The card had omitted that gate from its own validation list. HistoryCentre v3, core half. Deletes the v2 stitcher rather than repairing it: `childrenOf` is a vector and `emitRun` writes runs back to back at one depth, so several forks land at one position. The premise was overstated in the first version of this card and in the source handoff, and Longhorn corrected it in `db9ac5c7`: `depth` and `lane.parentBranchId` *do* separate the two shapes. Measured on main — two-forks-at-one-entry gives `depth=1/parentBranchId=main`, fork-off-fork gives `depth=2/parentBranchId=lead`, and past the depth cap a five-deep chain saturates both `f3` and `f4` at `depth=3`. The encoding is weak, not wrong. The redesign stands on the separate fact that several forks at one node are ordinary and a tree cannot draw that unambiguously. Rulings: core flattens and no renderer recurses (`poodle-render/src/tree.rs` already does this, and native parity is queued, so a self-rendering component has no native counterpart); no Longhorn dependency, so the three controller operations arrive as callbacks; core reverses newest-first pages once so every depth agrees; `forkCount = continuationCount - 1`; core holds only open runs; rename survives in the opened region. Both v2 defects — oldest-first-only, and silent drops on a missing shared prefix — are forbidden from carrying forward |
| `g13-b029-history-center-v3-web` | `g13/batch-cards/029-history-center-v3-web.md` | `thread/g13-029-history-center-v3-web` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `16b68e61` → merge `2a6d3af9`; hit its time limit during the visual enumeration with 11 files uncommitted, finished by the orchestrator. One loop per runtime, no `svelte:self` and no self-import, lane CSS deleted with no clamp replacing it, eight specimens per runtime with identical labels. `test:components` 944/30-fail → 980/0. Also carries Longhorn `777de887`: `precedingContinuationCount` replaces `rootContinuationCount` (the old name described the history root even on a run page), and R4 is saturated in the wording to match an implementation that already floored it |
| `g13-b030-history-center-fork-select` | `g13/batch-cards/030-history-center-persistent-fork-select.md` | `thread/g13-030-history-center-fork-select` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `61d3f49d` → merge `c6590823`; clean, all seven gates exit 0, core 482→487 and components 980→982. Verified: the `chosen === null` gate is gone, select emits only `loadContinuationRun`, checkout emits only `checkoutContinuation` and clears the anchor's disclosure, `Select`/`IconButton` imported and the hand-rolled list deleted, Longhorn untouched. Raised a real a11y finding — `Select`'s portalled listbox escapes `Popover`'s focus trap, since `trapFocusKeydown` only intercepts targets inside the surface. Field defect from a live screenshot: opening a forked node shows only the forks, and choosing one hides the selector with no way back short of closing and reopening the parent. Two causes, one per layer — core gates the picker row on `level.chosen === null` so it vanishes after a choice, and the renderer hand-rolls a button list with a "Choose" `Button` instead of using Poodle's `Select` with a confirm `IconButton`. Also splits select from checkout: selecting shows that fork's entries and commits nothing, while checkout makes the fork primary — the root list becomes that fork's history and the old root becomes a fork at the same entry, with no delta applied. The first version of this card said checkout changes nothing on screen, which was wrong; nothing in v3 ever implemented the swap. Poodle emits the command and renders the root the host supplies rather than fabricating it, since the authority may also undo the operator back to the fork entry. And the vocabulary becomes checkout, not prefer — `prefer` is Longhorn's word for its own operation and v2 already used `onCheckoutVersion`. Disclosing now selects the preferred fork so the region is never empty |
| `g13-b031-nested-dismiss-and-select-affordance` | `g13/batch-cards/031-nested-dismiss-and-ghost-select-affordance.md` | `thread/g13-031-nested-dismiss-and-select-affordance` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `4daf6993` → merge `ef12a3bf`; first attempt died on a provider stream closure with zero output, redispatched at `xhigh` since that is not a thinking loop. Verified: the four peer tests pass unedited, `Popover.svelte` has a zero-line diff so `contains` was not widened, both `▾` workarounds deleted, HistoryCentre untouched, all seven gates exit 0. Found two stale references it correctly left alone (outside writable paths, ungated): `select.rs:24` still says Ghost "hides the chevron", and `recipe-inventory.json:1514` keeps an orphaned `--poodle-recipe-order-by-chevron-text`. Two shared-primitive defects from a live HistoryCentre screenshot, neither of them HistoryCentre's. Clicking a `Select` option inside a `Popover` dismisses the Popover — a regression from the peer-dismissal change, which returns every non-containing layer and cannot tell a peer from a host; the Select portals its listbox to `body`, so the Popover does not contain it. Fix is ancestry: layers record the layer on top at registration, and outside-dismissal spares any ancestor of a containing layer. Widening `Popover.contains` is explicitly rejected — it would make a host know every component that might portal inside it. Second: `Select` gates its chevron on `variant !== "ghost"`, so a ghost Select signals nothing; `OrderBy:318` and `FilterBuilder:346` already work around it with a literal `▾`, which is what makes it a `Select` defect |
| `g13-b032-history-center-multi-fork-rename` | `g13/batch-cards/032-history-center-multi-fork-rename.md` | `thread/g13-032-history-center-multi-fork-rename` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `8da4141a` → merge `321fc790`; clean, all seven gates exit 0, components 982→990. Verified badge gone from both runtimes and CSS, pencil between `Select` and checkout, one rename path (3 functions, 10 call sites). Review also finished the `precedingContinuationCount` rename, which `2a6d3af9` left stale in four live sites while every gate stayed green — `check:svelte` covers `src/**` only, so `test/` is unchecked and neither preview is checked at all. The multi-fork picker cannot rename a fork while the single-fork path can, so a name is editable only when there is one continuation — backwards, since naming matters most when several must be told apart. Adds a pencil `IconButton` between the `Select` and checkout that renames whichever fork the `Select` shows. Reuses `startRename`/`commitRename`/`finishRename` and the existing `RENAME` event rather than building a second rename path. Also deletes the "Current" badge, which is wrong rather than merely confusing: `historyCenterForksAt` filters out the continuation already on the list, so the picker shows alternatives only and never the branch the main list is on. `preferred` stays on the record and keeps disabling checkout, so the fact survives without the contradictory label. Runs alongside `b031`, which owns `Select`/`OrderBy`/`FilterBuilder` and is barred from HistoryCentre, so the two do not overlap |
| `g13-b033-history-center-unified-fork-row` | `g13/batch-cards/033-history-center-unified-fork-row.md` | `thread/g13-033-history-center-unified-fork-row` | `deepseek-v4-flash` (`xhigh`→`high`) | merged | Merge `aa0350d2`; finished by the orchestrator after the worker looped at `xhigh` and again on resume at `high` — first time the lower tier looped too, so the tier is not the variable. Ten tests still asserted the old layout. Found a real defect in review: React's `HistoryCenter` called `formatRelativeTime`, which existed only in the Svelte runtime, so the trigger threw `ReferenceError` on timestamped data. Also added `afterEach(cleanup)` to the Svelte suite, whose helpers query `document` globally and manufactured cascading false failures. components 990→996. Four changes in the opened fork region: `git-branch` icon in the `Select` trigger, `variant="default"` instead of ghost, an opt-in delete `IconButton` between pencil and checkout, and — the structural one — the single-fork path adopts the same row with a **disabled** `Select`. That supersedes `b030`'s R1 (`forkCount === 1` renders no picker) and retires the run-header layout along with the raw-`<button>` rename `b032`'s R4 deliberately left alone: the two layouts were separate then and are not now. The run header's entry count and relative time must survive the move — losing them is a regression, not a simplification. Delete is opt-in by callback presence, emits for the selected fork, and carries no Poodle-side confirmation; Longhorn is building the operation now, and `prune_to` is budget retention rather than this |
| `g13-b034-history-center-field-defects` | `g13/batch-cards/034-history-center-field-defects.md` | `thread/g13-034-history-center-field-defects` | `deepseek-v4-flash` (`xhigh`, redispatched) | merged | Commit `6257077f` → merge `8e2f9e50`; core 496→503, components 1004→1010, all twelve gates exit 0. First attempt died on a provider stream closure after Step 2 only, exactly as `b031` did — not a thinking loop, so redispatched at `xhigh` rather than dropping a tier. The repro test it had landed was left in place and the resume started at Step 3. Review verified the two constraints held (no `SYNC`/`REFRESH` event, no pages-identity trigger) and independently re-tested the load-bearing no-loop claim rather than taking the prose: drop emits exactly one `loadContinuations`, a second pass emits none, and after the host answers, R3 chooses the line just left rather than the fork now on the spine — so it settles. Worker found a third defect the card did not name: `CONFIRM` only committed `pick`, so enabling Checkout on the single-fork row would have shipped a dead button. It now commits `pick ?? chosen`, which is what R1's "counts as picked" implies. Three defects Loophole hit in live use, reported through the maintainer. (1) The picker row's `disabled: forkCount <= 1` is passed both to the `Select` — correct, nothing to pick — and into `pickerActions`, where it greys out Checkout on the single-fork row. That is the one case checkout-without-moving-HEAD exists for. (2)+(3) are one root cause, and Loophole's stated diagnosis for (2) is wrong: the report says the machine never re-reads props after OPEN, but `rows` and `machineContext` are `$derived` in Svelte and recomputed per render in React, so props already flow live. The card forbids adding a `SYNC`/`REFRESH` event on that reasoning. The real cause is a stale open level: it caches `chosen`/`runPages` at load, nothing invalidates them when the host supplies root pages that already contain that run, and `pushRun` splices the cached copy under a spine that now holds the same entries. Reproduced before dispatch — `historyCenterVisibleRows` returns `entry:c1 entry:c2 picker:c2 entry:f1 entry:f2 entry:f1 entry:f2`, which is exactly the reported `each_key_duplicate … at indexes 7 and 8`, and also explains the "list unchanged until reopen" reading (it changed; it grew a duplicate run). Ruling: detect staleness from the data (the shown fork's first entry now on the spine), keep the level open, drop its loaded data and re-request through the existing `loadContinuations`, showing `not-yet-loaded` meanwhile. A `pages`-identity effect is explicitly rejected — a host rebuilding its array each render would loop. The failing test ships in the card and must be landed and seen to fail before the fix |
| `g13-b035-shell-scene-rust-authoring-and-web` | `g13/batch-cards/035-shell-scene-rust-authoring-and-web.md` | `thread/g13-035-shell-scene-rust-authoring-and-web` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `db4e4510` → merge `492f240c`; clean first run, all gates exit 0 post-merge (core 505, components 1012, ir:build/ir:check green). First card to author a value into Scene IR — b011 built the SHELL-01..SHELL-10 vocabulary and nothing had used it. R1 verified empirically rather than read: adding a fourth tab to `preview_shell.rs` makes `ir:check` exit 1 with "the committed fixture differs from packages/codegen/src/models/preview_shell.rs; run effigy ir:build". Rust is genuinely the source; the JSON is a generated intermediate produced by `--author-shell`. R2 verified: no `poodle-ir` diff, no `[[bin]]`, `synthetic-model.json` untouched. The parity test is scene-derived and byte-exact over both committed web artifacts, as required — no hand-listed expectation. **Feedback for `g13.008`: Scene IR carries no label or placeholder fields.** `SceneAxis` is `{kind, values, description}` and `SearchConfig` is `{case_insensitive, fields}`, so "Theme" and "Find component..." are projected by the emitter from `SceneAxisKind`, not authored in the scene. That satisfies R4's intent — one Rust home, both shells read it verbatim — and authoring them would have needed the schema change R2 forbade. Whether presentation strings belong in Scene IR is a real schema question the pilot should answer. Two papercuts appended, both pre-existing and outside writable scope: the Svelte SHELL-08 URL-persistence effect never fires where React's does, and both catalogue-landing grids render all components ignoring the filtered prop. `036` takes the native shells and closes `g13.004` |
| `g13-b036-shell-scene-native-shells` | `g13/batch-cards/036-shell-scene-native-shells.md` | `thread/g13-036-shell-scene-native-shells` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `97da7195` → merge `22307726`; clean first run, every gate exits 0 post-merge. **`g13.004` closes here.** R1 held under independent check: neither preview manifest gained `poodle-ir` or `poodle-codegen`, and both generated Rust artifacts carry zero Poodle imports. R2 looks violated and is not — `targets/shell.rs` shows 9/5, but the whole diff is `fn` → `pub(crate) fn` on `axis_label`, `humanize` and `kind_name` plus doc comments, and both committed `.ts` artifacts are byte-identical to main. Sharing the label projection is the better call than duplicating it: a second copy of "Theme" in a sibling target is exactly the two-homes problem R3 exists to prevent. Evidence was produced rather than asserted — removing the Density axis and running one `ir:build` dropped it from all four shells, confirmed by browser for Svelte/React, OCR of a GPUI screenshot (`THEME SIZE CONTRAST 0.25 SEARCH`, no DENSITY), and a Jetstream pixel-diff confined to the controls bar. Rust drift proven by planting a line and watching `ir:check` name `preview-shell.rs`. Environment finding, now in `PAPERCUTS.md` and the log: `jetstream-poodle` is a sibling-repo path dep (`../../../../jetstream/…`) that resolves from the main checkout but not from a worktree, so Jetstream cannot build in a worktree without a `poodle-wt/poodle` symlink. The worker created one pointing at its own worktree; at cleanup that would have dangled, so it now points at the main repo. Any future Jetstream-touching card needs this. Closes `g13.004`. The constraint that shapes the card is `b003 R2`, which forbids both previews depending on `poodle-ir` during the pilot and forbids anything depending on `poodle-codegen` at all — so the natives cannot import the model, the authoring module, or the fixture's types. They get a self-contained generated Rust artifact instead (plain data, no `use` of any Poodle crate, mirroring the TS artifact's zero imports), on the `poodle-tokens` `#[path = …]` precedent `b003 R1` names. The card says explicitly that if that looks impossible, stopping is a finding for `g13.008`, not a licence to add the dependency. A sibling emitter target, not a change to `targets/shell.rs`: it hard-codes `.ts` and `b035`'s parity test byte-compares its render against both committed web artifacts, so editing it would break the thing that makes `b035` mean anything. Measured before-state: Jetstream's five labels already agree with the scene but are hand-written in `shell.rs`; GPUI has three deltas in `main.rs` — `THEME` and `SEARCH` upper-case, and contrast reading `Neutral contrast`. Casing may stay as styling; the different word may not. GPUI's contrast is already a continuous range matching the web's, so every capability exists in both natives — this is ownership and wording, not a missing control. Requires the `b035` removal test repeated across all four shells, and — per the standing rule that both natives are pixel-verifiable — screenshots of both rather than "it compiles". A moving native baseline is a stop condition, not a refresh |
| `g13-b037-focus-ring-radius-audit` | `g13/batch-cards/037-focus-ring-radius-audit.md` | `thread/g13-037-focus-ring-radius-audit` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `f7946bec` → merge `75d59c48`; clean first run, all gates exit 0. **R1 was the whole value of the card: 22 of the 34 scan hits were false positives.** A blanket sweep would have made 22 wrong changes and looked green doing it. Final split — 7 confirmed square-on-rounded and fixed (stepper `__summary`/`__trigger`, filter-toolbar header button, changed-files toggle, message-center interactive row, hover-card and popover triggers); 5 correctly square and baselined with measured reasons (rings 4.6px clear of any arc, plus resize-handle's square grip); 22 false positives where the radius arrives via the focus rule itself, a sibling the ring actually lands on, or a base class. Spot-checked the classification rather than accepting it: `checkbox.css` applies its ring to `.poodle-checkbox__indicator` through a `+` combinator, and that element carries a per-size radius — my scan had mis-attributed it to `__control`, so the false-positive call is right and the scan was wrong. Gate independently mutation-tested: removing one of the seven fixes makes `docs:focus-ring-drift` exit 1 naming the file and selector. Radius only — no outline colour, width or offset moved anywhere. An outline follows its own element's `border-radius`, so an element drawing a focus outline with no radius renders a hard square. HistoryCentre had three (fixed in `7c88908e`); a scan finds 34 more. R1 is the load-bearing ruling: the scan is a crude regex with false positives, so every candidate is verified in a browser and sorted into confirmed / correctly-square / false-positive — a blanket sweep is a failed card even if the gates pass. R5 carries the technique, because this defect evades the obvious check: `element.focus()` does not trigger `:focus-visible` and a Tab sweep misses roving-tabindex rows, which is how HistoryCentre's evaded one; CDP `forcePseudoState` is how they were measured. Radius only. Split from `038`, which takes the 79 sheets with no focus treatment and the 56 that do not suppress the UA outline |
| `g13-b038-focus-treatment-coverage` | `g13/batch-cards/038-focus-treatment-coverage.md` | `thread/g13-038-focus-treatment-coverage` | `deepseek-v4-flash` (`xhigh`, ×2) | merged | Commit `95c9c02a` → merge `6b84a12b`; all gates exit 0. Two runs. The first hit its 45-minute deadline — a third failure mode alongside stream closures and thinking loops, and this one was the card's fault for bundling per-component discovery with a mechanical sweep. It left 28 stylesheets and the extended gate uncommitted, with 27 new `:focus-visible` rules and **no classification recorded**, which is what R1 forbids. Resumed with the verification it owed as step one, and explicitly told not to rationalise a change already in the tree. Outcome was non-trivial: **27 re-verified under CDP `forcePseudoState`, 25 kept, 2 removed** — detail-item and field had retargeted a ring onto a 1.25em info icon. The more valuable finding: of the ~18 remaining stacked-outline flags, **only three were real; four were bugs in the gate itself** (comma-list per-part miscompare, `outlineSuppressed` rejecting outline *replacement*, and a `tabindex="-1"`/hidden-input heuristic) and the rest were mislabels. It fixed the gate rather than editing CSS to satisfy a wrong rule — the failure mode a gate-first card most invites. Review re-planted both regression kinds independently: absent and stacked each caught, clean at rest. Real fixes were agent-chat-input (two rings measured, its own plus the field's `:focus-within`) and audio-player/order-by controls (UA-only `outline: auto 1px`, given R3 rings). CSS, contracts and the gate only — no component or markup file touched |
| `g13-b039-settings-shell` | `g13/batch-cards/039-settings-shell.md` | `thread/g13-039-settings-shell` | `deepseek-v4-flash` (`xhigh`, ×2) | merged | Merge `90594196`. Two runs. The first stopped correctly at a stop condition — SidebarNav had no way to carry a tooltip on a truncated group heading — and touched nothing; ruled as R4a (native `title`, the house idiom at 13 components against 2 using `<Tooltip>`, with AT already served by the group section's `aria-label`). The second died on a provider stream closure, the third this session, with the work complete but uncommitted and unvalidated. Finished by the orchestrator rather than redispatched, since the remainder was validation. **The refused close did not hold.** `Dialog` assigns to its own `open` prop (`Dialog.svelte:200`), so an unbound value lets it mutate its local copy where the parent cannot veto — and a refusal is exactly the case where the parent's state deliberately does not change, so the stale `false` stuck and the dialog closed against the refusal. Mirrored into bindable state. React was never affected: one-way props mean the parent's render always wins. The component's own test caught it, which is why writing the tests mattered more than the component compiling. **Three registration points were missing from the card's own R6 list**, all the orchestrator's omission: `docs/contracts/components/README.md`, `docs/contracts/README.md`, and `shared-demo-app-audit.json`'s `exportCount`, the last of which only fails once `ci:web` regenerates the parity report (201→202). Worth adding to R6 for the next new-component card. `closeRefusedReason` shipped undocumented and `docs:contract-drift` caught it. A new component replacing Longhorn's settings dialog — the last substantial design still living in an authority package. Designed, not ported: read the existing 435-line implementation once for the data shape, then close it; a layout mirroring its structure is a failed card. R1 turns the maintainer's seven defects into acceptance criteria, of which the durable one is the group label — Longhorn composes `${module} · ${section}` and it wraps, Longhorn will stop, but a component assuming short labels breaks on the next host, so one line, truncate, tooltip, enforced by a test. R2 rules it a composition with no `packages/core` machine, following DetailShell/PickerShell/FormShell/ScrollShell, which all ship CSS and no core logic. R4 pins it to the eight primitives the handoff names, all verified present in both web runtimes with contracts; hand-rolling an equivalent fails the card, and a primitive genuinely missing something is a finding rather than a local workaround. R5 scopes it web-only on the HistoryCenter precedent (contract with props, no Rust spec — `docs:spec-drift` only checks contracts that have one) and requires adding it to the native registration gap inventory in the same commit. R6 enumerates every registration point including `test/fixtures/component-props.ts`, since the smoke and parity suites are glob-driven and a missing fixture reads as a pass |
| `g13-b040-dock-region-tab-passthroughs` | `g13/batch-cards/040-dock-region-tab-passthroughs.md` | `thread/g13-040-dock-region-tab-passthroughs` | `deepseek-v4-flash` (`xhigh`, ×2) | merged | Commit `51b0e76c` → merge `6b2673a9`; all gates exit 0, components 1014→1051. Two runs: the first stopped on the condition R4 named — documenting the props trips `docs:spec-drift` against `DockRegionSpec` — and asked whether to hold for `g13.014` or invent a scoped exception. Neither. `tabVariant`, whose naming convention the five follow, is already implemented in Svelte and deliberately absent from `dock-region.md`'s Public Props table, which is why it does not trip the gate; it sits in `contract-prop-drift`'s BASELINE as `svelteOnly` with the reason inline. The five joined that entry (R4a), documented in contract prose rather than the table, recorded as a tranche that `g13.014` collapses. `contract-spec-drift`'s `OPEN_GAPS` stayed empty — its own comment says an entry there means a prop escaped the shared spec surface, which is what the gate exists to stop. Verified rather than accepted: five props forwarded at all three Svelte call sites (React routes its three through `stripTabs`), overflow trio absent per R1a, `OPEN_GAPS` still empty, hook named `--poodle-recipe-tabs-active-underline-border` beside its `-active-outline-border` sibling and shared across both orientations with the current value as fallback. **The card cited `docs/contracts/003-recipe-hooks.md`, which does not exist** — the orchestrator's error. The worker found the real grammar at `docs/architecture/007-appearance-recipe-contract.md`, used it, and said so instead of inventing a spelling |
| `g13-b041-button-slice-rust-authoring-and-web` | `g13/batch-cards/041-button-slice-rust-authoring-and-web.md` | `thread/g13-041-button-slice-rust-authoring-and-web` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `ff391651` → merge `50031f13`; clean first run. Button is the first **real** component authored in Rust IR — the R2 proof held: renaming a state attribute in `button.rs` and rebuilding moved the DOM in both web previews with no hand edit. All 34 props, all 11 data attributes and every pixel unchanged (R3); the R4 hand-written exception inventory is in the log, per runtime. **Shipped a real regression that the card's own gate list could not see.** `Button.svelte`/`Button.tsx` imported the artifact across a package boundary (`../../preview/src/generated/button`), and the packed tarballs ship `files: src` without the preview package, so `test:web-pack-install` — green on `main` — failed on the branch. Cause was the orchestrator's card: its Writable Paths named `preview/src/**/generated/**`, copied from `b035` where the consumer *was* the preview. A component's artifact must live in the package that ships it. Fixed at review by emitting into `packages/{svelte,react}/components/src/generated/button/` and repointing four importers. **`test:web-pack-install` is not in `ci:web`**, so all fourteen gates the card listed passed while the packaged build was broken. Any card that changes where code is emitted has to run it, and it likely belongs in `ci:web`. One out-of-scope touch, accepted: `targets/shell.rs` `fn camel_case` → `pub(crate)`, visibility only, no output change — the same sharing-beats-duplicating call `b036` made. Also fixed a shared-root orphan sweep so `shell-scene` and `button-ts` can co-exist under one `generated/` tree |
| `g13-b043-self-container-queries` | `g13/batch-cards/043-self-container-queries.md` | `thread/g13-043-self-container-queries` | `deepseek-v4-flash` (`xhigh`) | merged | Merge `9bba0956`; finished by the orchestrator after a stream closure — the fourth this session — left three stylesheets done, no gate and no log. Real split was **3 genuine, 2 false positives**: `form-actions` and `form-layout` target descendants (`__danger`, `__grid`), which is correct usage, and were flagged only because the ad-hoc scan's `[a-z0-9-]+` class regex stops at the underscore. The worker had in fact changed exactly the three genuine ones. Wrapper route verified in a browser: the reference DetailItem at 240px went from columns `180px 20px 0px` to a single `216px`, value intact. **The gate needed two fixes of its own before it was right** — it first reported five, because it matched every class token in a selector rather than the *subject* (`.a .b__c` styles `b__c`; `.a` is an ancestor qualifier) and because it parsed comment prose as CSS, inventing classes `001` and `043` from "g13.043". Subject-only matching plus comment stripping took it to two. Both survivors baselined with reasons: detail-item's is `[data-span]`, deliberately section-keyed per R2; detail-section's separator inset is marked **UNVERIFIED** and logged in PAPERCUTS rather than asserted. Mutation-tested with a real planted self-query |
| `g13-b044-preview-shell-defects` | `g13/batch-cards/044-preview-shell-defects.md` | `thread/g13-044-preview-shell-defects` | `deepseek-v4-flash` (`xhigh`) | merged | Commit `18be4a67` → merge; clean first run, all gates exit 0, components 1059. Two defects `b035` found while doing something else, measured live and correctly left out of scope at the time. `hasMounted` was a plain `let` in a runes component, so the URL-persistence `$effect` never re-ran and Svelte silently diverged from React on `SHELL-08`; the catalogue grids rendered `componentsByTag()` while using the filtered prop only for the count line. Both re-verified independently at review rather than read from the log: density `compact`→`comfortable` now follows into the query string, and with a `date` query the header reads 6 while `.poodle-catalogue-landing__grid` renders exactly 6 children, down from ~165. Zero console errors. Preview-only; `packages/codegen`, `packages/render` and every `generated/` artifact untouched while `b042` ran alongside |
