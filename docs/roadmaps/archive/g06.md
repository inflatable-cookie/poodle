# g06 — Shared multi-renderer contracts

Status: archived
Kind: roll-up

## Generation intent and boundary

Removed GPUI bias from shared Rust contracts through typed tokens, layout,
events, style descriptors, adapter traits, and expanded primitive/composite specs.

## Shipped outcomes

- Renderer-neutral contracts and typed token resolution.
- Explicit layout, interaction, style, and adapter boundaries.
- Jetstream constraint record and parity validation tooling.

## Current canonical destinations

Current Rust packages, architecture, contracts, and adapter implementations
carry the lasting rules.

## Material migrations, limits, and retained risks

The split-crate era described here was superseded by `poodle-specs` and later
package consolidation. Jetstream admission remains separately held.

## Deferred or unresolved items

Renderer build-out moved to `g07`; Jetstream admission is retained in current
triage, not this archive.

## Selected evidence

Git preserves all fifteen tasks and the split-crate transition.

## Succeeding generation

`g07` implemented GPUI adapters and downstream proof.

