# g04 — Underlay parity and specialist component depth

Status: archived
Kind: roll-up

## Generation intent and boundary

Closed the major reusable component gaps needed for Poodle to replace Underlay
as the shared UI library while leaving native counterparts for later work.

## Shipped outcomes

- Dialog, file, temporal, list, data, operational, media, and editing families.
- Existing component hardening and specimen coverage.
- Clear separation between reusable Poodle surfaces and app-specific widgets.

## Current canonical destinations

Component contracts, guides, Svelte implementation, preview catalogue, and
product guardrails carry the live surface.

## Material migrations, limits, and retained risks

Task-local `planned` labels were stale against the generation closeout and
later shipped catalogue. GPUI parity followed in later generations.

## Deferred or unresolved items

Native parity moved to `g05` and later multi-renderer work; no executable g04
commitment remains.

## Selected evidence

Generation closeout and the current component catalogue settle the outcome;
git preserves all eighteen task files.

## Succeeding generation

`g05` established the GPUI foundation and cross-runtime baseline.

