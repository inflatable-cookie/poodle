# g05 — GPUI foundation and cross-runtime baseline

Status: archived
Kind: roll-up

## Generation intent and boundary

Established GPUI theme/runtime integration, renderer-neutral spec types,
native preview, accessibility proof, parity reporting, and demo alignment.

## Shipped outcomes

- Structural, input, selection, overlay, data, media, and workstation spec coverage.
- Native accessibility and cross-runtime evidence baseline.
- Shared demo contract and rebuilt Svelte demo coverage.

## Current canonical destinations

Rust packages, architecture, component contracts, preview applications, and
current parity checks carry the durable result.

## Material migrations, limits, and retained risks

The original GPUI-biased split-spec shape was redesigned in `g06` and later
consolidated into current packages.

## Deferred or unresolved items

Expanded renderer implementation and downstream proof moved to `g06`/`g07`.

## Selected evidence

Current package history and git at `85741961d` preserve the fourteen tasks.

## Succeeding generation

`g06` created the shared multi-renderer contract layer.

