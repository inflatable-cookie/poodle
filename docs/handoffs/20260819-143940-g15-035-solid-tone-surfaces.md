---
title: g15.035 solid tone surfaces worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-19
updated: 2026-08-19
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260819-143940-g15-035-solid-tone-surfaces.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g15, tone-fill]
---

## What This Thread Was Doing

The operator asked for opt-in solid-color variants of Callout and
RemediationBanner, then added Pill before the worker lane was dispatched. The
orchestrator expanded `g15.035` rather than create two overlapping PRs. All
three components now share one approved additive API:
`fill: "tint" | "solid"`, default tint.

The earlier two-component handoff at
`docs/handoffs/20260819-140017-g15-035-solid-status-surfaces.md` is superseded.
Do not combine or execute it with this file. This handoff is the complete
worker instruction.

## Why It Matters

These are public tone-bearing primitives used throughout Poodle's consumers.
The solid treatment must be one dependable contract across Svelte, React,
shared Rust composition, and GPUI. The work also has to preserve every current
tint and Pill appearance while producing readable solid fills in all twelve
themes. It lands before visual conformance and v0.2.0 certification.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `c671a431bc539c97baced8a71a0b56df1418f7c3`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `c671a431bc539c97baced8a71a0b56df1418f7c3` before this handoff was created
- **Planning checkout:** clean at the planning base
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:**
  `docs/roadmaps/g15/035-solid-status-surfaces.md` is ready; the g15 front
  doors and dispatch ledger name the three-component lane; the earlier handoff
  is marked superseded
- **Worker branch:** `t3code/g15-035-solid-tone-surfaces`
- **Worker worktree:** harness-managed; expected placeholder
  `/Users/tom/.t3/worktrees/poodle/g15-035-solid-tone-surfaces`
- **Worktree creation command:** the launcher should supply the worktree. Only
  if fallback is required, read `.agents.local.env` as data, validate
  `AGENTS_WORKTREE_CONTAINER_DIR`, then run `git worktree add
  <validated-container>/poodle-g15-035-solid-tone-surfaces -b
  t3code/g15-035-solid-tone-surfaces origin/main`
- **Worker worktree policy:** use a clean, dedicated, non-`main` registered
  worktree supplied by the launcher even when its path or branch differs from
  these placeholders. Record the actual values and do not create another
  worktree. If the current context is unusable, use the named worktree when it
  matches; only then use the validated manual fallback. Never use `/tmp`,
  `TMPDIR`, or a guessed path.
- **Active spec lane:** component contracts plus
  `docs/contracts/004-shared-control-types.md`; no provisional architecture
- **Roadmap milestone:** `docs/roadmaps/g15/README.md`
- **Ready cards, in order:**
  `docs/roadmaps/g15/035-solid-status-surfaces.md`
- **Allowed runway:** `g15.035` only
- **Remaining card budget:** one card, one PR, then stop
- **Dispatch topology:** independent of the next specimen-curation child,
  `g15.022`
- **Parallel safety check:** `g15.022` owns audio/music specimen curation. This
  lane owns Callout, Pill, and RemediationBanner contracts, components, styles,
  types, renderers, specimens, generated display-model artifacts, focused
  evidence, and one unique log. Do not absorb audio/music work.
- **Canonical refs:** `AGENTS.md`,
  `docs/architecture/001-poodle-system-shape.md`,
  `docs/architecture/product-guardrails.md`,
  `docs/contracts/001-working-rules.md`,
  `docs/contracts/004-shared-control-types.md`,
  `docs/contracts/components/callout.md`,
  `docs/contracts/components/pill.md`,
  `docs/contracts/components/remediation-banner.md`, and
  `docs/specs/022-packaging-versioning-and-release-channel-rules.md`
- **Model capability profile:** frontier coding model, high reasoning; this is
  public API, cross-runtime color math, generated evidence, and accessibility
- **Tool/runtime restrictions:** headless only. Never run windowed,
  native-visual, conformance, Jetstream, or release selectors.
- **Required validation:** focused paired component, Rust render/spec,
  contrast, and specimen evidence; `effigy ir:check`;
  `effigy test:components`; `effigy check:svelte`; `effigy react:build`;
  `effigy test:parity`; `effigy check:gpui`; `effigy regressions:native`;
  `effigy test:web-pack-install`; `effigy docs:check`;
  `git diff --check origin/main...HEAD`
- **PR base/head:** `main` / selected worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review after the pushed PR
- **Merge authorisation:** absent; the worker must not merge

## Boundaries

Keep this run inside `g15.035`.

- **In scope:** the shared `ToneFill`; Callout, Pill, and RemediationBanner
  contracts, shells, shared CSS, Rust specs/renderers, focused tests,
  representative Svelte/React/GPUI specimens, Callout and Pill's
  presentation-only specimen-model entries and regenerated artifacts, contrast
  evidence, and one August batch log.
- **Out of scope:** other component APIs, global status-tone values, token
  schema, shared catalogue navigation/shells, audio/music curation, Jetstream
  integration, conformance architecture, release work, and
  `.github/workflows/`.
- Keep tint behavior byte-for-byte or observably unchanged. The only adjacent
  correction is the card's explicit Rust Callout default change from Info to
  the contracted Neutral.
- Use one exported `ToneFill = "tint" | "solid"` type in both TypeScript
  runtimes and one Rust enum. Do not leave the old planned
  `StatusSurfaceFill` name, add component-local aliases, or create a Pill-only
  fill type.
- Use the approved 45% tone / 55% text-primary sRGB solid formula. Neutral uses
  text-primary directly. Pill custom accent substitutes its accent for the
  tone base. Do not use raw status fills, component-specific ratios, or an
  unproved renderer approximation.
- Pill's existing `appearance="solid" | "subtle" | "badge"` API remains. Do
  not rename it. `fill="solid"` is the color authority: it suppresses
  appearance-specific tint/opacity recipes, preserves badge typography, and
  makes subtle add no opacity reduction. This precedence must be explicit in
  code, contract, and tests; it is not a fallback.
- Preserve Pill's muted behavior. Its solid dot and optional native remove
  affordance must remain visible using inverse treatment.
- Keep Callout and Pill's generated specimen paths presentation-only. If they
  need callbacks or conditions, stop and report the boundary.
- Work only in the selected worker worktree. Never edit the orchestrator's
  `main` checkout or clean/reset somebody else's dirty state.
- Do not merge the PR.

## Important Context

- **Planning lineage:** g13 rejected behavior-heavy component IR and g14
  rejected executable conformance authority. The small display-specimen model
  survived only for static presentation. Add `fill` and representative
  Callout/Pill instances there without growing it into behavior.
- **Why this card is ready:** the operator approved the prop name, two values,
  tint default, solid neutral, and Pill inclusion. The card fixes color math,
  Pill appearance precedence, runtime denominator, release class, specimens,
  evidence, validation, and stop conditions.
- **Shared type decision:** adding Pill makes the earlier
  `StatusSurfaceFill` name too narrow. The approved authority is now
  `ToneFill`; all three consumers use it directly.
- **Contrast ruling:** `color.text.inverse` is the solid foreground. Raw
  warning and success fills do not meet normal-text contrast in every light
  theme. The 45/55 sRGB mix keeps one portable formula and measured margin.
- **Pill naming tension:** its old `appearance="solid"` means standard shell,
  despite using a tinted background. No breaking rename or compatibility
  alias is authorised before v0.2.0. The documented fill precedence resolves
  the new combination without changing old calls.
- **Composition ruling:** Poodle Buttons in Callout/RemediationBanner action
  areas remain readable without rewriting supplied variants. Solve this
  locally to the containing tone surface.
- **Existing parity defect:** `CallOutSpec::default()` is Info while web and
  contract default neutral. Correct it and record the behavioral release note.
- **Human-facing rule:** each page gets one compact representative solid group.
  Tests own exhaustive matrices; Examples must not become a matrix.
- **Known baseline:** Effigy doctor findings and open `PAPERCUTS.md` entries are
  outside scope. Append only genuinely new execution friction.
- **Report after:** first, shared type/contracts plus paired web implementation
  and focused tests; second, Rust/GPUI, generated specimens, and final headless
  validation.
- **Report to:** the operator, who will relay progress to the orchestrator.

## Suggested Next Move

Read this handoff from the top, then run the four-command worktree preflight
below before broad repository reads. Once the worktree is accepted, read the
card, shared control types, and all three component contracts. Start by
contracting `ToneFill` and the Pill precedence rule, then add a focused
contrast test that pins the 45/55 formula against all current themes.

Build the three thin web shells and their shared CSS together. Prove existing
tint output before adding solid rules. Then carry the same enum and color
resolver through the Rust specs/renderers and GPUI specimens. Use the generator
for Callout and Pill artifacts; never hand-edit generated output.

## Completion Protocol

### Before you start

1. This file's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run only: `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, status is empty, and the
   branch is not `main`, accept it as the launcher-provided worktree. Record
   its actual root and branch. Do not compare them with the placeholders above
   or create another worktree merely because they differ.
3. Only if the current context is `main`, dirty, unregistered, or otherwise
   unusable should you inspect the named worktree. If that also cannot be used,
   read `.agents.local.env` as data and require an absolute, outside-repository
   `AGENTS_WORKTREE_CONTAINER_DIR`. Ask the operator if it is missing. Create a
   unique worktree and branch under that container from `origin/main`. Never
   use `/tmp`, `TMPDIR`, or a guessed path. Never clean, reset, stash over, or
   discard the original checkout. If the launcher itself supplied a dirty or
   `main` worktree, stop and report it rather than hiding the problem.
4. From the selected worktree, run `git fetch origin`; confirm `HEAD` equals
   `origin/main`; confirm `git merge-base --is-ancestor
   c671a431bc539c97baced8a71a0b56df1418f7c3 HEAD`; and confirm this handoff
   exists in `HEAD`.
5. Read `AGENTS.md`, `docs/roadmaps/g15/README.md`, the `g15.035` card, all
   three component contracts, shared control types, working rules, and spec
   022.
6. Run `effigy tasks` and `effigy doctor`. Record the known doctor baseline;
   stop only if a new finding changes this card's plan.

### While you work

- Update the shared type and contracts first, then keep Svelte/React and
  Rust/GPUI changes in coherent paired chunks.
- Add durable full-matrix, appearance-precedence, custom-accent, and contrast
  evidence before relying on specimens.
- Regenerate Callout and Pill artifacts with the repository generator and
  inspect the resulting diff. Never hand-edit generated output.
- Keep representative specimen copy and order aligned across all three active
  previews. Do not turn Examples into a tone/fill matrix.
- Update one August batch log with package change class, the Rust default
  correction, exact contrast results, Pill precedence, generated files,
  commands actually run, and unresolved findings.
- Report each meaningful chunk through the operator with changed files,
  validation run, remaining work, and blockers.
- Stop on any card stop condition or if implementation needs token-schema,
  Button public-API, a Pill appearance migration, generated behavior, or
  another component change.

### When the assigned runway is complete

1. Run the focused paired component, Rust spec/render, contrast, and specimen
   evidence, then `effigy ir:check`, `effigy test:components`,
   `effigy check:svelte`, `effigy react:build`, `effigy test:parity`,
   `effigy check:gpui`, `effigy regressions:native`,
   `effigy test:web-pack-install`, `effigy docs:check`, and
   `git diff --check origin/main...HEAD`.
2. Do not run windowed, native-visual, conformance, Jetstream, or release
   selectors.
3. Finish the batch log. Leave live Svelte/React review of Callout, Pill, and
   RemediationBanner as an explicit open PR checkpoint; do not claim it passed.
4. Push the selected worker branch and open a PR against current `main`.
5. The PR body must link this handoff, `g15.035`, all three component
   contracts, shared control types, spec 022, changed surfaces, contrast and
   precedence evidence, generated artifacts, validation, live review
   checkpoint, and unresolved items.
6. Report the PR URL and evidence to the operator. Do not merge.

### Review and merge path

The orchestrator will inspect the PR independently against the card,
contracts, diff, checks, and rendered surfaces. Because the worker and
orchestrator may share a GitHub identity, the orchestrator's PR comment is the
canonical review record when formal self-approval is unavailable. Make only
requested changes on this branch and report the updated head through the
operator.

Current review state: awaiting review. Requested changes: none. The operator
must explicitly authorise any merge after the code/check gate and live paired
preview checkpoint are satisfied.

- **Closeout refs:** `docs/roadmaps/g15/035-solid-status-surfaces.md`, the
  August batch log, `docs/roadmaps/g15/README.md`,
  `docs/roadmaps/generation-index.md`, `docs/roadmaps/README.md`, and
  `docs/roadmaps/dispatch.md`.

### Handoff closeout

Stop after one reviewable PR. Do not mark the card complete or advance the
runway; the orchestrator owns review, merge, roadmap currentness, and the next
dispatch decision.
