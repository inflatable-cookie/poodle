---
title: g15.050 v0.2.0 release candidate worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle core
created: 2026-08-23
updated: 2026-08-23
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260823-124431-g15-050-v020-release-candidate.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g15, release-candidate]
---

## What This Thread Was Doing

Poodle's implementation runway is complete through `g15.053`. This worker run
prepares the one exact v0.2.0 candidate needed by the final operator
certification gate.

Execute `g15.050` only. Align release-bearing versions, write the release
narrative, reconcile release-facing metadata, produce clean local packs, run
the complete supported headless board, and record reproducible evidence against
one candidate commit. This is candidate preparation, not publication.

## Why It Matters

Longhorn and the wider adopter set are waiting on a dependable Poodle v0.2.0
baseline. The candidate must make a narrower promise than “the repo looks
green”: 175 Svelte components are the certified release denominator, React is
experimental, shared Rust and GPUI carry their documented active-cohort
evidence, and Jetstream remains deferred.

The exact-SHA split matters. Artifacts, gates, notes, and the commit proposed
for release must describe the same tree. A green run from one commit cannot be
used to certify later edits.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Posture:** `strict-ready`
- **Planning branch:** `main`
- **Planning base before this handoff commit:**
  `9ae26bfa6017d5b9001edf4f4fe17de3eb781e7b`
- **Pushed-main verification:** local `HEAD` matched `origin/main` at that SHA;
  the tightened `g15.050` card and this handoff are committed in the following
  planning commit.
- **Planning checkout:** clean before these planning edits.
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Intended worker branch:** `t3code/g15-050-v020-release-candidate`
- **Worker worktree:** use the clean registered non-`main` worktree supplied by
  the launcher, regardless of its generated path or branch name.
- **Worktree creation command:** none in the normal path; the launcher owns it.
  Manual fallback is allowed only through the repository's ignored
  `.agents.local.env` path contract.
- **Active spec lane:**
  `docs/specs/022-packaging-versioning-and-release-channel-rules.md`
- **Roadmap milestone:** `docs/roadmaps/g15/README.md`
- **Ready card:** `docs/roadmaps/g15/050-v020-release-candidate.md`
- **Allowed runway:** `g15.050` only; `g15.013` remains an operator gate.
- **Remaining card budget:** one implementation card.
- **Dispatch topology:** serial. There is no parallel release lane.
- **Parallel safety:** versions, locks, release notes, packed artifacts, and
  the exact candidate SHA are one coupled transaction.
- **Canonical refs:** `AGENTS.md`, `.agents/skills/effigy/SKILL.md`, working
  rules 001, local-path contract 005, spec 022, the release baseline roster,
  release gap register, release operations/manifest, `g15.049`, `g15.050`, and
  `g15.013`.
- **Known baseline health:** `effigy doctor` reports the already recorded
  generated-in-src, god-file, stale-suppression, and comment-ratio findings.
  They are not candidate blockers unless this run introduces or worsens them.
- **Model capability profile:** frontier coding/review model with high
  reasoning; stop rather than improvise around release or public-surface
  ambiguity.
- **Tool/runtime restrictions:** headless only. No `*-windowed`, native-visual,
  Jetstream preview/QA, workflow dispatch, tag, publish, registry, GitHub
  release, `effigy release prepare`, or `effigy release execute` command.
- **Required validation:** the complete validation block in `g15.050`,
  including `effigy qa`, the one-gate read-only `effigy release gates`, packed
  roster evidence, licences/security, docs, checksums, and diff hygiene.
- **PR base/head:** current pushed `main` <- worker branch.
- **PR URL:** pending.
- **Review state:** awaiting implementation.
- **Merge authorisation:** absent. Push a PR and stop.

## Boundaries

Keep this run inside candidate assembly and evidence:

- **In scope:** release-bearing versions and intra-repo requirements, lockfiles,
  `CHANGELOG.md`, `docs/release-notes/0.2.0.md`, truthful release-facing
  metadata/guides/front doors, local packed artifacts outside the tracked tree,
  the exact-SHA receipt, and one August `g15.050` log.
- **Out of scope:** component contracts or implementations, specimens, visual
  baselines, workflow files, Effigy tasks, dependency-policy changes,
  Jetstream admission, new features, downstream repositories, tags, releases,
  publication, or registry state.
- Version the three public-intent TypeScript packages and every Rust crate under
  `packages/` exactly as fixed in the card. Do not silently promote private
  TypeScript tooling packages into the release set.
- Preserve the v0.2.0 publish set: core + Svelte only. React is packed and
  certified as experimental, not published. Rust is source/tag distribution.
- Do not make a red gate green by weakening the gate, changing policy, adding
  an exception, or rewriting evidence. Stop and report the blocker.
- Do not edit another roadmap card's status or declare `g15.013` complete. The
  orchestrator owns planning closeout; the operator owns release authority.
- Work only in the selected clean worker worktree. Never edit the orchestrator
  checkout, clean a dirty checkout, or create a second worktree because a
  launcher-generated name differs from this handoff.
- Do not merge the PR.

## Important Context

- `g15.001` fixes the denominator at 175 public Svelte components. The paired
  React roster is complete but remains experimental for v0.2.0.
- The active cohort is Svelte, React, renderer-neutral Rust composition, and
  GPUI. Jetstream's backend integration is program-deferred; it is not passing
  parity and is not a per-component exception.
- `g15.048` proves clean packed-root reachability. `g15.049` makes the release
  automation truthful and gives `effigy release gates` one read-only headless
  gate backed by `effigy qa`. `g15.051` closes native dependency-licence policy.
- The release workflow checks the three public TypeScript package versions and
  every Rust crate version against the tag. Update Rust intra-repository
  dependency requirements along with package versions.
- Public publication remains core + Svelte. React needs a clean local pack and
  install evidence but must not be added to the publish step.
- Build candidate evidence in two commits. The first is the complete candidate
  tree. Run every gate from that clean commit and keep tarballs outside Git.
  The second is evidence-only: one execution log/receipt naming the candidate
  SHA, commands, artifact filenames, byte sizes, and SHA-256 checksums. If a
  candidate-bearing file changes, repin and rerun everything.
- Release notes must distinguish breaking, additive, and behavioral changes,
  give concrete migration actions, name the 175-component Svelte baseline,
  describe React/Rust/GPUI honestly, explain the visual comparison boundary,
  and state Jetstream's deferral.
- The open transitions.dev and Longhorn conformance-lab triage notes remain
  post-v0.2.0 work. Do not pull either into this candidate.
- Report after three meaningful chunks: version/lock denominator; release
  narrative and metadata reconciliation; exact-SHA gates, packs, and receipt.

## Suggested Next Move

Start by reading this file from the top. Run the four-command worktree preflight
below before broad repository reads. Reuse a clean, registered, non-`main`
launcher worktree immediately; do not compare its generated path or branch with
the placeholders here.

Then inspect the current release-manifest denominator, all Rust manifests and
intra-repository version requirements, the three public TypeScript manifests,
both lockfile families, the 0.1.0 release-note shape, `CHANGELOG.md`, and the
release workflow's read-only version/pack checks. Build a mechanical manifest
inventory before editing. If that inventory conflicts with `g15.050`, stop and
report the exact conflict instead of choosing a denominator yourself.

Once the denominator is proven, make the candidate-facing changes as coherent
batches. Do not run the full board until the complete candidate commit exists
and the worktree is clean.

## Completion Protocol

### Before editing

1. This file's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run only: `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Reuse a clean registered non-`main` launcher worktree. Record its real root
   and branch. If the launcher supplied `main`, a dirty checkout, or an
   unregistered checkout, stop and report it. Do not clean, reset, stash, or
   silently create a second worktree.
3. Only when no usable launcher worktree exists and the operator explicitly
   requests manual fallback, read `.agents.local.env`, require an absolute
   `AGENTS_WORKTREE_CONTAINER_DIR`, and create a unique branch/worktree below
   it from `origin/main`. Ask if the key is absent. Never use `/tmp`, `TMPDIR`,
   or a guessed/repository-adjacent path.
4. Fetch `origin`; require selected `HEAD == origin/main`; require
   `9ae26bfa6017d5b9001edf4f4fe17de3eb781e7b` to be an ancestor; confirm this
   handoff exists in selected `HEAD`.
5. Read `AGENTS.md`, the Effigy skill, `g15.050`, `g15.013`, active g15 README,
   spec 022, release roster/gap register, release manifest/operations, and the
   release automation log/workflow evidence named above.
6. Run `effigy tasks`. Do not treat the known Doctor baseline findings as new
   candidate defects without measured regression evidence.

### While working

- Execute only `g15.050`. Keep commits meaningful: candidate content first,
  evidence-only receipt second.
- Use `apply_patch` for authored edits and the supported package/Cargo tooling
  for mechanical lockfile updates. Do not hand-edit generated locks.
- Report each meaningful chunk through the operator with changed files,
  validation actually run, remaining work, and blockers.
- Stop on any card stop condition, manifest-denominator conflict, policy
  conflict, red release gate, unexpected tracked generation, or need to touch a
  forbidden surface.
- Never run a release mutation or windowed/native-visual/Jetstream selector.

### Candidate and evidence commits

1. Finish every candidate-bearing edit: versions, intra-repo requirements,
   lockfiles, changelog, release notes, metadata, guides, and release-facing
   reconciliation.
2. Run narrow version/docs/pack checks, confirm `git diff --check`, then commit
   the complete candidate tree. Record that exact candidate SHA.
3. From a clean worktree at that SHA, run the required validation in the card.
   Pack core, Svelte, and React into an untracked/external artifact directory;
   record filenames, byte sizes, and SHA-256 checksums. `effigy release gates`
   is read-only and allowed; `prepare` and `execute` are not.
4. If any candidate-bearing file changes, create a replacement candidate
   commit and rerun the complete board. Do not reuse earlier receipts.
5. Add only the August `g15.050` execution log/receipt in the evidence commit.
   It must name the candidate SHA, full command results, artifact details,
   known non-blocking warnings, and the absence of all release mutations.

### PR handoff

1. Run `git diff --check origin/main...HEAD` and inspect the two-commit boundary
   against the card's writable/forbidden scope.
2. Push the selected worker branch and open one reviewable PR against current
   `main`.
3. Link `g15.050`, spec 022, the release notes, execution receipt, candidate
   SHA, artifact checksums, validation, and unresolved warnings in the PR body.
4. Report the PR URL and stop. Never merge, tag, publish, dispatch a workflow,
   create a GitHub release, or mutate a registry.

### Review and merge path

The orchestrator independently checks the manifest denominator, version and
lock agreement, release-note truth, two-commit boundary, candidate SHA,
artifact receipt, changed files, and all headless gates. Formal self-approval
may be unavailable; the orchestrator's PR comment is the canonical review
record. Any candidate-bearing review fix invalidates the receipt and requires a
new candidate commit plus a complete rerun.

The operator must explicitly authorise merge. Merge does not authorise a tag or
publication. After an accepted merge, the orchestrator closes `g15.050`,
updates the g15 front doors, and presents `g15.013` as the separate operator
certification/release gate.

### Handoff closeout

Leave `g15.050`'s execution log honest and preserve `g15.013` as blocked until
the orchestrator accepts the exact candidate evidence. If any gate is red,
record it and stop rather than weakening the candidate claim.
