---
title: Longhorn Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-24
updated: 2026-08-24
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260824-205427-g16-008-longhorn-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, longhorn]
---

## What This Thread Was Doing

Poodle 0.2.2 has been published with crates.io GPUI restored. The earlier
Longhorn 0.2.1 adoption stopped and its worktree was removed because that
release leaked a fork-sourced GPUI identity. This is a fresh worker lane to
adopt 0.2.2 across Longhorn's web packages, examples, Rust adapter, and GPUI
prototypes.

This handoff is complete on its own. You do not need the originating transcript.

## Why It Matters

Longhorn is the foundation for Jetstream and most Poodle-consuming product
apps. Its adoption is also the real proof that Poodle's corrected tag composes
with a consumer's direct crates.io GPUI dependency without duplicate types.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/longhorn`
- **Target base:** `4780e7fadbeae40fc88b31a02146a0e83cac8677`
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `3434a32d1a3e2e94ea67f225f2739c4f9f6355ef`, pushed to `origin/main`
- **Planning checkout:** clean and synchronized when dispatched
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-008-longhorn-poodle-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Roadmap card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/008-longhorn-poodle-v022-adoption.md`
- **Allowed runway:** that one card only
- **Parallel topology:** runs beside g16.009 Underlay and g16.010 Soundcheck
  Library; no shared mutable repository files
- **Required validation:** card validation plus Longhorn `effigy qa`, all
  headless; never launch a GPUI prototype
- **PR:** Longhorn `main` <- worker branch; pending
- **Review state:** awaiting orchestrator review; worker must not merge

## Boundaries

- Update only the active Longhorn Poodle declarations, locks, and compatibility
  surface named by g16.008.
- Preserve one Poodle `v0.2.2` source and one crates.io GPUI 0.2.2 identity.
- Do not add a fork, patch, path override, duplicate-type conversion, alias, or
  compatibility shim.
- Do not bump or publish Longhorn, edit Poodle, change public/wire contracts,
  rewrite historical evidence, or touch sibling consumers.
- Do not launch visible applications or windowed prototype runs.
- Work only in the clean launcher-provided Longhorn worktree. Do not edit the
  Poodle planning checkout or Longhorn's `main` checkout.

## Important Context

- Poodle tag `v0.2.2` points to exact candidate
  `d5607def24c6833913df1b5dcfa06372fcd5dd81`.
- npm core/Svelte 0.2.2 are public `latest`; React remains source-only.
- The old g16.002 approach failed because Poodle 0.2.1 and Longhorn's
  crates.io `gpui = "0.2.2"` produced incompatible crate identities. Do not
  recover any stopped-branch source workaround.
- Longhorn has 19 active web manifests, three Rust manifests, the root Bun and
  Cargo locks, and two prototype Cargo locks in scope. Longhorn package
  versions themselves remain 0.1.0.
- Report after dependency/lock convergence and again after validation. Include
  changed files, exact resolved sources, tests run, remaining work, and blockers.

## Suggested Next Move

Read g16.008, then inventory the exact active Poodle declarations from the
clean worker worktree. Change the coordinated web and Rust pins in one batch,
regenerate locks narrowly, and inspect source identity before attempting any
compatibility repair. If the graph still contains a fork or duplicate GPUI,
stop immediately and report it.

## Completion Protocol

### Before you start

1. This file's worker metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only if it is a clean, registered, non-`main`
   Longhorn worktree supplied by the launcher. Record its actual root/branch;
   do not create another because its generated name differs from the suggestion.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered
   location, stop and tell the operator. Do not clean it or silently create a
   second worktree. A manual fallback requires Longhorn's operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR`; never guess `/tmp` or a sibling path.
4. Run `git fetch origin`; confirm target `HEAD == origin/main == 4780e7fa`.
   Confirm this handoff exists at the absolute Poodle path and that Poodle
   planning base `3434a32d` is an ancestor of Poodle `origin/main`. This is a
   cross-repository handoff, so it is not expected inside Longhorn's `HEAD`.
5. Read Longhorn `AGENTS.md`, contracts 012/013, g16.008, and its governing
   Poodle release/inventory refs. Run Longhorn's cheap orientation checks.

### While you work

- Keep commits aligned with meaningful migration chunks.
- Report after source/lock convergence and after final validation.
- Stop on a contract change, scope expansion, source-identity failure,
  unrelated lock churn, visible-run requirement, or Poodle release defect.

### When the assigned runway is complete

1. Run every validation named by g16.008, `git diff --check`, and inspect all
   lockfile diffs.
2. Record exact npm versions, Poodle tag/SHA, crates.io GPUI source identity,
   compatibility edits, and test results in the PR body.
3. Push the worker branch and open a reviewable PR against Longhorn `main`.
4. Report the PR URL and evidence to the operator. Do not merge and do not edit
   Poodle roadmap/log state; the orchestrator owns that closeout.

### Review and merge path

The Poodle orchestrator will independently inspect the Longhorn diff, commits,
checks, dependency graph, and lockfiles. Shared GitHub identity means the
canonical verdict may be a PR comment rather than formal approval. Make only
requested changes on this branch. Merge requires separate operator authority.
