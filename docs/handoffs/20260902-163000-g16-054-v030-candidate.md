---
title: g16.054 HistoryCenter v0.3.0 candidate worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-163000-g16-054-v030-candidate.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, release-candidate, Papercuts]
---

## Assignment

Implement only `g16.054` from
`docs/roadmaps/g16/054-historycenter-v030-release-candidate.md`. Prepare and
certify the immutable `0.3.0` candidate. Do not release it.

## Current State

- Repository: `/Users/tom/Dev/projects/poodle`
- Required base: pushed current `origin/main`, including PR #163 merge
  `536c9f7435c779e5829016432394d0c01ec442ee`
- Accepted installed-distribution proof:
  `b37f404737062f9603c097dee0ee8b8916595d1f`
- Accepted receipt SHA-256:
  `5cb4b6d2f848e426de868fd56501b1d2c02c368f6ece7f2fb32692394c9f733c`
- Worker class: mechanical implementation. Version, package set, release-note
  boundary, gates, and no-release rule are frozen; this is long candidate and
  evidence work, not frontier reasoning.
- Integration ownership: orchestrator owns exact-head review, merge, and any
  later tag/workflow/npm or Loophole authorization

## Boundary

Complete every card row: lockstep the candidate tree to `0.3.0`, reconcile the
unpublished `0.2.3` truth, create release notes, freeze the candidate commit,
then validate and record evidence without changing candidate inputs.

Never tag, publish, dispatch or edit workflows, mutate npm/registries, run
release prepare/execute/simulate, edit Loophole or any sibling, admit React,
or run windowed/native-visual selectors. A green PR is only a candidate.

## Required Proof

- Prove the accepted g16.059 receipt and merged mainline prerequisite before
  editing candidate inputs.
- Produce exactly two semantic commits: candidate tree, then evidence-only
  receipt. The second must not change package inputs.
- Run every card validation from a clean checkout of the exact candidate
  commit, including installed distribution, packs/hashes, release gates,
  lockstep/generated checks, docs, QA, and tag absence.
- Plant and restore the card oracles from a committed point. Release/workflow
  scope protection must inspect actual changed paths.

## Completion Protocol

1. Use only the launcher-provided clean non-main worktree. Do not create
   another, reset, stash, or discard state.
2. Read this handoff, g16.054, specs 022/044, architecture 014, and the accepted
   g16.059 log before edits.
3. Stop if `v0.3.0` or `v0.2.4` exists locally/remotely, the receipt does not
   reconcile, or main gains candidate-bearing changes during the freeze.
4. Push one branch and open one PR. Never merge.
5. Return PR URL, candidate commit, exact head, tarball/receipt hashes, tag
   absence, validation, falsification receipts, and explicit no-release limits.

## Next Move

Reconcile the accepted installed receipt and read-only tag/registry state first.
Only then form the candidate-tree commit and freeze it for certification.
