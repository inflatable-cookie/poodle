---
title: Jetstream Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-181552-g16-023-jetstream-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, jetstream]
---

## What This Thread Was Doing

Poodle's 0.2.2 consumer rollout has reached its final authoritative repository.
This worker moves Jetstream's Svelte editor from Poodle 0.1.x plus committed
sibling Poodle overrides to exact public 0.2.2. Jetstream's paired Rust
integration remains local and unchanged.

## Why It Matters

Jetstream is the last active consumer still proving Poodle through adjacent web
source instead of the published package. Its migration closes Poodle `g16` and
also advances the Poodle half of Jetstream `g06.014` without forcing the paired
Rust renderer boundary into a premature distribution decision.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/jetstream`
- **Target base:** `2106faec624973607c16a52985c9d1044e830261`, verified as
  clean current `origin/main` on 2026-08-25
- **Poodle planning repository/base:** `/Users/tom/Dev/projects/poodle` at
  pushed `83bc15d53c60bd784901286e24fd90b8c33a7028`
- **Planning checkout:** clean `main`, with `HEAD == origin/main` before this
  handoff commit
- **Worker branch:** launcher-generated non-`main`; suggested
  `t3code/g16-023-jetstream-v022`
- **Worker worktree:** use a fresh clean launcher worktree
- **Ready card:** `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/023-jetstream-poodle-v022-adoption.md`
- **Allowed runway:** `g16.023` only; one repository and one PR
- **Parallel safety:** no open Jetstream PR exists. The registered
  `/Users/tom/Dev/projects/jetstream-head` entry is detached and prunable; do
  not use, repair, remove, or clean it.
- **Web baseline:** `editor-ui/package.json` declares Poodle core and Svelte as
  `^0.1.0`, then overrides both to `file:../../poodle/...`; `editor-ui/bun.lock`
  records the same local identities.
- **Retained Longhorn baseline:** three local Longhorn packages and overrides
  remain in scope as retained sources. Current Longhorn `origin/main` is clean
  at `c94f72e9`; its Poodle Svelte peer is exact 0.2.2.
- **Retained Rust baseline:** `crates/jetstream-poodle/Cargo.toml` keeps six
  local Poodle contract/render/adapter paths. The migration must not change
  those paths or the Rust lock.
- **Canonical refs:** Poodle `g16.001`, `g16.008`, and `g16.023`; Jetstream root
  `AGENTS.md`, working rules, `g06.013`, `g06.014`, release-consumption model,
  cross-repo boundary policy, and repo-local Effigy skill
- **Required validation:** `bun install` in `editor-ui`; lock and resolution
  inspection; `effigy editor:test`; `effigy check`;
  `effigy check:sibling-boundaries`; `effigy check:single-ui-stack`;
  `effigy test:cargo`; `effigy validate`; `effigy qa`; `git diff --check`
- **Known repo-health baseline:** `effigy doctor` currently exits red on 361
  existing god-file findings and also reports a stale graph index. Separate
  that baseline from migration failures; do not weaken policy or absorb it.
- **PR base/head:** Jetstream `main` <- worker branch
- **PR URL:** pending; review awaiting Poodle orchestrator; worker must not merge

## Boundaries

- Change the two direct Poodle web dependencies to exact registry `0.2.2` and
  remove only the two Poodle entries from `overrides`.
- Regenerate `editor-ui/bun.lock` narrowly. Prove core and Svelte resolve from
  npm at 0.2.2 with published integrity and that the retained local Longhorn
  adapter converges on the same Poodle Svelte identity.
- Keep all three Longhorn `file:` dependencies and overrides. Their release
  migration is Jetstream-owned work outside Poodle `g16.023`.
- Keep every `crates/jetstream-poodle` Poodle path and the Rust lock unchanged.
- Repair only Jetstream-owned compatibility fallout caused by this web bump.
- Update Jetstream `g06.014` evidence narrowly if needed: Poodle npm adoption
  is complete; retained Longhorn `file:` dependencies mean the wider npm half
  and clean-no-sibling claim are not complete.
- Do not edit Poodle or Longhorn, publish packages, redesign the adapter,
  convert Rust paths, add aliases/shims, touch workflows, or launch any visible
  demo, editor, Tauri, or window selector.

## Important Context

- This is intentionally asymmetric: public web packages are ready, while the
  Rust adapter remains a paired-runtime integration. Do not turn a package bump
  into a renderer or crate-distribution programme.
- Jetstream's own `g06.014` release-consumption model wants every web sibling
  public eventually. This card closes only the Poodle part because local
  Longhorn packages remain explicitly retained.
- The existing Bun lock predates Longhorn's 0.2.2 Poodle peer and records its
  old 0.1.0 metadata. A clean lock regeneration should update that peer while
  leaving Longhorn itself local. Inspect this rather than hand-editing it.
- `check:sibling-boundaries` deliberately reads the adjacent Poodle tree to
  prove adapter acyclicity. That check does not authorize changing Poodle or
  replacing the retained Rust paths.
- Report after the dependency/lock batch and again after validation. Stop if
  the registry graph duplicates Poodle, the local Longhorn peer cannot
  converge, the Rust graph moves, or proof requires a visible runtime.

## Suggested Next Move

Run the worker preflight, select only the fresh launcher worktree, and read the
ready card plus Jetstream authority. Inventory the two Poodle manifest entries,
two overrides, Bun lock identities, retained Longhorn peer, and Rust paths
before changing the web graph as one coherent batch.

## Completion Protocol

### Before you start

1. This handoff activates worker mode. Run the four-command
   root/branch/status/worktree preflight before broad reads.
2. Use only a clean registered non-`main` launcher worktree. Ignore the stale
   detached `jetstream-head` entry. If the launcher context is unusable, stop.
   Manual fallback requires Jetstream's `.agents.local.env`
   `AGENTS_WORKTREE_CONTAINER_DIR`; never guess or use `/tmp`.
3. Fetch origin; confirm `HEAD == origin/main == 2106faec`. Confirm this
   handoff exists and planning base `83bc15d5` is an ancestor of Poodle
   `origin/main`.
4. Recheck worktrees and open PRs, then read root instructions, Poodle
   `g16.023`, Jetstream authority, `PAPERCUTS.md`, and the repo-local Effigy
   skill. Run `effigy tasks`. Stop if another active lane owns the editor
   manifest or lock.

### While you work

Keep the edit bounded to the two public Poodle identities, narrow lock churn,
necessary compatibility repairs, and truthful `g06.014` evidence. Inspect the
full lock diff. Do not run `editor`, `run:*`, `demo`, or other visible/windowed
selectors.

### When the assigned runway is complete

Run every required check and `git diff --check`. Record the exact registry
identities and integrity, Longhorn peer convergence, unchanged Rust paths and
lock, complete changed-file list, known baseline separation, worktree, and
branch in one PR against current Jetstream `main`. Link Poodle `g16.023`, report
the URL, and do not merge.

### Review and merge path

The Poodle orchestrator reviews independently. Address review on the same
branch and report the new head. Merge needs separate operator authority.

### Handoff closeout

Leave Jetstream's remaining Longhorn release lane explicit. The Poodle
orchestrator owns `g16.023`, generation closeout, and next-task currentness
after merge.
