---
title: Underlay Poodle 0.2.2 follow-up worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-24
updated: 2026-08-24
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260824-205428-g16-009-underlay-v022-follow-up.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, underlay]
---

## What This Thread Was Doing

Underlay already removed its Poodle sibling overrides and adopted public 0.2.1.
Poodle 0.2.2 is now published, so this bounded follow-up moves the exact
registry dependency and lock to the corrected release.

## Why It Matters

Underlay gates seven product repositories in the adoption order. Proving its
adapter/template boundary on 0.2.2 lets those app cards compile against the
actual published foundation rather than a local source tree.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/underlay`
- **Target base:** `750005eb3c2b42df1d4152214abc8178e3cc0dda`
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `3434a32d1a3e2e94ea67f225f2739c4f9f6355ef`, pushed to `origin/main`
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main`; suggested identity
  `t3code/g16-009-underlay-poodle-v022`
- **Worker worktree:** launcher-managed; no manual worktree pre-created
- **Roadmap card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g15/063-underlay-poodle-v022-follow-up.md`
- **Allowed runway:** g15.063 only
- **Parallel topology:** independent of Longhorn and Soundcheck Library
- **Required validation:** Underlay health plus `effigy validate`, focused
  adapter checks if needed, lock review, and `git diff --check`
- **PR:** Underlay `main` <- worker branch; pending; do not merge

## Boundaries

- Change exact Poodle Svelte 0.2.1 to 0.2.2 and regenerate only the necessary
  Bun lock entries.
- Preserve Underlay-owned adapters and token bridges; application code must not
  learn about Poodle.
- Do not publish/version Underlay, widen its public API, restore sibling links,
  add aliases, edit Poodle, or touch consumer apps.
- Work only in the clean launcher-provided Underlay worktree. Do not edit either
  repository's planning/main checkout.

## Important Context

- Underlay PR 4 already removed committed Poodle overrides and passed
  `effigy validate`; no compatibility migration was needed at 0.2.1.
- The current target has only root `package.json` and `bun.lock` Poodle version
  evidence. Core is transitive through Svelte.
- Poodle core/Svelte 0.2.2 are public npm `latest`; exact pins remain policy.
- Report once the dependency/lock diff is understood and again with final
  validation, especially if anything beyond the two expected files changes.

## Suggested Next Move

Read g15.063 and Underlay's local authority. Make the exact package change,
regenerate `bun.lock` narrowly, inspect the complete diff, then run the focused
and broad headless validation. A source migration should not be necessary; if
one appears, keep it bounded or stop under the card's conditions.

## Completion Protocol

### Before you start

1. Run the quick worker preflight: repository root, current branch,
   `git status --porcelain`, and registered worktrees.
2. Use the current context only if it is a clean, registered, non-`main`
   Underlay worktree supplied by the launcher. Record its generated path/branch.
3. If it is dirty, `main`, or unregistered, stop and tell the operator. Do not
   clean it or create a hidden second worktree. Manual fallback requires an
   operator-selected Underlay worktree container; never guess one.
4. Fetch and confirm `HEAD == origin/main == 750005eb`. Confirm the absolute
   Poodle handoff exists and planning base `3434a32d` is an ancestor of Poodle
   `origin/main`; it is intentionally not part of Underlay `HEAD`.
5. Read Underlay `AGENTS.md`, g15.063, and the named adapter/token authority.

### While you work

- Keep the migration in one coherent dependency/lock batch.
- Stop on public API change, unrelated lock churn, browser-focus requirement,
  or evidence of a Poodle release defect.

### When the assigned runway is complete

1. Run the g15.063 validation and inspect the full diff.
2. Push the worker branch and open a PR against Underlay `main` with registry
   resolution, changed files, and exact validation in the body.
3. Report the PR URL to the operator. Do not merge or edit Poodle planning docs.

### Review and merge path

The Poodle orchestrator will independently review the Underlay PR and record a
verdict. Make requested fixes on this branch only. Merge requires separate
operator authority.
