---
title: Finch Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-162410-g16-019-finch-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, finch]
---

## What This Thread Was Doing

Poodle 0.2.2 and the matching Longhorn adapter line are published. Finch's
active Tauri application still declares Poodle 0.1.x. This worker moves only
that active application to exact registry 0.2.2 and leaves its archived
Electron application untouched.

## Why It Matters

Finch is a clean proof that a Longhorn-backed application can consume one
public Poodle identity while retaining local Longhorn packages. The archive is
historical evidence, not part of the release denominator.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/finch`
- **Target base:** `ab4f5a6072ee70391ed6d2b8513c8d035dbc7609`, verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `3a9b69b471259539b5c13585ae06e42cf5fa2d63`, pushed before
  this handoff commit
- **Planning checkout:** clean `main`, with `HEAD == origin/main`
- **Worker branch:** launcher-generated non-`main`; suggested
  `t3code/g16-019-finch-v022`
- **Worker worktree:** use the launcher's clean registered worktree; none was
  pre-created
- **Ready card:** `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/019-finch-poodle-v022-adoption.md`
- **Allowed runway:** `g16.019` only; one repository and one PR
- **Parallel safety:** Finch had no secondary worktree or open PR at dispatch
- **Canonical refs:** Poodle `g16.001`, `g16.011`, `g16.019`; Finch root/scoped
  `AGENTS.md`, planning authority, and repo-local Effigy skill
- **Required validation:** Finch's supported install flow, active-app selectors
  discovered through `effigy tasks`, `effigy validate`, `effigy qa`, and
  `git diff --check`; no visible app selector
- **PR base/head:** Finch `main` <- worker branch
- **PR URL:** pending; review awaiting orchestrator; worker must not merge

## Boundaries

- Pin `app-tauri` Poodle core/Svelte to exact `0.2.2`; regenerate only its Bun
  lock and prove the Longhorn peer converges on one Svelte identity.
- Do not touch `archive/`, Longhorn package sources/overrides, Tauri APIs,
  controller behavior, product semantics, Poodle, or Longhorn.
- Reject unrelated lock churn, aliases, compatibility shims, and visible proof.
- Work only in the selected worker worktree. Do not merge or edit Poodle
  planning state.

## Important Context

- Current active manifests declare `^0.1.0`; the archived Electron app does too
  but is explicitly excluded.
- The active app intentionally keeps local Longhorn packages. Only Poodle moves
  to the registry.
- Report after manifest/lock convergence, then after validation. Stop on a
  duplicate/local Poodle identity or any Finch/Longhorn/Tauri decision.

## Suggested Next Move

Start with the worker preflight, read `g16.019` and Finch authority, then use
the repo-owned install path. Review the complete lock diff before compatibility
work and open the PR only after one registry identity is proved.

## Completion Protocol

### Before you start

1. This file activates worker mode. Before broad reads run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use only a clean, registered, non-`main` launcher worktree. Record its real
   root/branch. If the launcher supplied `main` or a dirty/unregistered path,
   stop; never clean it. Manual fallback requires Finch's `.agents.local.env`
   `AGENTS_WORKTREE_CONTAINER_DIR`; never guess a path or use `/tmp`.
3. Fetch origin; confirm `HEAD == origin/main == ab4f5a60`. Confirm this
   handoff exists at its absolute Poodle path and planning base `3a9b69b4` is
   an ancestor of Poodle `origin/main`.
4. Read Finch instructions, the card, and the repo-local Effigy skill. Run
   cheap orientation only after selecting the worktree.

### While you work

- Keep one coherent manifest/lock batch. Inspect all changed resolutions.
- Stop on overlap, unrelated churn, a missing public package surface, visible
  proof, or any decision outside the card.
- Report changed files, validation, remaining work, and blockers through the
  operator.

### When the assigned runway is complete

Run the card validation and `git diff --check`; record exact identities, peer
convergence, lock review, compatibility edits, worktree, and branch in one PR.
Push it against Finch `main`, link `g16.019`, report the URL, and do not merge.

### Review and merge path

The Poodle orchestrator reviews independently and records a canonical comment
when shared identity prevents formal approval. Requested fixes stay on this
branch. Merge needs separate operator authority.

### Handoff closeout

Leave evidence honest. The orchestrator owns card, log, runway, and next-task
closeout after merge.
