---
title: g18.001 contract-bound GPUI functionality census
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-09
updated: 2026-09-09
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Operator said 'Go for it' after Chatterbox recommended the GPUI gap census and executable runway, under the standing instruction to dispatch approved Poodle work through northstar-queue."
queue:
  capability: complex
  skipPRReview: false
  notifyOriginOnCloseout: true
tags: [coordination, handoff, worker, g18, g18.001, gpui, parity, evidence]
---

## What This Thread Was Doing

Execute [`g18.001`](../roadmaps/g18/001-contract-bound-gpui-functionality-census.md)
from the exact pushed planning commit. Build the first reproducible capability-
level census for all 175 portable GPUI components and admit only mounted
execution evidence that proves its named contract claims.

## Why It Matters

Poodle currently proves that 175 GPUI specimen routes construct, but only 29
components have validated mounted-behaviour receipts. Forty-four of the 146
missing rows cite retained expected tests and 102 have no admitted receipt or
expected entry. Those categories do not tell the operator which components are
fully functional. The census must establish that truth before repair tranches
can be compiled.

## Current State

Planning `main` is pushed at the commit containing this handoff and g18 is the
active generation. `g18.001` is the sole approved frontier. The current
176-row ledger, Nucleus M1/A1/V1 receipts, historical mounted expectation map,
GPUI construction probe, cross-runtime report, and headless regression suite
are the starting evidence. g17 is complete and compacted.

## Boundaries

Follow the task exactly. Keep component contracts as semantic authority and
make every admitted capability traceable to actual production renderer plus
GPUI node-backend execution. Preserve Nucleus receipts unchanged. Keep A2
platform proof separate from component-level node semantics, keyboard, and
focus.

Do not repair component behaviour, change public APIs, accept deltas, create a
shared portable case corpus, revive the rejected g14 conformance architecture,
run windowed selectors, edit workflows, publish a release, admit Jetstream, or
merge the PR. Stop and report exact rows if the census exposes repair work.

## Important Context

Read the task, active generation README, working rules, component documentation
architecture, parity evidence rules, native accessibility contract, current
ledger generator and tests, Nucleus receipt generator/schema, GPUI cross-
runtime report checker, and `headless_regressions.rs`. A test name or source
inspection is planning input, never execution evidence. MeterSurface is the
single contract-approved non-portable row.

## Suggested Next Move

Plant the denominator, stale/ignored-test, claim-overreach, backend-bypass, and
widened-A2 negative cases first. Then define the smallest closed receipt and
capability-manifest shape that keeps all 29 Nucleus rows valid byte-for-byte.

## Completion Protocol

Use Effigy inventory and focused validation. Produce the schema, deterministic
generator/checker, generated census, accepted existing receipts, missing-
capability groupings, and one execution log. Run every retained expected test
before admitting its claims. Run focused receipt/census tests, existing Nucleus
receipt and ledger checks, relevant native regression selectors, docs checks,
and `git diff --check`. Commit, push, open one PR from the queue-owned branch,
and report `ready_for_review` with its number, exact head, admitted count,
remaining gap counts, and refusals. Leave the tracked tree clean. The plugin
owns independent review, merge, closeout, and handoff archival; no successor
auto-starts.
