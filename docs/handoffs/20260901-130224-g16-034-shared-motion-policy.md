---
title: g16.034 shared motion policy worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260901-130224-g16-034-shared-motion-policy.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, motion]
---

## What This Thread Was Doing

PR #121 settled Poodle's shared motion policy. The orchestrator promoted it
into architecture 012, MotionPolicyProvider, eight pilot contracts, and ready
card g16.034. This thread implements that one card and returns one reviewable
PR.

This dispatches one bounded implementation lane. No transcript or second prompt
is part of the authority chain.

## Why It Matters

Disclosure, notifications, Tabs selection, discrete state, and loading loops
currently use inconsistent lifecycle and reduced-motion assumptions. The card
gives all active runtimes one explicit host policy and proves it before icon
morphing, shimmer, or further transition work consumes the boundary.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `b89c1127593f1bb77597bd4de6a48fde73db507e`
- **Pushed main verification:** local and `origin/main` both resolved to
  `b89c1127593f1bb77597bd4de6a48fde73db507e` before this handoff was created
- **Planning checkout:** clean
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** architecture 012,
  MotionPolicyProvider contract, eight amended pilot contracts, g16.034, and
  the pruned post-motion triage queue
- **Worker branch:** `feature/g16-034-shared-motion-policy`
- **Worker worktree:**
  `/Users/tom/.t3/worktrees/poodle/g16-034-shared-motion-policy`
- **Worktree creation command:** `git worktree add -b
  feature/g16-034-shared-motion-policy
  /Users/tom/.t3/worktrees/poodle/g16-034-shared-motion-policy origin/main`
- **Worker worktree policy:** follow Completion Protocol; launcher worktree
  first, named/manual fallback only when required
- **Required sibling worktree links:** none
- **Active spec lane:** architecture 012 is the semantic policy authority; no
  separate active spec
- **Roadmap milestone:** `docs/roadmaps/g16/README.md`
- **Ready cards, in order:**
  `docs/roadmaps/g16/034-shared-motion-policy-and-five-family-pilot.md`
- **Allowed runway:** g16.034 only
- **Remaining card budget:** one card
- **Dispatch topology:** runs beside the operator-driven interactive drag-fix
  lane and the design-guidance planning delegate
- **Parallel safety check:** design guidance owns one separate triage packet.
  The drag lane owns only a concrete operator-reported defect after
  reproduction. If that defect names a g16.034 pilot component or shared
  substrate file, stop before editing the overlapping chunk and report the
  exact files; continue only genuinely partitioned work.
- **Surfaces this lane owns:** the g16.034 writable scope, its execution log,
  card evidence, and motion-specific front-door closeout
- **Integration ownership:** this worker owns g16.034 closeout. The orchestrator
  owns merge ordering and reconciles any later drag-fix overlap after the first
  same-repository PR merges.
- **Merge ordering:** same-repository PRs merge one at a time; the orchestrator
  refreshes this head against current `main` and re-reviews it if a sibling lane
  merges first
- **Canonical refs:** `docs/architecture/012-semantic-motion-policy.md`,
  `docs/architecture/010-native-presentation-construction-context.md`,
  `docs/architecture/001-poodle-system-shape.md`;
  `docs/contracts/components/motion-policy-provider.md` plus Accordion,
  Collapsible, ToastStack, Tabs, Checkbox, IconButton, Skeleton, and Spinner
- **Review oracle:** g16.034 `## Review Oracle`
- **Model capability profile:** matching non-frontier day-to-day profile for
  bounded cross-runtime implementation; read live adapter notes at launch
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** use Effigy; headless only; never run
  `*-windowed`, release, tag, publication, workflow mutation, or sibling-repo
  commands
- **Required validation:** focused paired policy, provider, family, Rust render,
  GPUI, and browser probes; relevant drift selectors; `effigy ci:web`,
  `effigy ci:rust`, `effigy ci:native`, `effigy docs:check`, final headless
  `effigy qa`, and `git diff --check origin/main...HEAD`
- **PR base/head:** `main` ← `feature/g16-034-shared-motion-policy`
- **PR URL:** pending
- **Review state:** awaiting implementation PR
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks

## Boundaries

- **In scope:** execute the ten ordered g16.034 steps and exact oracle.
- **Out of scope:** Dialog, block sliders, icon morphing, shimmer,
  arbitrary layout/path/filter/canvas animation, Jetstream admission, releases,
  consumers, and parity-ledger movement.
- **Outcome shape:** complete contract-valid implementation, cleanup,
  falsification evidence, closeout, and PR. Diagnosis-only is not completion.
- Do not invent architecture, change the promoted contracts, widen the roadmap,
  or choose a new public policy, Tabs geometry, Toast focus, or native
  approximation rule.
- This handoff represents one worker lane, and sibling lanes may run
  concurrently. Write only inside the named surfaces. Stop and report shared
  mutable scope or hidden dependency instead of silently resolving it.
- Work only in the clean worker worktree selected by Completion Protocol.
- Do not merge. Merge belongs to the orchestrator.

## Important Context

- **Planning lineage:** research PRs #109–#112 informed the post-g16 queue;
  motion decision PR #121 merged as `7f718dd42`; canonical promotion is at
  `b89c11275`.
- **Why the card is ready:** architecture, public provider shape, inheritance,
  mode laws, lifecycle, identity, property budget, per-family outcomes,
  approximation rules, evidence, validation, and stop conditions are settled.
- **Decisions and preferences:** missing preference is full; reduced allows only
  selected short opacity; frozen has zero clocks; restriction-only nesting;
  latest state wins; authored initial state paints its endpoint; Skeleton uses
  a 1.6s opacity pulse; Tabs underline is one measured owner; focused Toast
  dismissal follows the exact contract fallback.
- **Open tensions:** GPUI cannot realize generic translation, scale, or clipped
  disclosure height. Use only the named approximation and record the gap. Do
  not turn it into renderer-capability work.
- **Report after:** policy/provider/context substrate; then each coherent family
  batch; then final evidence and PR.
- **Report to:** the operator through the active control plane.

## Suggested Next Move

Run the Completion Protocol preflight before broad reads. Read AGENTS.md,
architecture 012, MotionPolicyProvider, g16.034, and the eight pilot contracts.
Use Effigy to discover exact focused selectors. Start with paired policy laws
and provider/context propagation before component migrations.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root/branch; do not create another merely because it differs from the
   planned path or branch.
3. If current context is main, dirty, unregistered, or unusable, inspect the
   named worktree. If unusable, read `.agents.local.env`, require
   `AGENTS_WORKTREE_CONTAINER_DIR`, and ask when absent. Create a unique
   worktree there from pushed `origin/main`. Never use `/tmp`, `TMPDIR`, or a
   guessed path; never clean, reset, stash over, or discard dirty state.
4. From the selected worktree, record this handoff's repository-relative path.
   Fetch with `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git
   fetch origin`. Confirm `HEAD == origin/main`, confirm
   `b89c1127593f1bb77597bd4de6a48fde73db507e` is an ancestor, and confirm the
   handoff exists in `HEAD`. Load the tracked blob. If the absolute file differs,
   stop. The committed `HEAD` copy is canonical.
5. Required sibling links are none.
6. Read the milestone, g16.034, AGENTS.md, and canonical refs.
7. Run cheap orientation checks and record what ran.

### While you work

- Execute g16.034 in order and keep commits aligned with meaningful chunks.
- Report each named chunk with changed files, validation, remaining work,
  risks, and blockers.
- Stop on missing contract, ambiguous intent, scope expansion, authority gap,
  shared mutable files, or validation that changes the plan.
- Do not quietly turn a new threshold into architecture.

### When the assigned runway is complete

1. Run the required validation named above.
2. Falsify every oracle row. Commit real proofs before planting pre-fix
   behavior, confirm each named proof fails for the intended reason, restore,
   and rerun green. Reconcile card, roadmap, log, handoff, and front doors.
3. Update card/log evidence and actual worktree/branch details when needed.
4. Push the worker branch. If a sibling merged first, refresh against current
   main, revalidate, and say so in the PR.
5. Open a PR against current pushed main. Link architecture 012, provider and
   component contracts, g16.034, changed surfaces, evidence, validation, and
   unresolved items.
6. Report the PR URL. Do not merge.

### Review And Merge Path

The orchestrator reviews the exact PR head against the canonical refs, diff,
checks, and every oracle row. Same-identity acceptance is recorded as a PR
comment. Blocking findings are `execution-miss`, `oracle-gap`,
`planning-change`, `validation-gap`, or `integration-drift`. Requested changes
are none at dispatch. An accepted exact head with passing checks and clean
mergeability is merged without another approval prompt.

- **Closeout refs:** g16.034, `docs/roadmaps/g16/README.md`, generation index,
  one September execution log, affected contracts/parity notes, and any
  motion-specific currentness surface.

### Handoff Closeout

Leave the card, roadmap, log, and next-task state honest. If blocked, record the
blocker and stop rather than making the handoff look complete.
