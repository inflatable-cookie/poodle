---
title: g16.066 GPUI Node tooltip lifecycle worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: queued-after-g16.062
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-234015-g16-066-gpui-tooltip-runtime.md
base_required: pushed-main-after-g16.062
tags: [coordination, handoff, worker, pr, g16, gpui, tooltip, parity]
---

## What This Thread Will Do

Implement only `g16.066` from
`docs/roadmaps/g16/066-gpui-node-tooltip-lifecycle-runtime.md` after PR #170
is accepted and merged.

## Why It Matters

PR #169 proved GPUI 0.2.2 `.tooltip()` cannot meet Poodle's 300ms and
focus/Escape dismissal contract. Nucleus Tabs remains blocked until the shared
backend owns that lifecycle.

## Current State

- Repository: `/Users/tom/Dev/projects/poodle`
- Planning base: current pushed `main`; launch only after g16.062 merge
- Worker branch: `fix/g16-066-gpui-tooltip-runtime`
- Worker worktree: launcher-provided Paseo worktree; planned slug
  `g16-066-gpui-tooltip-runtime`
- Required sibling worktree links: none
- Card: `docs/roadmaps/g16/066-gpui-node-tooltip-lifecycle-runtime.md`
- Integration ownership: orchestrator owns g16.062/066/065 merge order
- Model capability profile: ordinary/day-to-day native lifecycle work;
  material backend review stays frontier
- Frontier-worker justification: none
- PR URL: pending; do not merge

## Boundaries

In scope: existing `Node.tooltip` string meaning, one backend-owned 300ms
runtime, mounted timing and terminal proofs, retained IconButton and
SegmentedControl behavior.

Out of scope: Tabs projection, public props or Node fields, web Tooltip,
Nucleus source, lab, Jetstream, release/workflow, and local windowed or
native-visual selectors.

## Important Context

Read AGENTS, card, Tooltip contract, node backend interaction/layer/frame
paths, and PR #169's card/log evidence. Reproduce the 500ms and blur-while-hovered
counterexamples before replacing `.tooltip()`.

## Completion Protocol

1. Use only the clean launcher worktree after g16.062 is on `main`.
2. Prove 299/300ms, leave, focus departure, Escape, disablement, removal,
   teardown, supersession, stale timer, and two-window isolation.
3. Keep IconButton and SegmentedControl tooltip/activation/focus regressions
   green.
4. Run focused tests, `effigy ci:rust`, `ci:native`, `docs:check`, and diff
   checks. Never run windowed/release selectors.
5. Push one PR and return exact head, falsifications, boards, and limits.
   Never merge.

- Closeout refs: card and one September log; front doors stay orchestrator-owned.
