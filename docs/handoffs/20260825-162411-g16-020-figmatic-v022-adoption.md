---
title: Figmatic Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-162411-g16-020-figmatic-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, figmatic]
---

## What This Thread Was Doing

Figmatic Studio still resolves Poodle through committed sibling `file:` paths.
This worker replaces those paths and overrides with exact registry 0.2.2 while
leaving Figmatic's local Longhorn and native componentization work alone.

## Why It Matters

Studio must prove the packed public Poodle surface independently of the sibling
checkout. That keeps this release adoption separate from Figmatic's deeper
native/session-machine roadmap.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/figmatic`
- **Target base:** `775312c9ec9579ce4cac9d627c45cc21bdeb77fd`, verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository/base:** `/Users/tom/Dev/projects/poodle` at
  pushed `3a9b69b471259539b5c13585ae06e42cf5fa2d63`
- **Planning checkout:** clean `main`, with `HEAD == origin/main`
- **Worker branch:** launcher-generated non-`main`; suggested
  `t3code/g16-020-figmatic-v022`
- **Worker worktree:** use a fresh clean launcher worktree
- **Ready card:** `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/020-figmatic-poodle-v022-adoption.md`
- **Allowed runway:** `g16.020` only; one repository and one PR
- **Parallel safety:** Figmatic has no open PR, but retains several existing
  native/session-machine worktrees. Do not use, clean, or alter them. Recheck
  that none owns `studio/package.json` or `studio/bun.lock` before editing.
- **Canonical refs:** Poodle `g16.001`, `g16.011`, `g16.020`; Figmatic root and
  Studio `AGENTS.md`, working rules, and repo-local Effigy skill
- **Required validation:** supported Studio install,
  `effigy check:studio`, `effigy build`,
  `effigy test:native-componentize`, `effigy qa`, and `git diff --check`; no
  visible Studio/native demo
- **PR base/head:** Figmatic `main` <- worker branch
- **PR URL:** pending; review awaiting orchestrator; worker must not merge

## Boundaries

- Replace Studio Poodle core/Svelte dependencies and overrides with exact
  registry `0.2.2`; retain local Longhorn dependency/override unchanged.
- Regenerate only `studio/bun.lock` and prove one Poodle/Svelte identity.
- Do not edit native componentization/session architecture, Longhorn/Poodle,
  harnesses, product behavior, other worktrees, or unrelated dependencies.
- Stop on missing packed exports, overlap, or visible-runtime requirements.

## Important Context

- Both Studio dependencies and overrides currently use sibling Poodle paths.
- Existing dirty Figmatic worktrees are outside this maintenance lane and must
  be preserved. Branch/worktree isolation does not authorise cleanup.
- Report after registry/lock convergence, then after the headless board.

## Suggested Next Move

Run the worker preflight, read Figmatic/Studio authority and `g16.020`, then
inventory open worktrees and the exact Studio graph. Make the manifest and lock
change as one batch and review the lock before any compatibility repair.

## Completion Protocol

### Before you start

1. This handoff activates worker mode. Before broad reads run the four-command
   root/branch/status/worktree preflight.
2. Use only a clean registered non-`main` launcher worktree. If it is `main`,
   dirty, or unregistered, stop. Never clean existing Figmatic worktrees.
   Manual fallback requires Figmatic's `.agents.local.env`
   `AGENTS_WORKTREE_CONTAINER_DIR`; never guess or use `/tmp`.
3. Fetch origin; confirm `HEAD == origin/main == 775312c9`. Confirm this
   handoff exists and Poodle planning base `3a9b69b4` is an ancestor of
   Poodle `origin/main`.
4. Recheck open PRs/worktrees, then read the card, instructions, and local
   Effigy skill. Stop if another lane owns Studio manifest or lock files.

### While you work

Keep the diff to Studio dependency adoption and bounded compatibility fallout.
Inspect every lock hunk. Report each meaningful chunk; stop on shared scope,
unrelated churn, public API decisions, or visible proof.

### When the assigned runway is complete

Run all required validation and `git diff --check`. Record identities, peer
convergence, lock review, compatibility edits, actual worktree, and branch in
one PR against Figmatic `main`. Link `g16.020`, report the URL, and do not merge.

### Review and merge path

The Poodle orchestrator reviews independently; shared identity may require a
canonical PR comment. Merge needs separate operator authority.

### Handoff closeout

Leave evidence honest. The orchestrator owns planning closeout after merge.
