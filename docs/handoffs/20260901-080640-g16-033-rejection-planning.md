---
title: g16.033 HistoryCenter rejection planning delegate handoff
kind: northstar-handoff
handoff_mode: planning-delegate
planning_mode: conversational-discovery
dispatch_authority: orchestrator
promotion_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260901-080640-g16-033-rejection-planning.md
base_required: pushed-main
tags: [coordination, handoff, planning, conversation, history-center, papercuts, pr]
---

## What This Thread Was Doing

Resolve the remaining product decision that keeps `g16.033` reserved: whether
HistoryCenter deletion refusals use structured Poodle semantic codes with
component-owned copy or a host-owned message surface. Capture a reviewable
decision packet without implementing, promoting, or disturbing `g16.028`.

This dispatches one operator-facing planning conversation. The delegate owns
discovery and evidence capture for this topic, not canonical promotion or
implementation.

## Why It Matters

Loophole currently collapses materially different deletion failures to
`UnknownEntry`. Poodle needs one renderer-neutral semantic boundary before the
later Svelte, React, shared Rust, and GPUI repair can be honest. The same packet
must preserve the already-correct v3 `HistoryEntry` source shape and keep
publication and adoption separate.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Base commit:** `697d6fc579a3eedf3406c8863a7518cb83f51d5e`
- **Pushed main verification:** local `HEAD` equals `origin/main` at the base commit
- **Planning-delegate branch:** `planning/g16-033-rejection-surface`
- **Planning-delegate worktree:** `/Users/tom/.paseo/worktrees/1ugbsx1t/g16-033-rejection-planning`
- **Required sibling worktree links:** none
- **Topic boundary:** HistoryCenter rejection semantics, packed v3 type-proof boundary, and disposition of the related portfolio ownership packet
- **Canonical context:** `docs/roadmaps/g16/033-history-center-rejection-surface.md`, `docs/contracts/components/history-center.md`, `docs/architecture/006-headless-core-and-machine-model.md`, `docs/contracts/001-working-rules.md`, `docs/triage/20260831-194043-history-papercut-ownership.md`
- **Named triage packet:** `docs/triage/20260901-080640-history-center-rejection-decision.md`
- **Named research evidence:** none
- **Allowed write paths:** `docs/triage/20260901-080640-history-center-rejection-decision.md` only
- **Concurrent orchestrator work:** `g16.028` implementation runs in a separate worker and owns drag code, component contracts, cards, logs, ledger, spec 069, and front doors; a separate delegate owns only the post-g16 research queue packet
- **Frontier planning profile:** frontier conversational planner with high reasoning
- **PR base/head:** `main` ← `planning/g16-033-rejection-surface`
- **PR URL:** pending
- **Promotion owner:** orchestrator after accepted review and merge

## Boundaries

- Stay inside the HistoryCenter decision and the one named triage file.
- Talk directly with the operator. Ask focused questions, explore alternatives,
  and distinguish operator-confirmed decisions, recommendations, evidence, and
  unresolved questions.
- Do not edit product code, architecture, contracts, specs, roadmaps, cards,
  logs, front doors, the existing ownership note, or research dossiers.
- Do not decide canonical promotion, readiness, implementation scope, release,
  publication, or Loophole adoption.
- Do not reopen Longhorn `AlreadyAtTarget`; Longhorn PR #20 and Loophole PR #16
  closed that source/adoption branch.
- Keep CS20 `groupId` Loophole/Pulse-owned and keyboard vertical geometry
  design-deferred. Record only their confirmed ownership disposition where it
  helps bound the HistoryCenter decision.
- Do not launch a papercut implementation worker. The eventual `g16.033`
  workspace must carry the capitalized `Papercuts` label and remains serial
  behind accepted and merged `g16.028`.
- Do not merge. The orchestrator reviews and merges the planning PR, then owns
  promotion against current canonical state.

## Important Context

- **Known decisions:** Poodle owns reusable rejection semantics and copy;
  protocol vocabulary stays in hosts; `UnknownEntry` and `AlreadyAtTarget`
  remain source-compatible absent explicit operator approval; Poodle source
  already re-exports v3 `HistoryEntry`; this lane proves packed source later but
  does not publish; no Poodle duplicate exists for the completed Longhorn wire
  work.
- **Questions worth exploring:** Does the operator confirm structured Poodle
  codes over host-owned messages? If so, which generic meanings must remain
  distinct: unknown entry, already at target, stale history, protected/current
  entry, and deletion unavailable? Should Poodle own exact copy and
  localization-ready lookup for every renderer? Are any categories deliberately
  merged? What exact evidence will make the later packed root and `./types`
  proof credible without claiming publication? Which parts of the existing
  portfolio note become promotable, remain open, or belong in Loophole?
- **Research needs:** Read-only inspection of the current HistoryCenter
  contract, TypeScript and Rust unions/resolvers, focused tests, package export
  map, and the existing ownership packet. No external research is required.
- **Non-goals:** implementation, code naming by fiat, drag-and-drop changes,
  keyboard geometry, CS20 implementation, release mutation, publication,
  consumer pin movement, or sibling edits.
- **Mainline drift risk:** `g16.028` may update g16/front-door state, but it does
  not own this new triage packet. Rebase before opening the planning PR and
  report any contradiction rather than editing canonical files.
- **Stop conditions:** the choice requires a breaking change beyond the card;
  categories cannot stay renderer-neutral; Longhorn/Loophole wire details would
  leak into Poodle; the operator defers the decision; an unlisted write path is
  required; or current main contradicts the reservation.

## Suggested Next Move

Read the canonical context and inspect the live semantic shapes, then open with
the structured-code recommendation and its trade-off against host-owned copy.
Keep the first question small enough for the operator to answer directly. Once
the choice is clear, work through category boundaries, copy ownership, packed
type proof, and the portfolio-note disposition.

## Completion Protocol

### Before the conversation

1. Confirm the current checkout is a clean, dedicated, non-`main` registered
   worktree for `planning/g16-033-rejection-surface`. Start with
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`. If the launcher
   did not supply one, use the repository's operator-selected worktree
   container; never guess `/tmp`, clean another checkout, or overwrite dirty
   state.
2. Fetch `origin`, confirm this handoff exists in the selected `HEAD`, and
   confirm `697d6fc579a3eedf3406c8863a7518cb83f51d5e` is an ancestor. The tracked
   handoff is canonical.
3. Required sibling links are `none`; skip sibling-link setup.
4. Read `AGENTS.md`, the named canonical refs, the existing ownership note, and
   the live HistoryCenter/package-export evidence. Do not treat this handoff or
   a triage packet as execution authority.

### During the conversation

- Keep the operator in the loop directly; the orchestrator is not a message
  proxy and may continue unrelated work.
- Preserve exact operator decisions. Label recommendations, evidence,
  alternatives, and open questions separately.
- Keep any research delegation read-only and bounded. Reconcile its output
  before writing the packet.
- Stop on topic expansion, conflicting decisions, an unlisted write path,
  required implementation, or a canonical change that cannot wait for
  promotion.

### When the planning packet is ready

1. Re-read the packet against the conversation. Make settled decisions,
   recommendations, evidence, alternatives, unresolved questions, non-goals,
   and suggested canonical destinations explicit.
2. Run `effigy docs:lint` and `git diff --check origin/main...HEAD`. Inspect the
   full diff; it may contain only the named triage file.
3. Commit and push `planning/g16-033-rejection-surface`, then open a PR against
   current `main`. The PR body lists base/head, changed files,
   operator-confirmed decisions, recommendations, unresolved questions,
   validation, and the proposed promotion map.
4. Report the PR URL. Do not edit canonical surfaces or merge.

### Review, merge, and promotion

The orchestrator reviews the exact PR head for fidelity to this handoff and the
operator confirmations recorded in the packet, evidence quality, scope, and a
clean separation between confirmed decisions, recommendations, and open
questions. Requested changes stay on this branch. After merge, the orchestrator
reconciles the packet with current `main`, promotes settled meaning into the
HistoryCenter contract/card and ownership surfaces, and only after `g16.028`
closes may it decide readiness or launch the `Papercuts`-labelled worker.

