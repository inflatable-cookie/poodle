---
title: g16.073 Nucleus Menu M1 worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: ready
owner: Poodle Northstar orchestrator
created: 2026-09-03
updated: 2026-09-03
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260903-064013-g16-073-nucleus-menu-receipt.md
base_required: current-pushed-main
tags: [coordination, handoff, worker, pr, g16, nucleus, gpui, menu, receipt]
---

## What This Thread Will Do

Implement only `g16.073` from
`docs/roadmaps/g16/073-nucleus-menu-m1.md`.

## Current State

- Repository: `/Users/tom/Dev/projects/poodle`
- Worker branch: `feature/g16-073-nucleus-menu-receipt`
- Worker worktree: launcher-provided Paseo worktree; planned slug
  `g16-073-nucleus-menu-receipt`
- Required sibling worktree links: none
- Card: `docs/roadmaps/g16/073-nucleus-menu-m1.md`
- Integration ownership: orchestrator owns receipt-source merge ordering and
  all g16 front-door closeout edits
- Worker class: day-to-day; establish one mounted panel fixture and one receipt,
  with a focused native repair only if the mounted counterexample bites
- Frontier-worker justification: none
- PR URL: pending; never merge

## Boundaries

In scope: one new production Menu panel fixture; exact flat action/disabled/
separator/danger plus checked/shortcut semantics; production Icon checkmark;
mounted pointer/keyboard activation, controlled checked-state rebuild, inert
rows, keyboard command-list movement, one M1 receipt, and exact evidence
refresh. A focused shared Rust/backend repair is allowed only when the mounted
proof demonstrates the missing behavior.

Out of scope: Nucleus source/data, web behavior, public APIs, trigger/open
ownership, nested-menu pointer intent or recursive submenus, browser geometry,
pixels, screenshots, A1/V1/V2, Jetstream, releases/workflows, and local
windowed/native-visual selectors.

## Important Context

Read AGENTS, the card, g16.062, g16.067, g16.072, the Nucleus manifest/ledger
and receipt emitter, plus the Menu and Icon contracts/specs/renderers before
editing. Native `poodle_render::menu` is deliberately panel-only; do not claim
the web trigger or `onOpenChange`. The current renderer may expose a biting
disabled or keyboard gap. Prove it before repairing it, keep one machine, and
stop if a public contract decision is required.

## Completion Protocol

1. Add the exact named fixture and update only the Menu manifest row's test.
2. Commit the initial proof/counterexample before repair. Build through the
   production renderer and dispatch every behavioral claim through the mounted
   test platform.
3. Keep the panel-only boundary explicit. Do not import nested-menu research.
4. Commit final runtime/test changes before falsification. Plant representative
   review-oracle counterexamples, record observed failures, restore from the
   committed source, and rerun.
5. Emit the receipt only after all assertions. Run the native receipt selector
   at the exact runtime commit, then refresh all receipts, manifest, ledger,
   card, and one log.
6. Run the card boards; never run windowed or native-visual selectors.
7. Push one PR and return URL, exact head/runtime source SHA, receipt,
   falsifications, validation, and limits. Never merge or edit shared front
   doors.
