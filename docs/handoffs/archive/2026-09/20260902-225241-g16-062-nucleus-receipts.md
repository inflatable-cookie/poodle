---
title: g16.062 Nucleus parity receipt foundation worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-225241-g16-062-nucleus-receipts.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, nucleus, parity]
---

## What This Thread Was Doing

Implement only `g16.062` from
`docs/roadmaps/g16/062-nucleus-parity-receipt-foundation.md`.

## Why It Matters

Nucleus is the first GPUI switch target. The current ledger treats expected
test names as mounted evidence. This lane makes the 29-component target and
its execution receipts auditable before component cards move.

## Current State

- Repository: `/Users/tom/Dev/projects/poodle`
- Planning base commit: `4ffa31345bc94f82c22d64d83e64b3af2613cfe3`
- Worker branch: `feature/g16-062-nucleus-receipts`
- Worker worktree: launcher-provided Paseo worktree; planned slug
  `g16-062-nucleus-receipts`
- Required sibling worktree links: none
- Active programme: `docs/roadmaps/g16/nucleus-gpui-parity-programme.md`
- Card: `docs/roadmaps/g16/062-nucleus-parity-receipt-foundation.md`
- Dispatch topology: parallel with `g16.063`, `g16.064`, and `g16.065`
- Parallel safety: owns parity receipt/ledger infrastructure only; component
  behavior and Tabs/Slider/HistoryCenter contracts belong to sibling lanes
- Integration ownership: orchestrator owns g16 README, generation index,
  programme closeout, exact-head review, and merge
- Model capability profile: ordinary/day-to-day implementation with strong
  test and data-schema work
- Frontier-worker justification: none
- PR base/head: `main` / worker branch; PR URL pending

## Boundaries

In scope: fixed cohort manifest, receipt schema/emitter/validator, actual
mounted-run integration, ledger expected-vs-executed distinction, focused
tests, card/log evidence, and generated ledger rows.

Out of scope: component behavior, Nucleus source/data, A2/M2/V2, lab code,
workflows, releases, versions, Jetstream, and all windowed/native-visual runs.
Do not mark a row mounted from a test name or direct handler call.

## Important Context

Read `AGENTS.md`, the programme, card, current parity ledger, ledger generator,
and working rules. The accepted denominator is 29 rendered components plus
`IconProvider` as a separate prerequisite. Preserve historical expected-map
counts without promoting them to execution.

## Suggested Next Move

Run the worker preflight, then reproduce the smallest mapped-but-unrun false
positive through the production checker before choosing the receipt seam.

## Completion Protocol

1. Use only the clean launcher-provided non-main worktree. Confirm this tracked
   handoff exists at `HEAD`; do not create a second worktree.
2. Commit the real oracle before planting counterexamples. Prove wrong commit,
   direct-handler, proof-level substitution, cohort 30, and unmanifested
   receipt failures through production paths; restore from a committed point.
3. Run focused receipt/schema tests, the relevant real mounted selector,
   `effigy check:parity-evidence-ledger`, `effigy docs:check`, `effigy qa`, and
   `git diff --check origin/main...HEAD`.
4. Push one branch and open one PR. Do not merge. Report exact head, receipts,
   changed counts, falsifications, limits, and PR URL.
5. Stop on an architecture or product-data requirement. No local windowed,
   release, workflow, Nucleus, lab, or sibling mutation.

- Closeout refs: card and one September log only; shared front doors stay with
  the orchestrator.
