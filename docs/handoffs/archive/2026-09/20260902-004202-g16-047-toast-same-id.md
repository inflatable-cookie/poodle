---
title: g16.047 Toast same-id update convention worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-004202-g16-047-toast-same-id.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, toast]
---

## What This Thread Was Doing

The Poodle orchestrator compiled the accepted post-triage runway and made
`g16.047` ready. This lane implements the fixed consumer-owned same-id Toast
update convention across the active Poodle runtimes.

This dispatches one bounded implementation lane. No transcript or second prompt
is part of the authority chain.

## Why It Matters

Hosts already model pending and settled work by replacing one toast row. Poodle
must make that existing path safe: timers must follow configured stickiness,
settlement must announce once without restarting visual presence, focus must
survive action changes, and native danger must carry the honest renderer role.
The fix must not turn Toast into an operation or promise owner.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `7f59ae42f4917c675968819eb23a5e41dc90013c`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `7f59ae42f4917c675968819eb23a5e41dc90013c` before this handoff was drafted
- **Planning checkout:** clean
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Planning artifacts included at the base:** accepted canonical runway and
  ready `docs/roadmaps/g16/047-toast-same-id-update-convention.md`
- **Worker branch:** `feature/g16-047-toast-same-id`
- **Worker worktree:** launcher-provided; manual fallback is
  `<AGENTS_WORKTREE_CONTAINER_DIR>/poodle-g16-047-toast-same-id`
- **Worktree creation command:** launcher-managed; manual fallback only after
  reading `.agents.local.env`: `git worktree add <resolved-container>/poodle-g16-047-toast-same-id -b feature/g16-047-toast-same-id origin/main`
- **Worker worktree policy:** follow `Completion Protocol`; launcher worktree
  first, named/manual fallback only when required.
- **Required sibling worktree links:** none
- **Active spec lane:** `docs/specs/015-loading-empty-error-notification-and-remediation-rules.md`
- **Roadmap milestone:** `docs/roadmaps/g16/README.md`
- **Ready cards, in order:** `docs/roadmaps/g16/047-toast-same-id-update-convention.md`
- **Allowed runway:** `g16.047` only
- **Remaining card budget:** one card
- **Dispatch topology:** may run beside `g16.048`; other ready g16 lanes may
  also exist, but none shares this lane's Toast-owned implementation surface
- **Parallel safety check:** g16.047 owns Toast contracts, timer machinery,
  component-local runtime files, focused evidence, its card, and its log;
  g16.048 owns AgentSubagent contract/benchmark evidence. Global g16 front
  doors remain orchestrator-owned.
- **Surfaces this lane owns:** ToastHost/ToastStack contracts; core Toast timer
  machinery; Toast component-local Svelte, React, shared Rust render, specimens,
  and focused tests; the g16.047 card; one g16.047 execution log; new relevant
  `PAPERCUTS.md` entries only
- **Integration ownership:** the orchestrator owns g16 README, generation index,
  continuation register/runway, triage, global logs/indexes, merge ordering, and
  post-merge closeout
- **Merge ordering:** same-repository PRs merge one at a time; the orchestrator
  refreshes this head against current `main` and re-reviews it if a sibling lane
  merges first
- **Canonical refs:** `docs/architecture/012-semantic-motion-policy.md`;
  `docs/contracts/001-working-rules.md`,
  `docs/contracts/components/toast-host.md`,
  `docs/contracts/components/toast-stack.md`,
  `docs/contracts/003-native-accessibility.md`, and
  `docs/specs/015-loading-empty-error-notification-and-remediation-rules.md`
- **Review oracle:** `docs/roadmaps/g16/047-toast-same-id-update-convention.md#review-oracle`
- **Model capability profile:** day-to-day implementation worker — bounded
  contract and active-runtime repair with settled semantics and exact tests
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** use the repo-local Effigy skill; no
  `*-windowed` or native-visual selector; no release command, workflow edit,
  registry mutation, sibling-repository write, or Jetstream behavior change
- **Required validation:** focused timer, presence, motion-retarget,
  announcement, focus, Svelte, React, Rust-node, and mounted GPUI tests;
  relevant drift selectors; `effigy ci:web`; `effigy ci:rust`; `effigy
  ci:native`; `effigy docs:check`; one final headless `effigy qa`; `git diff
  --check origin/main...HEAD`
- **PR base/head:** current pushed `main` / `feature/g16-047-toast-same-id`
- **PR URL:** pending
- **Review state:** awaiting implementation PR
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks

## Boundaries

Please keep this run inside the named runway:

- **In scope:** implement `g16.047`: contract-first Toast uniqueness, timer,
  announcement, focus, remediation, and native-role laws; repair the existing
  timer/update paths; add paired active-runtime, specimen, focused, and mounted
  proof; record the execution log.
- **Out of scope:** consumer repositories and operation models; Toast-owned
  pending/settled state; promise helpers; progress anatomy; retry or cancellation
  policy; a second creation API; Progress/Spinner APIs; motion-policy changes;
  releases, packages, workflows, registries, sibling repositories; GPUI AT or
  visual-ledger claims; Jetstream behavior.
- **Outcome shape:** one complete contract-valid repair and evidence PR. Preserve
  the configured `autoDismissMs` and `stickyTones` as authorities. `6000` and
  `['danger']` are default fixtures only: custom positive delay, zero/negative
  disabled expiry, and custom sticky-tone transitions must remain exact.
- Do not invent architecture, change unrelated contracts, widen the roadmap, or
  choose an unresolved product/API/persistence/security decision.
- This handoff represents one worker lane, and sibling lanes may be running
  concurrently. Write only inside **Surfaces this lane owns**. Leave any
  closeout or front-door surface assigned to **Integration ownership** to its
  named owner. If shared mutable scope, a hidden dependency, or another lane's
  write appears, stop and report it through the active control plane or the
  operator instead of resolving it yourself.
- Work only in the clean worker worktree selected by `Completion Protocol`.
  Never edit the planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge belongs to the orchestrator after its accepted
  review/check gate.

## Important Context

- **Planning lineage:** `g16.034` fixed motion ownership; `g16.043` concluded
  that in-place pending-to-settled lifecycle remains consumer-owned; the
  canonical post-triage runway accepted the bounded Poodle repair as `g16.047`.
- **Why this card is ready:** identity, timer transitions, announcement split,
  focus fallback, native severity, API non-goals, evidence, and stop conditions
  are fixed in the card.
- **Decisions and preferences:** one row per id; same-id replacement keeps the
  row and phase; sticky owns no clock; becoming sticky cancels the clock;
  becoming non-sticky starts exactly one current configured delay; copy/tone/
  action churn alone never resets a running clock; discrete settlement announces
  once; numeric progress does not enter atomic toast copy; danger maps to
  `NodeRole::Alert` without claiming GPUI AT parity.
- **Open tensions:** current runtime behavior may expose a focused reusable
  defect, but the fix cannot introduce operation ownership or a new native
  accessibility API. Stop rather than widening the lane.
- **Report after:** contracts plus timer transition table and biting focused
  proof form one coherent first chunk; report before wider runtime/specimen
  projection if evidence changes the plan.
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

Run the `Completion Protocol` preflight before broad reads. Then read
`AGENTS.md`, the g16 milestone, the full g16.047 card, and every canonical ref
from the selected worker worktree. Inspect the existing timer/update ownership,
commit biting tests before falsification plants, and start with the contract plus
complete configured timer transition table.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run: `git rev-parse --show-toplevel`, `git branch --show-current`, `git
   status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record its
   actual root/branch; do not compare its generated path/branch with the planned
   path/branch or create another worktree merely because they differ.
3. If current context is `main`, dirty, unregistered, or unusable, inspect the
   named worktree. If unusable, read `.agents.local.env`, require
   `AGENTS_WORKTREE_CONTAINER_DIR`, and ask the operator when absent. Create a
   unique worktree/branch there from pushed `origin/main`. Never use `/tmp`,
   `TMPDIR`, or a guessed path; never clean, reset, stash over, or discard dirty
   state. Report a launcher-supplied dirty or `main` worktree instead of creating
   another.
4. From the selected worktree, record this handoff's repository-relative path:
   `docs/handoffs/20260902-004202-g16-047-toast-same-id.md`. Run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm `git rev-parse HEAD` equals `git rev-parse origin/main`, confirm
   `git merge-base --is-ancestor 7f59ae42f4917c675968819eb23a5e41dc90013c HEAD`
   succeeds, and confirm that relative path exists in the selected `HEAD`. Load
   the tracked handoff with `git show HEAD:<relative-path>`. If the absolute
   dispatch file is readable and differs from that tracked blob, stop and
   report. The committed `HEAD` copy is the canonical execution input.
5. Required sibling links: none.
6. Read the active milestone, assigned card, `AGENTS.md`, and canonical refs.
7. Use `effigy tasks` only if selector discovery is needed; run the cheapest
   relevant orientation check and record what actually ran.

### While you work

- Execute the card in ordered, meaningful chunks and keep commits aligned with
  those chunks, not arbitrary turns.
- Preserve custom `autoDismissMs` and `stickyTones` in implementation and
  evidence; never encode the defaults as universal settlement behavior.
- Commit real proof before planting pre-fix behavior. Exercise every review-
  oracle counterexample, restore from committed truth, and rerun green.
- After each meaningful chunk, report through the active control plane or the
  operator with changed files, validation actually run, remaining work, risks,
  and blockers.
- Stop and say so if a contract is missing, intent is ambiguous, scope expands,
  authority/access is missing, or validation changes the plan.

### When the assigned runway is complete

1. Run the required final validation listed in **Current State**.
2. Falsify every exact, negative, and universal claim in the g16.047 review
   oracle, including custom delay, custom sticky tones, disabled expiry, focus,
   announcement, native role, and API-absence rows.
3. Update the g16.047 card and one execution log with actual evidence. Do not
   edit global roadmap front doors or continuation maps.
4. Push the selected worker branch. If a sibling lane merged first, rebase onto
   current `main`, rerun the required validation, and report the new exact head.
5. Open a reviewable PR against current pushed `main`. This handoff's planning
   base precedes the commit that contains the handoff and is not the PR base
   after later integration.
6. Link the card, milestone, contracts, changed surfaces, evidence, validation,
   and unresolved items in the PR body.
7. Report the PR URL and exact head. Do not merge.

### Review and merge path

The orchestrator reviews the exact PR head against the canonical refs, diff,
checks, and every g16.047 oracle row. Current review state: awaiting
implementation PR. Requested changes: none.

When the exact reviewed head is current, checks pass, the PR is mergeable, and
no stricter rule or operator pause applies, the orchestrator merges. If another
same-repository lane merged first, integration and exact-head re-review are
mandatory.

- **Closeout refs:** `docs/roadmaps/g16/047-toast-same-id-update-convention.md`;
  one new `docs/logs/2026-09/*g16-047*.md`; g16 README, generation index,
  continuation maps, and other front doors remain orchestrator-owned

### Handoff closeout

Before calling the runway complete, leave the card, execution log, and next-task
state honest. If blocked, record the blocker and stop rather than making the
handoff look complete.
