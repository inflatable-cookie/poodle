---
title: g16.057 core build substrate worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-102700-g16-057-core-build-substrate.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, web-distribution]
---

## What This Thread Was Doing

Poodle has accepted and frozen its compiled web distribution contract. This
handoff dispatches only `g16.057`: implement the reusable clean-staging and
build-driver substrate, compile core JavaScript and declarations, stage the
exact core CSS/token/icon/licensing assets, reject forbidden output, and emit a
deterministic core build receipt.

This is one bounded implementation lane. It does not build Svelte or React,
create the permanent installed-certification harness, or perform release work.
No transcript or second prompt is part of the authority chain.

## Why It Matters

Core is the substrate for both shell distributions. `g16.058` cannot compile
Svelte or React honestly until one repo-owned driver can produce a typed,
source-free, stable-name, receipt-backed core `dist/` tree. This card proves
that foundation without letting shell or release concerns leak into it.

Accepted merge unlocks only `g16.058`. It does not certify installed packages,
unblock `g16.054`, change a version, or release anything.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `b515f005a40b40005528a44933e49f4fd1c446c7`
- **Pushed main verification:** local `HEAD` and `origin/main` both resolved to
  `b515f005a40b40005528a44933e49f4fd1c446c7` before this handoff was finalized
- **Planning checkout:** clean; PR #159 is merged and the `g16.057` readiness
  update is committed and pushed
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** accepted architecture 014,
  active spec 070, completed `g16.056`, ready `g16.057`, and serial successor
  cards `g16.058`–`g16.059`
- **Worker branch:** `feature/g16-057-core-build-substrate`
- **Worker worktree:** dedicated launcher-managed non-`main` worktree; manual
  fallback only under the operator-selected `AGENTS_WORKTREE_CONTAINER_DIR`
- **Worker worktree policy:** follow `Completion Protocol`; reuse a clean,
  registered, launcher-provided non-`main` worktree regardless of its generated
  path or branch name; never create a second worktree behind it
- **Required sibling worktree links:** none
- **Active spec lane:**
  `docs/roadmaps/g16/component-continuation-runway.md`
- **Roadmap milestone:** `docs/roadmaps/g16/README.md`
- **Ready cards, in order:**
  `docs/roadmaps/g16/057-core-build-substrate.md`
- **Allowed runway:** `g16.057` only
- **Remaining card budget:** one card
- **Dispatch topology:** independent ready-frontier lane beside `g16.050` icon
  geometry runtime work; `g16.058` stays serially blocked on this card
- **Parallel safety check:** this lane owns core build/staging code, core
  manifest targets, core assets/declarations, focused build proofs, and its
  card/log. It must not edit icon geometry/runtime files or global roadmap
  front doors. Stop if a shared mutable surface or generated-artifact
  dependency appears.
- **Surfaces this lane owns:** core build/staging driver and configuration;
  `packages/core` manifest/export targets, declaration configuration, and exact
  asset-copy/receipt logic; focused core build tests and disposable core-only
  test fixtures; `docs/roadmaps/g16/057-core-build-substrate.md`; one
  `g16.057` execution log; new `PAPERCUTS.md` entries only for newly observed
  execution friction
- **Integration ownership:** the orchestrator owns
  `docs/roadmaps/g16/README.md`, `docs/roadmaps/generation-index.md`,
  `docs/roadmaps/g16/component-continuation-runway.md`, continuation-register
  and roadmap front doors, cross-lane status, dispatch state, review, and merge
- **Merge ordering:** same-repository PRs merge one at a time; if another lane
  merges first, refresh this branch against current `main`, revalidate, and
  expect exact-head re-review
- **Canonical refs:**
  `docs/architecture/014-compiled-web-package-distribution.md`;
  `docs/specs/070-compiled-web-distribution-contract.md`;
  `docs/specs/022-packaging-versioning-and-release-channel-rules.md`;
  `docs/architecture/002-token-system-and-package-layout.md`;
  `docs/contracts/001-working-rules.md`; current core manifest, entry, style,
  token, icon, notice, and declaration inventories
- **Review oracle:** `g16.057` `## Review Oracle` and spec 070's exact core
  inventory, export-map, receipt, and forbidden-content laws
- **Model capability profile:** `day-to-day` non-frontier implementation worker;
  architecture, inventories, acceptance rules, and stop conditions are frozen,
  leaving bounded build engineering and evidence work
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** no Svelte or React build/configuration/output;
  no g16.059 installed-certification redesign; no versions, changelog,
  release notes, candidate history, workflows, tags, publishing, registries,
  sibling repositories, compatibility aliases, source fallbacks,
  `*-windowed`, or native-visual work
- **Required validation:** focused build-driver/unit tests; core declaration
  resolution under Bundler and NodeNext; two clean core builds with exact
  inventory and file-hash comparison; core export/content/source/map/parser
  inspection; relevant Effigy core/build selectors; clean-checkout `effigy
  health`; the current `effigy test:web-pack-install`; `effigy ci:web`;
  `effigy docs:check`; and `git diff --check origin/main...HEAD`
- **PR base/head:** current pushed `main` at dispatch / worker branch head
  pending
- **PR URL:** pending
- **Review state:** awaiting worker implementation and PR, then exact-head
  orchestrator review
- **Merge path:** orchestrator after accepted exact-head review and passing
  required checks

## Boundaries

Please keep this run inside the named runway:

- **In scope:** complete every ordered-work, acceptance, evidence, and review-
  oracle row in `g16.057`; add the reusable clean-staging/build-driver
  foundation around Vite library mode and separate TypeScript declaration
  emit; compile the full explicit sorted core JavaScript entry inventory with
  stable public/chunk/asset names; stage exact core CSS, generated token CSS,
  icon runtime/CLI assets, licences, notices, and package docs; rewrite every
  existing core public target to `dist`; emit declarations with
  `emitDeclarationOnly` and `declarationMap: false`; prove Bundler and NodeNext
  resolution; reject raw source, maps, workspace/source paths, missing targets,
  unsorted inventories, absolute paths, timestamps, and unrelated parser or
  shell dependencies; write deterministic `dist/.poodle-build.json`; compare
  two clean builds file-for-file and hash-for-hash; add focused tests, one
  execution log, and one reviewable PR.
- **Out of scope:** Svelte or React compilation, manifests, exports, declaration
  builds, artifacts, or tests; `g16.058`–`g16.059`; the new
  installed-certification redesign; installed consumer
  certification; versions; changelog; release notes; candidate history;
  workflows; release commands; tags; publishing; registries; consumer or
  sibling-repository changes; compatibility shims, aliases, or source
  fallbacks; global roadmap/front-door edits.
- **Outcome shape:** one reusable core-only build substrate and deterministic
  staged core distribution that implements the frozen contract. Do not use
  hand-edited output, committed generated `dist/`, or shell-specific behavior
  as the foundation.
- Preserve spec 070 exactly: core export conditions are `types`, `import`, then
  `default`; `import` and `default` target the same compiled JavaScript; CSS-only
  exports remain conditionless; public filenames are unhashed; chunks use
  stable `[name]`; `bin.poodle-icons` and `./icons/build` target compiled
  runtime/declarations; core contains no Svelte, React, or `marked` edge.
- The receipt must use the schema and exact hash formulas frozen by spec 070.
  It must contain no timestamp or absolute path. Do not substitute a
  semantically similar receipt or redefine bytes in implementation.
- **Exact-head review addendum:** the first implementation head exposed an
  integration edge the original scope incorrectly deferred. Moving core
  `files` and exports to ignored `dist/` made a clean checkout's standalone
  health route fail, and the existing package-install gate treated only
  `files: ["src"]` as a directory tree. The worker may repair existing task
  prerequisites and generalize that one current archive-membership check so
  health, the existing pack gate, and `ci:web` remain green. This does not
  authorize g16.059's new receipt, installed-consumer, multi-version, or
  two-pack certification work.
- This handoff represents one worker lane, and `g16.050` may run concurrently.
  Write only inside **Surfaces this lane owns**. Leave every global roadmap and
  front-door surface to **Integration ownership**. Stop on shared mutable scope
  or a hidden cross-lane dependency instead of resolving it yourself.
- Work only in the clean dedicated worker worktree selected by `Completion
  Protocol`. Never edit the planning checkout or an unrelated dirty checkout.
- Do not merge the PR. Merge belongs to the orchestrator after exact-head
  review and the required check gate.

## Important Context

- **Planning lineage:** the holistic posture review made compiled JavaScript and
  declarations a prerequisite for `0.3.0`. Architecture 014 then split the work
  into four serial cards. `g16.056` froze the exact 176-name roster, 167 core
  CSS files, 108 icons, public export maps, stable naming, dependency ownership,
  receipt bytes, and forbidden archive content. This lane implements only the
  core foundation under that frozen denominator.
- **Why this card is ready:** PR #159 merged the exact contract on current
  pushed `main`. Core package membership, names, export conditions,
  declaration policy, asset ownership, receipt schema, acceptance, and stop
  conditions are settled. The worker does not need to choose package semantics.
- **Decisions and preferences:** use a repo-owned driver around Vite library
  mode; emit declarations separately; clean staging before every build;
  explicit sorted entries; stable output names; no declaration maps; CSS and
  tokens remain core-owned; the icon CLI and runtime are compiled; receipts
  are deterministic and provenance-bearing; two clean builds must agree.
- **Open tensions:** current core manifest targets still point into `src/` and
  the repository may lack a reusable driver. Repair those core-only mechanics.
  Stop if implementation requires changing a shell, weakening a frozen export
  or receipt law, adding a source fallback, or making a new dependency/product
  choice.
- **Report after:** the driver and clean staging work, then after declarations
  and exact asset/export projection, then after deterministic receipt/oracle
  validation and PR creation.
- **Report to:** the active control plane or operator for relay to the Poodle
  orchestrator.

## Suggested Next Move

Run the `Completion Protocol` preflight before broad reads. Then read
`AGENTS.md`, `g16.057`, architecture 014, spec 070, packaging spec 022, package
layout architecture 002, working rules, the core manifest, and the current
core entry/style/token/icon inventories. Build one exact input/output matrix
from spec 070 before touching build code. Implement clean staging and stable
naming as the first coherent batch; do not begin with manifest rewrites that
have no produced artifacts to validate.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`. Do not run
   `effigy tasks`, `effigy doctor`, discovery commands, or broad repository
   reads before this decision.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided dedicated worktree.
   Record the actual root and branch. Do not compare generated names with this
   handoff or create another worktree merely because they differ.
3. If the current context is `main`, dirty, unregistered, or otherwise
   unusable, stop and inspect the named launcher workspace first. Only when a
   manual fallback is required may you read `.agents.local.env`, require
   `AGENTS_WORKTREE_CONTAINER_DIR`, and create one unique branch/worktree under
   that operator-selected container from `origin/main`. Ask the operator if the
   variable is absent. Never use `/tmp`, `TMPDIR`, or a guessed path; never
   clean, reset, stash over, or discard dirty state. A launcher-supplied dirty
   or `main` worktree is a stop condition, not permission to create a duplicate.
4. From the selected worktree, run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm `HEAD == origin/main`, confirm
   `git merge-base --is-ancestor b515f005a40b40005528a44933e49f4fd1c446c7 HEAD`,
   and confirm this repository-relative handoff exists in `HEAD`. Load it with
   `git show HEAD:docs/handoffs/20260902-102700-g16-057-core-build-substrate.md`.
   If the absolute dispatch file differs from that tracked blob, stop. The
   committed `HEAD` copy is canonical.
5. Required sibling worktree links are `none`.
6. Read the active milestone, assigned card, `AGENTS.md`, and canonical refs.
   Use Effigy only where it fits the job and record what actually ran.

### While you work

- Execute `g16.057` as one core-build batch. Establish the reusable driver and
  clean staging, then project the exact entries/assets/declarations, then add
  the receipt and adversarial proofs. Keep commits aligned with those meaningful
  chunks rather than model turns.
- Preserve current public core coverage while changing its archive boundary.
  Derive inventories from the frozen spec and fail on disagreement; do not
  silently drop, add, or rename a public target.
- After each meaningful chunk, report changed files, validation actually run,
  what remains, risks, and blockers.
- Stop if the work needs a new export/product choice, shell changes, permanent
  certification code, release/version/history edits, workflow authority, or
  another lane's mutable surface.

### When the assigned runway is complete

1. Run the required final validation exactly as listed in **Current State**.
   Do not run any `*-windowed` or native-visual selector.
2. Falsify every `g16.057` oracle row. At minimum plant and restore: a hashed
   public filename; one missing token/style export or asset; staged raw
   non-declaration TypeScript; a timestamp or absolute path in the receipt; and
   a Svelte/React build or dependency entering the core lane. Record the proof
   that fails for each plant. Commit a clean proof point before planting and
   restore from that commit so tests cannot disappear with the planted change.
3. Run two clean builds from the same exact commit. Compare sorted inventories,
   every file hash, and the receipt. Inspect every core export target under
   Bundler and NodeNext and prove no raw source, map, source/workspace path,
   `marked`, Svelte, or React edge entered staging.
4. Reconcile implementation, `g16.057`, and one execution log with the actual
   result. Do not edit the g16 README, generation index, continuation runway or
   register, or another global roadmap front door.
5. Push the worker branch. If a sibling lane merged first, refresh onto current
   `main`, rerun required validation, and report the new exact head.
6. Open one PR against current pushed `main`. The planning base above is not a
   self-referential hash for the handoff commit.
7. Link architecture 014, spec 070, `g16.057`, changed build/manifest/test
   surfaces, deterministic receipts, validation, falsification evidence, and
   unresolved items. Report the PR URL and exact head. Do not merge or start
   `g16.058`.

### Review and merge path

The orchestrator reviews the current PR head against architecture 014, spec
070, `g16.057`, the exact core inventory/export/receipt laws, full diff,
falsification evidence, and required checks. Shared-identity review is posted
as the canonical PR comment when formal self-approval is unavailable.
Requested changes stay on this branch. Blocking classes are `execution-miss`,
`oracle-gap`, `planning-change`, `validation-gap`, and `integration-drift`; a
`planning-change` returns to planning before revision. Requested changes:
none. The orchestrator alone merges an accepted, current, mergeable head after
required checks.

- **Closeout refs:** core build/staging and package-manifest surfaces,
  `docs/roadmaps/g16/057-core-build-substrate.md`, and one
  `docs/logs/2026-09/` g16.057 execution log. Global roadmap/front-door state
  and `g16.058` readiness remain orchestrator-owned after merge.

### Handoff closeout

Before calling the runway complete, leave the core build, package targets,
receipt, card, log, and next-task state honest. An accepted `g16.057` merge
unlocks `g16.058` only. It does not compile either shell, certify installation,
unblock `g16.054`, change `0.3.0`, or release anything. If blocked, record the
exact blocker and stop.
