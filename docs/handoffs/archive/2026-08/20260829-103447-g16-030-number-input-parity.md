---
title: g16.030 NumberInput value, draft, and mounted parity worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle component parity
created: 2026-08-29
updated: 2026-08-29
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260829-103447-g16-030-number-input-parity.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, number-input]
---

## What This Thread Was Doing

The orchestrator has closed g16.029 and promoted the next bounded component
continuation card. This worker owns g16.030 only: replace NumberInput's
number-or-string and static-native split with one paired numeric editing model,
make the approved clean pre-1.0 callback migration, and mount a real GPUI
editor.

Do not continue into continuous audio or the drag-and-drop programme.

## Why It Matters

NumberInput is reused by form, filter, colour, inspector, and schema-driven
surfaces. Today the web adapters preserve partial drafts privately while the
Rust path exposes a concrete number and stepper-only renderer. A typed committed
value plus an explicit raw-draft channel gives all active runtimes the same
parsing, validity, step, precision, commit, and replacement semantics.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `7c1c2c98ac62916f34a509df39eb310a89f68c4b`
- **Pushed main verification:** local `HEAD` and `origin/main` both equalled the
  planning base before this handoff commit.
- **Planning checkout:** clean before this docs-only dispatch commit.
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Planning artifacts included at the base:** approved NumberInput contract and
  decision note, completed g16.029, merged g16.021, current g16 runway, and the
  parity ledger. This handoff commit marks g16.030 ready.
- **Worker branch:** `t3code/g16-030-number-input-parity` is the suggested manual
  fallback name; use the launcher-provided non-`main` branch when one exists.
- **Worker worktree:** harness-managed; record the actual launcher path. A
  manual fallback may only use the operator-selected container from
  `.agents.local.env`.
- **Worktree creation command:** none by default; use the supplied worktree.
- **Required sibling worktree links:** none. Sibling consumers are inspection
  evidence only, and Jetstream remains deferred.
- **Active spec lane:** none; the decision is promoted into
  `docs/contracts/components/number-input.md`.
- **Roadmap milestone:** `docs/roadmaps/g16/README.md`
- **Ready cards, in order:**
  `docs/roadmaps/g16/030-number-input-value-draft-and-mounted-parity.md`
- **Allowed runway:** g16.030 only
- **Remaining card budget:** one card in five coherent batches
- **Dispatch topology:** serial implementation lane. The orchestrator may
  prepare g16.022 while this runs, but no second worker may edit the shared core
  or Svelte/React export surfaces.
- **Parallel safety check:** no open PR currently owns NumberInput, the paired
  core/headless exports, the domain vectors, or GPUI input routing. Stop if an
  overlapping worker appears.
- **Canonical refs:**
  `docs/architecture/006-headless-core-and-machine-model.md`;
  `docs/contracts/components/number-input.md`,
  `docs/contracts/001-working-rules.md`,
  `docs/contracts/005-agent-local-paths.md`
- **Decision lineage:**
  `docs/triage/20260826-213343-number-input-native-value-model.md`
- **Model capability profile:** cross-language TypeScript/Rust state-machine
  work, controlled editing, decimal semantics, GPUI mounted interaction,
  accessibility, and a clean public migration
- **Tool/runtime restrictions:** use repo-local Effigy routing and keep every
  check headless. Never run `*-windowed`, native visual, Jetstream preview/QA,
  release, tag, publication, or workflow-mutation selectors.
- **Required validation:** every focused and broad selector named by g16.030,
  one final headless `effigy qa`, `git diff --check origin/main...HEAD`, and the
  required active-surface searches proving removed APIs are absent
- **PR base/head:** `main` / actual worker branch
- **PR URL:** pending
- **Review state:** awaiting implementation and orchestrator review
- **Merge authorisation:** worker must not merge; the operator authorises merge
  only after orchestrator review

## Boundaries

- **In scope:** the paired pure NumberInput model and shared vectors;
  Svelte/React committed-value and raw-draft migration; clean callback removal;
  bounded ColorPicker/FilterBuilder regression maintenance; Rust spec/render
  migration; mounted GPUI editing; curated specimens; the single ledger-cell
  move; and honest card/log/front-door closeout.
- **Out of scope:** TimeInput, EditableLabel, continuous audio, drag-and-drop,
  broad IME or locale formatting, tokens without a proven gap, Jetstream
  admission, sibling-repository edits, workflows, versions, and releases.
- Preserve the locked finite base-10 syntax, optional `number | null` committed
  value, explicit optional draft, invalid no-emit behavior, commit boundary,
  and decimal-safe step/precision rules. Do not add aliases, coercion,
  compatibility wrappers, silent normalization, or recovery defaults.
- This is one worker lane. If shared mutable scope or an unplanned contract
  decision appears, stop and report it through the operator.
- Work only in the selected clean worker worktree. Never edit the
  orchestrator's `main` checkout or another dirty checkout.
- Do not merge the PR.

## Important Context

- **Planning lineage:** g16.020 audited the roster. The operator approved the
  typed committed value plus explicit raw-draft model and the clean callback
  migration. g16.021 and g16.029 then landed the shared foundations that had to
  precede this card.
- **Why this card is ready:** value and draft ownership, syntax, validity,
  precision, bounds, step anchoring, callback timing, controlled replacement,
  public removals, mounted behavior, acceptance, validation, evidence movement,
  and stop conditions are explicit in the contract and card.
- **Decisions and preferences:** incomplete or invalid drafts remain visible
  and emit no committed value. Empty emits `null`. Blur or Escape reverts an
  unresolved draft. Valid Enter/blur commits; successful stepping emits value
  and commit. Direct entry never clamps, snaps, rounds, defaults, or coerces.
- **Open tensions:** decimal normalization must match across TypeScript and Rust;
  controlled host echoes must not erase active drafts; GPUI must express an
  editable SpinButton through the crates.io backend. The card names stop
  conditions for all three.
- **Report after:** paired semantics and vectors pass; web and clean API
  migration pass; mounted GPUI behavior is proven; final QA and PR are ready
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

Read this file from the top. Before broad repository reads, run the startup
worktree-safety preflight below and accept a clean launcher-provided non-`main`
worktree regardless of its generated name. Then read `AGENTS.md`, g16's front
door, card 030, the NumberInput contract and decision note, architecture 006,
the working rules, and the local Effigy skill. Use `effigy tasks` after the
worktree decision to resolve the named selectors. Implement the five card
batches in order and stop on any card stop condition.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Before
   broad reads, run only `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. If the current root is a clean, registered, dedicated non-`main` worktree,
   accept it as launcher-provided. Record its actual path and branch; do not
   create another worktree because its generated names differ from this file.
3. If the launcher supplied a dirty or `main` worktree, stop and report it. If
   no usable launcher worktree exists, inspect the named fallback, then read
   `.agents.local.env` and require `AGENTS_WORKTREE_CONTAINER_DIR`. Ask the
   operator if absent. Create a unique worktree and branch under that container
   from `origin/main`; never use `/tmp`, `TMPDIR`, or a guessed path, and never
   clean, reset, stash over, or discard another checkout's state.
4. In the selected worktree, record this file's relative path. Fetch origin,
   confirm `HEAD == origin/main`, confirm
   `git merge-base --is-ancestor 7c1c2c98ac62916f34a509df39eb310a89f68c4b HEAD`,
   and load the tracked handoff with
   `git show HEAD:docs/handoffs/20260829-103447-g16-030-number-input-parity.md`.
   If that blob differs from the absolute dispatch file, stop. The committed
   blob is canonical.
5. Required sibling links are `none`; create none.
6. Read the active milestone, assigned card, `AGENTS.md`, canonical refs, and
   `.agents/skills/effigy/SKILL.md`.
7. Run cheap repository orientation through the supported Effigy route and
   record what actually ran.

### While you work

- Execute only g16.030, in its five ordered batches. Keep commits aligned with
  meaningful chunks.
- Report after each meaningful chunk with changed files, checks actually run,
  remaining work, risks, and blockers.
- Stop on missing authority, changed semantics, scope expansion, a shared-file
  collision, or validation that changes the plan. Do not invent architecture.
- Record small execution friction in `PAPERCUTS.md` without widening this card.

### When the assigned runway is complete

1. Run the complete validation set from g16.030. Everything stays headless.
2. Update the required NumberInput contract/card/log/ledger/front-door evidence
   and leave one honest next task.
3. Push the selected worker branch and open a reviewable PR against current
   `main`.
4. The PR body must link g16.030 and its governing refs, summarize changed
   surfaces, migration evidence, ledger delta, validation, and unresolved
   items.
5. Report the PR URL and evidence to the operator. Do not merge.

### Review and merge path

The orchestrator independently reviews metadata, commits, diff, checks, and
changed files against the contract and card. When shared GitHub identity blocks
formal approval, the orchestrator posts the verdict as a PR comment. Address
only requested changes on this branch and report back. Merge requires separate
operator authorisation.

- **Requested changes:** none yet
- **Closeout refs:**
  `docs/roadmaps/g16/030-number-input-value-draft-and-mounted-parity.md`, one
  August execution log, `docs/roadmaps/g16/parity-evidence-ledger.md`,
  `docs/roadmaps/g16/README.md`, `docs/roadmaps/README.md`, and
  `docs/roadmaps/generation-index.md`

### Handoff closeout

Leave the card, log, ledger, milestone, and front doors truthful. If blocked,
record the blocker and stop rather than making the handoff look complete.
