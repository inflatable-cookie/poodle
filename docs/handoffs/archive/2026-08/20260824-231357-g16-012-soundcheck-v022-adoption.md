---
title: Soundcheck Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-24
updated: 2026-08-24
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260824-231357-g16-012-soundcheck-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, soundcheck]
---

## What This Thread Was Doing

Poodle 0.2.2, Longhorn's adoption, and Soundcheck Library's adoption are all
merged. This worker removes Soundcheck's committed sibling Poodle sources,
pins the exact public 0.2.2 packages, and proves its local Longhorn and
Soundcheck Library layers converge on the same release. No transcript is
required beyond this handoff.

## Why It Matters

Soundcheck is the first three-layer product proof: application, Longhorn UI
adapter, and Soundcheck Library components. Passing it gives stronger evidence
than another isolated manifest bump and unblocks an active product directly.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/soundcheck`
- **Target base:** `ddec155b0963d8dc0a6aba2ed9774c826cdc375c`
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `a7293ecd43bb6715afea8c061c27223d39b0b1aa`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean and synchronized before this planning batch
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-012-soundcheck-poodle-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Roadmap card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g15/066-soundcheck-poodle-v022-adoption.md`
- **Allowed runway:** that one card only
- **Parallel topology:** runs beside Nucleus `065` and Underlay Reference
  `067`; no shared mutable repository files
- **Required validation:** card validation plus Soundcheck's headless board;
  no dev/Tauri/assistant-proof launch
- **PR:** Soundcheck `main` <- worker branch; pending
- **Review state:** awaiting orchestrator review; worker must not merge

## Boundaries

- Update Soundcheck-owned Poodle declarations, remove only its Poodle source
  overrides, regenerate its lock, and repair bounded 0.2.2 compatibility fallout.
- Preserve local Longhorn and Soundcheck Library dependencies and the Longhorn
  override. Do not edit those sibling repositories.
- Do not change product, plugin-library, Tauri, or agent-control behavior; do not
  publish, add a shim, or launch the app.
- Work only in the clean launcher-provided Soundcheck worktree. Do not edit main
  checkouts and do not merge the PR.

## Important Context

- `package.json` declares Poodle core/Svelte `^0.1.0` and overrides both to
  `../poodle`; `bun.lock` therefore proves the sibling tree rather than npm.
- Longhorn PR 9 and Soundcheck Library PR 6 now require Poodle 0.2.2-compatible
  peers. Their local `file:` links remain intentional development inputs.
- Report after manifest/lock convergence and after validation. Separate any
  pre-existing baseline failure from the adoption result with target-base
  evidence.

## Suggested Next Move

Read `g15.066`, pin direct Poodle dependencies to exact 0.2.2, delete only the
two Poodle overrides, and regenerate `bun.lock` narrowly. Prove registry
integrity and one peer resolution before touching source code. Stop if local
Longhorn or Soundcheck Library metadata still forces an old Poodle identity.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Soundcheck worktree supplied by the launcher. Record it; do not create a
   second worktree because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean it or guess `/tmp`; manual fallback requires
   Soundcheck's operator-selected `AGENTS_WORKTREE_CONTAINER_DIR`.
4. Run `git fetch origin`; confirm target `HEAD == origin/main == ddec155b`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `a7293ecd` is an ancestor of Poodle `origin/main`.
5. Read Soundcheck `AGENTS.md`, working rules, `g15.066`, and its governing
   Poodle cards. Run `effigy tasks` as orientation.

### While you work

- Keep changes inside the adoption card and report meaningful chunks.
- Stop on contract/API scope, duplicate/local Poodle resolution, unrelated lock
  churn, visible-run requirements, or a release defect.

### When the assigned runway is complete

1. Run the card's source/lock inspection, `effigy build`, `effigy qa`, relevant
   headless integration selectors, and `git diff --check`.
2. Push the worker branch and open a reviewable PR against Soundcheck `main`.
3. Link `g15.066`; record exact registry and peer resolution, changed files,
   compatibility edits, baseline comparison, lock review, and validation.
4. Report the PR URL and evidence to the operator. Do not merge or edit Poodle
   planning state.

### Review and merge path

The Poodle orchestrator will review independently. Shared GitHub identity may
require a canonical PR comment instead of formal approval. Make only requested
changes. Merge requires separate operator authority.
