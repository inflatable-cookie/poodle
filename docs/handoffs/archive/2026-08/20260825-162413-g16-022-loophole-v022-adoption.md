---
title: Loophole Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-162413-g16-022-loophole-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, loophole]
---

## What This Thread Was Doing

Loophole Desktop still resolves Poodle through committed sibling paths. This
worker moves only the Desktop web package to exact registry 0.2.2 while
preserving local Longhorn integration, audio/renderer boundaries, and product
behavior.

## Why It Matters

Loophole is both a Poodle consumer and a demanding renderer application. The
adoption must prove the packed package without letting a dependency bump spill
into audio, persistence, or renderer redesign.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/loophole`
- **Target base:** `6f770f59c3a83a0da8ccd9ebcc03f15e7c5b88a2`, verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository/base:** `/Users/tom/Dev/projects/poodle` at
  pushed `3a9b69b471259539b5c13585ae06e42cf5fa2d63`
- **Planning checkout:** clean `main`, with `HEAD == origin/main`
- **Worker branch:** launcher-generated non-`main`; suggested
  `t3code/g16-022-loophole-v022`
- **Worker worktree:** use the launcher's clean registered worktree
- **Ready card:** `/Users/tom/Dev/projects/poodle/docs/roadmaps/g15/076-loophole-poodle-v022-adoption.md`
- **Allowed runway:** `g15.076` only; one repository and one PR
- **Parallel safety:** Loophole had no secondary worktree or open PR at dispatch
- **Canonical refs:** Poodle `g15.055`, `g15.066`, `g15.076`; Loophole root and
  Desktop `AGENTS.md`, working rules, strict guardrails, and local Effigy skill
- **Required validation:** supported install, Desktop `bun run check`,
  `effigy build:renderer`, `effigy test:renderer`,
  `effigy test:workspace`, `effigy qa`, scoped formatting if needed, and
  `git diff --check`; no visible app
- **PR base/head:** Loophole `main` <- worker branch
- **PR URL:** pending; review awaiting orchestrator; worker must not merge

## Boundaries

- Replace Desktop core/Svelte sibling dependencies and Poodle overrides with
  exact registry `0.2.2`; keep Longhorn dependency/override local.
- Regenerate only `apps/desktop/bun.lock` and prove one Svelte identity.
- Do not edit Poodle/Longhorn, audio engine, renderer architecture,
  persistence, product behavior, or unrelated workspaces.
- Never run `cargo fmt --all`; use Loophole's scoped formatter if Rust changes
  are genuinely required. Do not launch the app or add shims/aliases.

## Important Context

- The current Desktop manifest carries both sibling dependencies and sibling
  Poodle overrides.
- Renderer/workspace checks are required because the package sits across the
  web/native boundary, but this card does not authorise renderer redesign.
- Report after graph/lock convergence, then after validation. Stop on a public
  API/renderer decision, unrelated churn, or visible proof requirement.

## Suggested Next Move

Run the worker preflight, read Loophole authority and `g15.076`, then inventory
the Desktop manifest/lock. Use the supported install path, prove one registry
identity, and only then address bounded compatibility failures.

## Completion Protocol

### Before you start

1. This handoff activates worker mode. Run the four-command
   root/branch/status/worktree preflight before broad reads.
2. Use only a clean registered non-`main` launcher worktree. If the context is
   `main`, dirty, or unregistered, stop. Manual fallback requires Loophole's
   `.agents.local.env` `AGENTS_WORKTREE_CONTAINER_DIR`; never guess or use
   `/tmp`, and never clean an operator checkout.
3. Fetch origin; confirm `HEAD == origin/main == 6f770f59`. Confirm this
   handoff exists and planning base `3a9b69b4` is an ancestor of Poodle
   `origin/main`.
4. Read the card, instructions, guardrails, and local Effigy skill; recheck open
   PRs/worktrees before editing.

### While you work

Keep one bounded Desktop dependency batch. Review every lock hunk; use scoped
formatting only. Report meaningful chunks and stop on shared scope, missing
published surfaces, unrelated changes, or architectural decisions.

### When the assigned runway is complete

Run all required checks and `git diff --check`. Record exact identities, peer
convergence, lock review, compatibility edits, worktree, and branch in one PR
against Loophole `main`. Link `g15.076`, report the URL, and do not merge.

### Review and merge path

The Poodle orchestrator reviews independently; shared identity may require a
canonical PR comment. Merge needs separate operator authority.

### Handoff closeout

Leave evidence honest. The orchestrator owns planning closeout after merge.
