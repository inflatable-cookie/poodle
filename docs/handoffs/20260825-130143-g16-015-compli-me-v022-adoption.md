---
title: Compli Me Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-130143-g16-015-compli-me-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, compli-me]
---

## What This Thread Was Doing

Poodle 0.2.2 is published and its foundation consumers are already migrated.
This worker moves Compli Me's Admin and Front apps from Poodle 0.1.x and
committed sibling-Poodle overrides to the public release. It also refreshes the
repository's four Bun locks so stale Underlay snapshots do not preserve a local
Poodle identity behind otherwise-correct app manifests.

This is one independent repository lane. Acowtancy `g16.014` is already running
elsewhere; no files or decisions are shared between the two workers.

## Why It Matters

Compli Me is the next Underlay-shaped product proof: two Svelte apps, shared UI
and API-client packages, local Underlay integration, and package-scoped locks.
The migration must leave one public Poodle identity throughout that graph
without changing Compli Me's application or Underlay boundaries.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/compli-me`
- **Target base:** `ee27d84964c61d406f90a5b9f8d4ed96e059d5b7`; verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `3b1da7ea894470b96cf2ba3991024746976fd9b6`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean, on `main`, with `HEAD == origin/main` before
  this handoff
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-015-compli-me-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/README.md`
- **Ready card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/015-compli-me-poodle-v022-adoption.md`
- **Allowed runway:** `g16.015` only
- **Remaining card budget:** one repository and one PR
- **Dispatch topology:** independent parallel lane alongside `g16.014`; it
  does not own another consumer repository
- **Parallel safety:** separate repository, locks, branch, worktree, and PR;
  stop if a Poodle or Underlay source change becomes necessary
- **Canonical refs:** Poodle `g16.001`, `g16.009`, `g16.013`, `g16.015`;
  Compli Me root/Admin/Front/UI `AGENTS.md`, working rules, and local Effigy
  contract
- **Known starting graph:** Admin and Front declare core/Svelte `^0.1.0` and
  local Poodle overrides. Admin, Front, UI, and API-client locks retain local
  Poodle resolutions, including stale metadata copied through local Underlay.
- **Model capability profile:** bounded dependency-adoption worker; pause on
  public API, Underlay, template, deployment, or dependency-policy decisions
- **Tool/runtime restrictions:** use Compli Me's repo-local Effigy skill and
  Effigy-owned host/container routing. Do not launch the visible app stack and
  do not treat host artifacts as live-runtime evidence.
- **Required validation:** `effigy admin/validate`, `effigy front/validate`,
  `effigy ui/validate`, `effigy validate`, `effigy qa`, and
  `git diff --check`; use the package-owned Effigy prepare/install path before
  validation and record any reproduced pre-existing baseline separately
- **PR base/head:** Compli Me `main` <- worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review
- **Merge authorisation:** worker must not merge

## Boundaries

- Pin Admin and Front direct `@inflatable-cookie/poodle-core` and
  `@inflatable-cookie/poodle-svelte` dependencies to exact `0.2.2`.
- Remove their committed Poodle overrides. Retain local Underlay sources and
  Compli Me's existing workspace structure.
- Regenerate Admin, Front, UI, and API-client Bun locks narrowly. Inspect the
  complete lock diff: the final active graph must contain registry Poodle 0.2.2
  with integrity and no `file:` Poodle resolution, including under local
  Underlay snapshots.
- Repair only Compli Me-owned compatibility failures directly caused by the
  Poodle bump.
- Do not edit Poodle, Underlay, APIs, deployment, containers, templates, or
  product behavior. Do not add aliases, compatibility shims, new direct
  dependencies, test exceptions, or unrelated upgrades.
- Work only in the clean launcher-provided Compli Me worktree. Do not edit the
  Poodle planning checkout, the operator's Compli Me checkout, or another
  consumer lane. Do not merge.

## Important Context

- Compli Me has separate `bun.lock` files in `admin`, `front`, `ui`, and
  `api-client`; there is no single root lock to stand in for them.
- Admin and Front are the only direct Poodle consumers. UI and API-client still
  need lock refresh/review because their local Underlay snapshot currently
  embeds a sibling Poodle dependency.
- Underlay `g16.009` and Underlay Reference `g16.013` established the intended
  result: keep the Underlay source local where the consumer owns that boundary,
  while its Poodle dependency resolves from the registry. Do not remove
  Underlay merely to make the lock look simpler.
- Compli Me's live stack is Effigy-owned. Run `effigy tasks`, `effigy health`,
  and `effigy test --plan` after selecting the worker worktree, then use the
  supported package or container execution route. A raw host install is not
  evidence that the supported runtime changed.
- Report after registry/override/lock convergence, then after the full
  validation board. Stop if either phase changes the plan.

## Suggested Next Move

Read `g16.015`, then inventory the four manifests and four locks from the
selected Compli Me worktree. Confirm the current local Underlay checkout is the
already-migrated source before regenerating locks. Use Compli Me's Effigy-owned
prepare path, prove one registry Poodle 0.2.2 identity across every lock, and
only then address any compatibility fallout.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Compli Me worktree supplied by the launcher. Record its actual root and
   branch; do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean, reset, stash, or discard it. A manual
   fallback requires Compli Me's operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR` from `.agents.local.env`; never guess a
   temporary or repository-adjacent path.
4. Run `git fetch origin`; confirm selected `HEAD == origin/main == ee27d849`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `3b1da7ea` is an ancestor of Poodle `origin/main`.
5. Read Compli Me root, Admin, Front, and UI `AGENTS.md`; its project-local
   Effigy skill and working rules; Poodle `g16.015`; and the governing refs.
   Run `effigy tasks`, `effigy health`, and `effigy test --plan` only after the
   worktree decision.

### While you work

- Keep the two app manifests and four package locks aligned to one registry
  Poodle 0.2.2 identity while preserving local Underlay.
- Inspect effective dependencies and lock churn before compatibility edits.
- Stop on Underlay/Poodle/template/public API decisions, duplicate or local
  Poodle after regeneration, unrelated lock churn, visible-runtime
  requirements, or a validation failure that changes the plan.
- Report meaningful chunks through the operator with changed files, actual
  validation, remaining work, and blockers.

### When the assigned runway is complete

1. Run the card's required final validation and `git diff --check`.
2. Record exact registry versions and integrities, retained Underlay links,
   four-lock review, compatibility edits, validation, actual worktree, and
   branch in the Compli Me PR.
3. Push the worker branch and open one PR against current Compli Me `main`.
4. Link Poodle `g16.015`, report the PR URL and evidence to the operator, and
   do not merge or edit Poodle planning state.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment instead of formal approval. Make only
requested changes. Merge requires separate operator authority.

### Handoff closeout

Leave the Compli Me PR evidence honest. If blocked, record the blocker and stop.
The Poodle orchestrator owns `g16.015`, generation currentness, and next-task
closeout after merge.
