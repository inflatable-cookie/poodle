---
title: Acowtancy Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-124835-g16-014-acowtancy-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, acowtancy]
---

## What This Thread Was Doing

Poodle 0.2.2 is published and the foundation plus first product wave are
complete. This worker moves Acowtancy's two web apps and shared Froyo UI package
from Poodle 0.1.x to the public release. It is one bounded repository lane; the
other consumer cards run separately.

## Why It Matters

Acowtancy proves the remaining Underlay-monorepo shape: two applications, one
shared UI library, and one root lock. Its result retires active 0.1.x use without
letting a sibling Poodle checkout masquerade as registry evidence.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/acowtancy`
- **Target base:** `a535969bab2a1b919d382e3e129d8eb95043b2b8`
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `3d8850d0271b08498b8ff3c5ce755b33fd1bff5d`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean, `HEAD == origin/main` before this handoff
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-014-acowtancy-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/README.md`
- **Ready card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/014-acowtancy-poodle-v022-adoption.md`
- **Allowed runway:** `g16.014` only
- **Remaining card budget:** one repository and one PR
- **Dispatch topology:** independent of `015`-`022`; no shared mutable files
- **Canonical refs:** Acowtancy root/scoped `AGENTS.md`, Acowtancy working
  rules, Froyo contract, Poodle `g16.001`, `g16.013`, and `g16.014`
- **Model capability profile:** bounded implementation worker; escalate any
  public API or dependency-identity ambiguity
- **Tool/runtime restrictions:** use Acowtancy's project-local Effigy skill and
  Effigy-owned runtime; no visible app/live-stack launch
- **Required validation:** `effigy workspace:js:prepare`,
  `effigy cream/validate`, `effigy dairy/validate`, `effigy froyo/check`,
  `effigy validate`, `effigy qa`, and `git diff --check`
- **PR base/head:** Acowtancy `main` <- worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review
- **Merge authorisation:** worker must not merge

## Boundaries

- Pin Cream and Dairy core/Svelte to exact registry `0.2.2`; move Froyo's
  Svelte peer to `^0.2.2`; regenerate the single root lock narrowly.
- Keep Underlay, internal workspace packages, Farmyard, APIs, and product
  behavior unchanged. Repair only Acowtancy-owned compatibility failures caused
  by the Poodle bump.
- Do not add sibling Poodle overrides, compatibility aliases, package-local
  locks, unrelated upgrades, or application-specific validation exceptions.
- Work only in the clean launcher-provided Acowtancy worktree. Do not edit the
  Poodle planning checkout or another consumer lane. Do not merge.

## Important Context

- Cream and Dairy currently declare core/Svelte `^0.1.0`. Froyo declares a
  Svelte `^0.1.0` peer. The root Bun lock is the only JS lock.
- Underlay Reference `g16.013` proved the canonical Underlay migration shape.
  Its review rejected `passWithNoTests` exceptions; reproduce and report a
  baseline rather than weakening test policy.
- Acowtancy treats its live workspace as Effigy/container-owned. A raw host
  install is not evidence that the supported runtime changed.
- Report after registry/peer/lock convergence, then after the full validation
  board. Stop if either phase changes the plan.

## Suggested Next Move

Read `g16.014`, then inventory Cream, Dairy, Froyo, and root `bun.lock` from the
selected worker worktree. Use the repo's Effigy-owned prepare path. Prove exact
registry source and one Svelte runtime before fixing any compatibility fallout.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Acowtancy worktree supplied by the launcher. Record its actual root/branch;
   do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean/reset/stash it or guess `/tmp`; manual
   fallback requires Acowtancy's operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR` from `.agents.local.env`.
4. Run `git fetch origin`; confirm target `HEAD == origin/main == a535969b`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `3d8850d0` is an ancestor of Poodle `origin/main`.
5. Read Acowtancy root, Cream, Dairy, and Froyo `AGENTS.md`; the project-local
   Effigy skill; `g16.014`; and the governing refs. Use `effigy tasks` for the
   current selector inventory.

### While you work

- Keep one root lock and use the supported Effigy runtime path.
- Inspect the effective dependency graph before compatibility edits.
- Stop on Underlay/Froyo/public API decisions, duplicate/local Poodle, unrelated
  lock churn, visible-runtime requirements, or a Poodle release defect.
- Report meaningful chunks through the operator with changed files, validation,
  remaining work, and blockers.

### When the assigned runway is complete

1. Run the card's required final validation and `git diff --check`.
2. Record exact registry versions/integrities, Froyo peer convergence, lock
   review, compatibility edits, validation, actual worktree, and branch in the
   Acowtancy PR.
3. Push the worker branch and open a PR against current Acowtancy `main`.
4. Link Poodle `g16.014`, report the PR URL and evidence to the operator, and do
   not merge or edit Poodle planning state.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment instead of formal approval. Make only
requested changes. Merge requires separate operator authority.

### Handoff closeout

Leave the Acowtancy PR evidence honest. If blocked, record the blocker and stop.
The Poodle orchestrator owns `g16.014`, generation currentness, and next-task
closeout after merge.
