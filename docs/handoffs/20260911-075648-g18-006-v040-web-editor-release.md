---
title: g18.006 v0.4.0 web editor release candidate
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle Northstar orchestrator
created: 2026-09-11
updated: 2026-09-12
base_required: pushed-main
queue_dispatch: northstar-queue
queue_approval: "Tom explicitly authorized the Poodle 0.4.0 candidate and public release on 2026-09-11 to unblock retained Desktop g02.058 and PR #215."
queue:
  capability: complex
  skipPRReview: false
  notifyOriginOnCloseout: true
tags: [coordination, handoff, worker, g18, g18.006, release, editor, desktop]
---

## What This Thread Was Doing

Execute [`g18.006`](../roadmaps/g18/006-v040-web-editor-release-and-desktop-unblock.md)
from the exact pushed planning commit. Prepare, review and merge the immutable
Poodle `0.4.0` candidate. Do not release it; g18.009 owns certification,
tagging, publication and the Desktop return after this task closes.

## Why It Matters

Desktop already proved its shell editor workspace against linked Poodle source.
It must not merge while the committed registry pin is `0.3.0`, whose Svelte
package lacks `./editor`. This task converts the accepted final web source into
the immutable release that lets Desktop unlink, repin and finish review.

## Current State

The retained Queue task, workspace and branch remain preserved with no
candidate mutation. g18.029 and g18.030 are complete. The latest probe found
the remaining hard-coded census version and wrong auto-detected release source;
g18.031 is the current structural precursor. g18.030 merged as PR #262
at `80609ff3528bc32fc44cd6f1a8dc5dd1ddb68b85`. Its accepted implementation
derives the current preview-lock hash and `0.3.0` resolution from the lockfile,
so the release-only candidate can proceed without a runtime-source edit. npm
latest for public core and Svelte remains `0.3.0`. Tom granted the explicit
release go on 2026-09-11.

## Boundaries

Follow g18.006 exactly. Public npm scope remains core and Svelte only. Keep
React private packed validation and Rust crates source/tag distributed. Do not
edit workflows, create a tag, dispatch a release workflow, publish anything,
mutate Desktop, bypass a gate, or change `0.4.0` without escalation. The worker
never merges its candidate PR.

## Important Context

The g18.002 tree hashes are lineage evidence, not the final package hashes;
rich text, Tabs and preview work landed later. Record final candidate, merge and
tag identities plus package trees and tarball integrity. Desktop task
`dbea8cc2-1872-40f3-b6d8-ba91b9d37618` and PR #215 must be resumed, not
recreated, only after public registry proof succeeds.

g18.029 owns candidate-scope policy. After it merges, use that closed admission
for the local gate and ordinary PR checks. Do not edit the guard in this lane.
The hosted branch dry run now belongs to post-merge g18.009.

g18.030 owns derived Nucleus lock provenance and its one current evidence
repin. It has merged; the final `0.4.0` lock change must flow into receipts
without editing emitter source.

g18.031 owns truthful root release metadata, exact root lockstep candidate
admission and manifest-derived GPUI census receipt versions. After it merges,
bump root with the other `0.4.0` inputs and regenerate final census evidence
without editing its generator. Root remains private and unpublished.

## Suggested Next Move

After g18.031 closes, rebase onto its accepted main, inventory the full
`v0.3.0..HEAD` public delta, and prepare the lockstep `0.4.0` candidate
manifests, locks, release notes, package evidence and final Nucleus repin. Use
focused selectors while building; run the full local release gate once after
the candidate is stable.

## Completion Protocol

Open one non-draft candidate PR after local source-free packed and release-gate
proof. `effigy release gates --json` runs once on the stable candidate;
do not follow it with redundant local `qa`, `ci:web`, or `docs:check` runs.
Report `ready_for_review` immediately after the clean PR head is pushed; do not
wait or poll for GitHub checks. Queue coordination owns asynchronous CI
observation and exact-head verification. Report the exact head, version set, package trees,
archive hashes, exact-head PR evidence and Desktop unblock oracle. Stop on any
red gate or identity mismatch. After queue-owned merge and closeout, g18.009
receives the exact candidate identity and owns the hosted branch dry run. No
tag, publication or Desktop resume occurs in this task.
