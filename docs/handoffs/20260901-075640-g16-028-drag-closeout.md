---
title: g16.028 drag migration and certification closeout worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle core
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260901-075640-g16-028-drag-closeout.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The dependable drag-and-drop runway is merged through `g16.027`. Northstar
reconciled the landed inventory and promoted `g16.028` as the final ordered
migration and certification card.

This dispatches one bounded implementation lane. No transcript or second
prompt is part of the authority chain.

## Why It Matters

Three web composites still bypass the common substrate, and three native
composites still show reorder affordances without their contract result path.
Closing those gaps leaves one semantic lifecycle, one honest capability
boundary, and an executable inventory instead of seven drifting drag designs.

## Current State

- **Repository:** `/Users/tom/Dev/projects/poodle`
- **Planning branch:** `main`
- **Planning base commit:** `894996f31292e1023cb790c313dcc27333687458`
- **Pushed main verification:** planning base matched `origin/main` before this
  handoff commit
- **Planning checkout:** clean at dispatch preparation
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** ready `g16.028`, reconciled
  spec 069, continuation register, milestone, generation index, and roadmap
  front door
- **Worker branch:** `codex/g16-028-drag-closeout`
- **Worker worktree:** Paseo-managed worktree created from pushed `origin/main`
- **Worktree creation command:** Paseo `create_workspace`, worktree
  `branch-off`, base `origin/main`, branch `codex/g16-028-drag-closeout`
- **Worker worktree policy:** launcher worktree first; named/manual fallback
  only under the completion protocol
- **Required sibling worktree links:** none
- **Active spec lane:** `docs/specs/069-dependable-drag-and-drop-substrate.md`
- **Roadmap milestone:** `docs/roadmaps/g16/README.md`
- **Ready cards, in order:**
  `docs/roadmaps/g16/028-drag-drop-migration-and-certification-closeout.md`
- **Allowed runway:** `g16.028` only
- **Remaining card budget:** one card
- **Dispatch topology:** serial final drag lane; `g16.033` remains reserved
- **Parallel safety check:** no second worker may edit shared drag machinery,
  the seven programme component surfaces, certification evidence, or ledger
- **Canonical refs:** `docs/architecture/011-drag-and-drop-substrate.md`,
  `docs/specs/069-dependable-drag-and-drop-substrate.md`,
  `docs/contracts/001-working-rules.md`, the seven programme component
  contracts, and `docs/roadmaps/g16/component-continuation-register.md`
- **Review oracle:** `g16.028` Review Oracle
- **Model capability profile:** frontier worker with high reasoning, selected
  from current Paseo profile notes at launch; this lane crosses public APIs,
  lifecycle exactness, renderer parity, and negative certification claims
- **Tool/runtime restrictions:** Effigy-routed validation only; never run
  windowed/native-visual, Jetstream, release, publication, workflow, or sibling
  mutation commands
- **Required validation:** focused component/substrate tests and paired vectors;
  `effigy test:drag-drop-browser`; `effigy regressions:native`;
  `effigy probe:gpui-specimens`; inventory and ledger checks; `effigy ci:web`;
  `effigy ci:rust`; `effigy ci:native`; `effigy docs:check`; one final
  headless `effigy qa`; `git diff --check origin/main...HEAD`
- **PR base/head:** `main` / `codex/g16-028-drag-closeout`
- **PR URL:** pending
- **Review state:** awaiting worker implementation and PR
- **Merge path:** orchestrator after accepted review of the exact current head
  and passing required checks

## Boundaries

- **In scope:** the ordered work and writable scope in `g16.028`: contracts;
  Svelte/React migrations for ModelCatalogueEditor, OrderBy, and BlockEditor;
  renderer-neutral Rust/native completion for EditableList, OrderBy, and
  BlockEditor; exact mounted evidence, inventory, ledger movement, programme
  certification, and closeout docs
- **Out of scope:** continuous-value gestures, general motion, broad
  accessibility or visual programmes, Jetstream admission, application
  window/file policy, package versions, release/publication, workflows,
  downstream repositories, siblings, HistoryCenter, CS20, and keyboard
  geometry
- **Outcome shape:** complete contract-valid implementation, falsified review
  oracle, evidence, closeout, and reviewable PR. Diagnosis alone is not a
  result.
- Do not invent architecture, add compatibility aliases, or choose a new
  component semantic. An additive renderer-neutral handler must preserve the
  already-documented complete reorder result.
- Stop if one complete native result requires a new public component semantic,
  a breaking callback, or another authority.
- Work only in the selected worker worktree. Never edit the planning checkout.
- Do not merge. Review and merge belong to the orchestrator.

## Important Context

- **Planning lineage:** `g16.021`–`g16.027` are merged. The common semantic
  machines, web custom-surface API, simple and nested migrations, Rust/GPUI
  controller, cross-window bridge, inbound files, and drag-out are landed.
- **Why this card is ready:** the inventory now names the exact residuals; all
  public result semantics are already contracted; the card fixes ordered work,
  validation, stop conditions, and seven adversarial oracle rows.
- **Decisions:** each component joins an ambient provider when present and
  owns an isolated controller otherwise; colliding item ids across instances
  never cross-drop; every accepted drop emits one complete next order; live
  state is revalidated at commit; dead native grips are removed or wired.
- **Evidence boundary:** GPUI 0.2.2 does not gain pen, touch, or device-originated
  cancel from mouse synthesis. An OS destination consuming a file remains
  manual downstream evidence. No ledger cell moves without a named mounted
  regression.
- **Known execution friction:** `effigy doctor` reports the recorded broad
  generated-source, god-file, and stale-suppression baseline. Fresh worktrees
  need `bun install`. Avoid crate-root Rust formatting and broad rustfmt churn.
- **Report after:** the three contract/web migrations; native completion; final
  certification, or immediately on a stop condition
- **Report to:** the operator through Paseo

## Suggested Next Move

Run the completion-protocol preflight before broad reads. Then read the card,
spec 069 migration/certification sections, all seven component contracts, and
the existing ModelCatalogueEditor Rust registration as the reference. Start
with the contract reconciliation and a source inventory that can fail when one
old path is planted back.

## Completion Protocol

### Before you start

1. The handoff metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Accept a clean registered non-`main` launcher worktree regardless of its
   generated path or branch spelling. Do not create another worktree.
3. If the launcher context is dirty, `main`, unregistered, or unusable, report
   it. A manual fallback requires `.agents.local.env` and its operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR`; never guess a path or discard dirty state.
4. In the selected worktree, fetch `origin` with the bounded SSH command from
   the Northstar worker contract. Confirm `HEAD == origin/main`, the planning
   base above is an ancestor, and this handoff exists in `HEAD`. Compare the
   absolute file with `git show HEAD:docs/handoffs/20260901-075640-g16-028-drag-closeout.md`;
   the tracked blob is canonical.
5. Required sibling links are `none`; create none.
6. Read `AGENTS.md`, the milestone, card, spec, architecture, working rules,
   component contracts, continuation register, and root `PAPERCUTS.md`.
7. Run `effigy tasks` and narrow orientation checks. Treat the recorded doctor
   scan baseline as unrelated unless this diff worsens it.

### While you work

- Execute the six ordered work items in coherent chunks.
- Update contracts before observable implementation. Preserve complete result
  payloads and existing controls, focus, disabled/pending, editing, selection,
  announcements, and specimens.
- Use one semantic lifecycle in both languages. Revalidate identity and target
  state at commit. Scope registrations independently from consumer item ids.
- Report each meaningful chunk through Paseo with changed files, checks,
  remaining work, risks, and blockers.
- Stop on a missing contract, new semantic choice, breaking API, scope
  expansion, authority failure, or validation result that changes the plan.

### When the assigned runway is complete

1. Run the required validation above.
2. Falsify every review-oracle row. Plant the smallest old behavior, confirm
   the named proof fails, restore the implementation, and record the result.
3. Reconcile card, one September closeout log, spec 069, continuation register,
   milestone/front doors, inventory, ledger, and next-task state. Keep
   `g16.033` reserved; do not dispatch it from this worker.
4. Push the worker branch and open a reviewable PR against current `main`.
5. Link the card, spec, changed surfaces, evidence map, ledger changes,
   validation, falsification results, and limits in the PR body.
6. Report the PR URL and exact head through Paseo. Do not merge.

### Review and merge path

The orchestrator reviews the exact PR head against the canonical refs, diff,
checks, and each oracle proof. With a shared GitHub identity it posts the
verdict as a PR comment. Requested changes stay on this branch and return to
this worker. Blocking findings use `execution-miss`, `oracle-gap`,
`planning-change`, `validation-gap`, or `integration-drift`. A planning change
returns to the planning checkout before revision.

When the reviewed head remains current, required checks pass, the PR is
mergeable into `main`, and no stricter rule or explicit operator pause applies,
the orchestrator merges without another approval prompt.

- **Requested changes:** none
- **Closeout refs:** card 028, one `docs/logs/2026-09/` execution log, spec 069,
  continuation register, g16 milestone, parity ledger, and roadmap front doors

### Handoff closeout

Leave the card, log, spec, milestone, inventory, ledger, and next-task state
honest. If blocked, record the blocker and stop.
