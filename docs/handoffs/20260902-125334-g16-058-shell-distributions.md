---
title: g16.058 shell distributions worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
review_authority: orchestrator
merge_authority: orchestrator
handoff: single-file-path-only
status: active-repair
owner: Poodle Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260902-125334-g16-058-shell-distributions.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, web-distribution]
---

## Assignment

Implement only `g16.058`: source-free Svelte browser/server distributions and
the matching private React distribution on the merged `g16.057` build
substrate. Preserve the frozen architecture 014 and spec 070 contract. Leave
permanent installed certification to `g16.059` and all release work to later
authority.

## Current State

- Repository: `/Users/tom/Dev/projects/poodle`
- Required base: pushed `main` containing PR #160 merge
  `8377a936ef8bb55e30e44da55fe728e2e5ced429` and PR #161 merge
  `0af7e7fa5bb9e01f04249597690247218e7c8781`, plus this active-repair
  addendum
- Assigned card: `docs/roadmaps/g16/058-shell-distributions.md`
- Canonical refs: `docs/architecture/014-compiled-web-package-distribution.md`,
  `docs/specs/070-compiled-web-distribution-contract.md`,
  `docs/roadmaps/g16/057-core-build-substrate.md`, and package manifests
- Worker class: day-to-day implementation. Contract, inventories, conditions,
  and stop rules are frozen; no frontier-model justification applies.
- Worker branch: `feature/g16-058-shell-distributions`
- Workspace: Paseo-managed worktree `wks_5af6c5db72647e59`, attached explicitly
  to agent `b2d71432-db63-4967-a630-24810b78730d`
- Integration ownership: orchestrator owns global roadmap/front-door status,
  exact-head review, merge, and dispatch of `g16.059`

## Boundary

In scope:

- complete every ordered-work and acceptance row in `g16.058`;
- compile Svelte `*.client.js` browser and `*.server.js` SSR lanes from one
  sorted entry inventory;
- emit source-free Svelte declarations and exact root/direct/markdown/`./types`
  exports without raw `.svelte` fallbacks;
- pin TypeScript `6.0.3` for distribution declaration generation and use real
  `@sveltejs/package` output; generic `Component` declaration shims fail;
- move AgentMessage, AgentPlan, AgentPlanRecord, AgentTranscript, and
  MarkdownEditor out of both root barrels and into `./markdown`, keeping their
  direct subpaths and synchronous browser/SSR rendering; no lazy parser import;
- compile the private React package to source-free JavaScript/declarations/CSS
  while keeping it unpublished;
- apply the exact peer, optional `marked`, CSS, files, and side-effects laws;
- run and remove the disposable no-workspace browser/SSR smoke;
- update only the assigned card, one execution log, new papercuts, and the
  existing package-install roster, compiled-path, Svelte-floor, CSS-execution,
  and markdown-import retargeting required for the 171-root/five-markdown
  split. Retarget existing runtime proofs; do not delete them.

Out of scope:

- `g16.059`, additions to permanent installed certification behavior, or edits
  to its receipt; only the named existing-gate retargeting is allowed;
- versions, changelog, release notes, workflows, tags, publishing, registries,
  sibling repositories, public React admission, compatibility shims, aliases,
  source fallbacks, Jetstream, or native work;
- global g16 README, generation index, continuation runway, or other front
  doors.

Stop if browser/SSR resolution is ambiguous, client output serves SSR, raw
source is required, declarations depend on workspace aliases/sibling source,
parser or CSS isolation drifts, React becomes publishable, or a permanent
certification/release surface must change.

## Required Proof

- Svelte 5.56.8 browser mount resolves client output; Node and worker-like SSR
  resolve the server default for root/direct Button/Select and markdown.
- Direct client output through `svelte/server` fails.
- Root/direct/markdown/`./types` runtime and declaration targets exist inside
  `dist`; archives contain no raw source or maps.
- Installed unsuppressed negative fixtures reject invalid public props,
  callbacks, snippets, and bindable assignments under Bundler and NodeNext.
- Ordinary root graphs are parser-free; markdown missing-peer behavior is
  explicit; focused CSS graphs contain no unrelated component styles.
- Private React value and declaration imports compile without source mapping.
- Falsify every card oracle from a committed clean proof point, restore from
  that commit, and record the failing proof.
- Run focused shell builds, Bundler/NodeNext declaration resolution, disposable
  installed browser/SSR smoke, relevant Effigy web selectors,
  `effigy docs:check`, and `git diff --check origin/main...HEAD`. Never run
  `*-windowed`.

## Completion Protocol

1. Before broad reads, confirm the launcher-provided root is a clean registered
   non-`main` worktree. Do not create a second worktree, reset, stash, or discard
   state.
2. Fetch origin; inspect and preserve the existing PR #162 state, then rebase
   it onto `origin/main`. Require the two merge commits above as ancestors and
   load this exact handoff from the rebased head. Stop on mismatch.
3. Preserve the coherent PR #162 batch, rebase it onto the promoted planning
   head, and repair it in place. Do not create another worktree or PR. Work in
   meaningful build/declaration/smoke batches. Keep `g16.059` and release
   surfaces untouched.
4. Remove disposable consumers and tarballs before handoff. Reconcile the card
   and one execution log with what actually passed.
5. Push one worker branch and open one PR against current `main`. If another
   lane merges first, rebase, revalidate, and report the new exact head.
6. Return PR URL, exact head, validation, falsification receipts, and limits.
   Do not merge or start `g16.059`.

## Next Move

Rebase PR #162 onto the promoted planning head. Move all five markdown-rendering
components together and remove the lazy-import regression. Keep the real
TypeScript `6.0.3` `@sveltejs/package` declarations, bound the existing-gate
retargeting, give compiler subprocess oracles an explicit realistic timeout,
and return `test:web-pack-install` plus `ci:web` green on the new exact head.
