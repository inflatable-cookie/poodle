---
title: Songsprout Underlay 0.9.2 and Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-144446-g16-018-songsprout-v092-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, songsprout]
---

## What This Thread Was Doing

Poodle 0.2.2 and Underlay tag `v0.9.2` are published. This worker moves
Songsprout's active web and Rust dependency graph from sibling sources to those
releases. Bloom and Greenhouse also move from Poodle 0.1.x and committed local
overrides to exact public Poodle 0.2.2.

This is one bounded dependency-maintenance lane authorised by the operator. It
must leave Songsprout's Trellis-owned product-planning runway unchanged.

## Why It Matters

Songsprout exercises the coupled release through two Svelte applications, a
typed client, and a 24-crate Underlay dependency surface in Nursery. A green
result must prove the installed tag and registry packages—not sibling source
trees reached through public-config generator imports or local overrides.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/songsprout`
- **Target base:** `b031c1a3c0dc197b1b082b706a4a134d7e5174ad`; verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `768e50d8970dbcdfdc429124380f1f1be9275fca`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean, on `main`, with `HEAD == origin/main` before
  this handoff
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-018-songsprout-v092-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Worktree creation command:** none in the planning checkout; the launcher
  owns creation. Do not invent a temporary or repository-adjacent fallback.
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/README.md`
- **Ready card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/018-songsprout-poodle-v022-adoption.md`
- **Allowed runway:** `g16.018` only
- **Remaining card budget:** one repository and one PR
- **Dispatch topology:** independent of other consumer repositories; it owns no
  Compli Me, Composer, Contact Patch, or Underlay Reference files
- **Parallel safety:** Songsprout has no open PR or additional registered
  worktree at dispatch. Stop if another worker appears in overlapping manifests
  or locks before implementation starts.
- **Canonical refs:** Poodle `g16.001`, `g16.009`, `g16.013`, and `g16.018`;
  Songsprout root and package `AGENTS.md`, Trellis
  `contracts/001-working-rules.md`, active roadmap currentness, and its local
  Effigy skill/task surface
- **Songsprout posture:** Atlas discovery is active in Trellis `g02.003`. This
  maintenance card does not answer its discovery questions, promote a product
  runway, or change roadmap currentness.
- **Known web graph:** Bloom and Greenhouse each declare Poodle core/Svelte
  0.1.x plus local Poodle overrides and sibling Underlay. Stem declares sibling
  Underlay. Three Bun locks record those sources.
- **Known source bypasses:** Bloom and Greenhouse public-config generators
  import `../../../underlay/ts/src/server/config-stack.ts` directly. Root docs
  and Vite comments still describe application dependencies as local or
  symlinked; explicit cross-repository QA mounts and scripts are separate.
- **Known Rust graph:** `nursery/Cargo.toml` declares 24 sibling-path Underlay
  workspace dependencies with feature sets that must be preserved. One Cargo
  lock records the graph.
- **Managed dependency state:** `effigy --json deps status` reported no active
  local links at dispatch
- **Model capability profile:** bounded dependency-adoption worker; pause on
  public API, Underlay, Trellis, deployment, or dependency-policy decisions
- **Tool/runtime restrictions:** use Songsprout's repo-local Effigy skill and
  Effigy-owned host/container routing. Do not launch the visible app stack or
  treat unrelated host artifacts as live-runtime evidence.
- **Required validation:** `effigy stem/validate`, `effigy bloom/validate`,
  `effigy greenhouse/validate`, `effigy nursery/validate`, `effigy validate`,
  `effigy qa`, and `git diff --check`; record any reproduced pre-existing
  baseline separately
- **PR base/head:** Songsprout `main` <- worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review
- **Merge authorisation:** worker must not merge

## Boundaries

- Pin Bloom and Greenhouse's existing direct
  `@inflatable-cookie/poodle-core` and
  `@inflatable-cookie/poodle-svelte` dependencies to exact `0.2.2`. Remove both
  committed override blocks and every active sibling Poodle resolution.
- Move Bloom, Greenhouse, and Stem Underlay dependencies to
  `git+ssh://git@github.com/inflatable-cookie/underlay.git#v0.9.2`.
- Move both public-config generators to the exported
  `@inflatable-cookie/underlay/server/config-stack` package subpath.
- Move all 24 active Nursery Underlay workspace dependencies to
  `ssh://git@github.com/inflatable-cookie/underlay.git`, tag `v0.9.2`, preserving
  every existing feature set.
- Regenerate Bloom, Greenhouse, and Stem Bun locks plus Nursery's Cargo lock
  narrowly. The final graph must contain exact Underlay commit
  `ddba26400f480638829917cf72eecc62be4b978d` and registry Poodle 0.2.2 with no
  manifest, lock, generator import, or other application source bypass.
- Update Songsprout-owned dependency-resolution documentation and comments
  that describe sibling Underlay/Poodle packages as application dependencies.
  Sibling mounts and scripts used only by explicit workspace cross-repository
  QA may remain, but they must not affect application package resolution.
- Repair only Songsprout-owned compatibility failures caused by the upgrade.
- Do not edit Poodle, Underlay, Trellis contracts, Nursery/API behavior,
  application behavior, containers, deployment, or release automation. Do not
  add compatibility shims, aliases, app exceptions, or unrelated upgrades.
- Do not execute, close, or rewrite Songsprout's active Atlas discovery card or
  roadmap currentness. The operator authorised this maintenance lane separately.
- Work only in the clean launcher-provided Songsprout worktree. Do not edit the
  Poodle planning checkout, the operator's Songsprout checkout, or another
  consumer lane. Do not merge.

## Important Context

- Underlay `v0.9.2` resolves to
  `ddba26400f480638829917cf72eecc62be4b978d` and directly pins
  `@inflatable-cookie/poodle-svelte` 0.2.2. Its root JavaScript package metadata
  still says 0.9.1; treat the Git tag and commit as source authority and do not
  repair Underlay from this repository.
- `@inflatable-cookie/underlay/server/config-stack` is exported by the tagged
  package, so neither config generator needs a relative source import.
- Songsprout's root Effigy tasks intentionally retain sibling Underlay
  conformance/template checks and the dev system mounts sibling Underlay and
  Poodle. Those tooling relationships are not permission to resolve application
  dependencies from sibling checkouts.
- Bloom and Greenhouse import Stem through their existing workspace file
  dependency and source alias. Preserve that Songsprout-owned relationship; the
  migration concerns Stem's Underlay source, not Stem's ownership.
- Songsprout's active product-planning state is discovery, not implementation.
  Do not turn this dependency maintenance PR into a Trellis planning update.
- Report after manifest/source/lock convergence, then after the validation
  board. Stop if either phase changes the plan.

## Suggested Next Move

Read `g16.018`, then inventory every active Underlay/Poodle manifest, lock, and
public-config generator import from the selected Songsprout worktree. Make the
source migration as one coherent batch, prove one tagged Underlay revision and
one registry Poodle 0.2.2 identity, and only then address compatibility fallout.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Songsprout worktree supplied by the launcher. Record its actual root and
   branch; do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean, reset, stash, or discard it. Do not invent a
   manual worktree container path.
4. Run `git fetch origin`; confirm selected
   `HEAD == origin/main == b031c1a3c0dc197b1b082b706a4a134d7e5174ad`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `768e50d8970dbcdfdc429124380f1f1be9275fca` is an ancestor of Poodle
   `origin/main`.
5. Read Songsprout root, Bloom, Greenhouse, Stem, and Nursery `AGENTS.md`; its
   repo-local Effigy skill; Trellis working rules and active roadmap
   currentness; Poodle `g16.018`; and the governing refs. Run `effigy tasks`,
   `effigy health`, and `effigy --json deps status` only after the worktree
   decision.
6. Recheck Songsprout's registered worktrees and open PRs. Stop if another
   active lane now owns any manifest or lock in this card.

### While you work

- Keep all web/Rust manifests, locks, imports, docs, and comments aligned to
  Underlay `v0.9.2` and one registry Poodle 0.2.2 identity.
- Inspect effective dependencies and lock churn before compatibility edits.
- Stop on Underlay/Poodle/Trellis/public API decisions, duplicate or local
  Poodle after regeneration, unrelated lock churn, visible-runtime
  requirements, or validation that changes the plan.
- Report meaningful chunks through the operator with changed files, actual
  validation, remaining work, and blockers.

### When the assigned runway is complete

1. Run the card's required final validation and `git diff --check`.
2. Record the exact Underlay tag/commit, registry Poodle versions/integrities,
   three Bun lock reviews, Cargo lock review, removed source bypasses,
   compatibility edits, validation, actual worktree, and branch in the
   Songsprout PR.
3. Push the worker branch and open one PR against current Songsprout `main`.
4. Link Poodle `g16.018`, report the PR URL and evidence to the operator, and
   do not merge or edit Poodle planning state.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment instead of formal approval. Make only
requested changes. Merge requires separate operator authority.

### Handoff closeout

Leave the Songsprout PR evidence honest. If blocked, record the blocker and
stop. The Poodle orchestrator owns `g16.018`, generation currentness, and
next-task closeout after merge.
