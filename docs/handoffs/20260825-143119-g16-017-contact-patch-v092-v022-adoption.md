---
title: Contact Patch Underlay 0.9.2 and Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-143119-g16-017-contact-patch-v092-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, contact-patch]
---

## What This Thread Was Doing

Poodle 0.2.2 and Underlay tag `v0.9.2` are published. This worker moves Contact
Patch's active web and Rust dependency graph from sibling sources to those
releases. Admin and Front also move from Poodle 0.1.x and committed local
overrides to exact public Poodle 0.2.2.

This is one bounded dependency-maintenance lane authorised by the operator. It
must leave Contact Patch's active Book product runway unchanged.

## Why It Matters

Contact Patch exercises the coupled release through two Svelte applications, a
shared UI package, a typed client, and a substantial Rust API graph. A green
result must prove the installed tag and registry packages—not a sibling source
tree reached through a TypeScript path map or config-generator deep import.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/contact-patch`
- **Target base:** `a6d2316f5c5248c1d27f7f595bfbf2b0de91127e`; verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `f9d4a3d5455885e4b69b22e1cfae0dd80821a36a`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean, on `main`, with `HEAD == origin/main` before
  this handoff
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-017-contact-patch-v092-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Worktree creation command:** none in the planning checkout; the launcher
  owns creation. Manual fallback is allowed only through Contact Patch's
  operator-selected `AGENTS_WORKTREE_CONTAINER_DIR`.
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g15/README.md`
- **Ready card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g15/071-contact-patch-poodle-v022-adoption.md`
- **Allowed runway:** `g15.071` only
- **Remaining card budget:** one repository and one PR
- **Dispatch topology:** independent of other consumer repositories; it owns no
  Composer, Compli Me, Songsprout, or Underlay Reference files
- **Parallel safety:** Contact Patch has no open PR or additional registered
  worktree at dispatch. Stop if another worker appears in overlapping manifests
  or locks before implementation starts.
- **Canonical refs:** Poodle `g15.055`, `g15.063`, `g15.067`, and `g15.071`;
  Contact Patch root and package `AGENTS.md`,
  `cp-docs/policy/001-working-rules.md`, and its local Effigy skill/task surface
- **Contact Patch posture:** strict-ready on the separate
  `g01.007` Book product lane. This maintenance card does not execute or close
  `cp-docs/specs/batch-cards/012-compile-cache-operations.md`.
- **Known web graph:** Admin declares Poodle core/Svelte 0.1.x; Front declares
  Poodle core 0.1.x. Both carry local Poodle overrides. Admin, Front, UI, and
  client declare sibling Underlay packages across four Bun locks.
- **Known source bypasses:** `cp-client/tsconfig.json` maps every Underlay
  subpath to sibling source, and `scripts/generate-public-api-config.ts`
  deep-imports the sibling config-stack module.
- **Known Rust graph:** `cp-api/Cargo.toml` declares 24 sibling-path Underlay
  workspace dependencies; `cp-api/crates/infra/Cargo.toml` adds direct
  sibling-path `underlay-config`. One Cargo lock records the graph.
- **Model capability profile:** bounded dependency-adoption worker; pause on
  public API, Underlay, template, deployment, or dependency-policy decisions
- **Tool/runtime restrictions:** use Contact Patch's repo-local Effigy skill and
  Effigy-owned host/container routing. Do not launch the visible app stack and
  do not treat unrelated host artifacts as live-runtime evidence.
- **Required validation:** `effigy cp-client/validate`,
  `effigy cp-admin/validate`, `effigy cp-front/validate`,
  `effigy cp-ui/validate`, `effigy cp-api/validate`, `effigy validate`,
  `effigy qa`, and `git diff --check`; record any reproduced pre-existing
  baseline separately
- **PR base/head:** Contact Patch `main` <- worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review
- **Merge authorisation:** worker must not merge

## Boundaries

- Pin every existing direct Admin/Front `@inflatable-cookie/poodle-core` and
  `@inflatable-cookie/poodle-svelte` dependency to exact `0.2.2`. Do not add a
  direct Svelte dependency where none is used.
- Remove committed Poodle overrides and every active sibling Poodle resolution.
- Move Admin, Front, UI, and client Underlay dependencies to
  `git+ssh://git@github.com/inflatable-cookie/underlay.git#v0.9.2`.
- Remove `cp-client`'s TypeScript path mapping to sibling Underlay source. Move
  the shared public-config generator to the exported
  `@inflatable-cookie/underlay/server/config-stack` package subpath.
- Move all active Rust Underlay dependencies to
  `ssh://git@github.com/inflatable-cookie/underlay.git`, tag `v0.9.2`, preserving
  every existing feature set. Include the direct `underlay-config` declaration
  in `cp-api/crates/infra/Cargo.toml`.
- Regenerate all four Bun locks and the API Cargo lock narrowly. The final graph
  must contain exact Underlay commit
  `ddba26400f480638829917cf72eecc62be4b978d` and registry Poodle 0.2.2 with no
  manifest, lock, TypeScript path, generator import, or other source bypass.
- Update Contact Patch-owned dependency-resolution documentation that describes
  sibling Underlay/Poodle packages as application dependencies. Sibling mounts
  and scripts used only by explicit workspace cross-repository QA may remain,
  but they must not affect application package resolution.
- Repair only Contact Patch-owned compatibility failures caused by the upgrade.
- Do not edit Poodle, Underlay, Book APIs, UI templates, product behavior,
  containers, deployment, or release automation. Do not add compatibility
  shims, aliases, app exceptions, or unrelated upgrades.
- Do not execute, close, or rewrite Contact Patch's current Book product card or
  roadmap currentness. The operator authorised this maintenance lane separately.
- Work only in the clean launcher-provided Contact Patch worktree. Do not edit
  the Poodle planning checkout, the operator's Contact Patch checkout, or
  another consumer lane. Do not merge.

## Important Context

- Underlay `v0.9.2` resolves to
  `ddba26400f480638829917cf72eecc62be4b978d` and directly pins
  `@inflatable-cookie/poodle-svelte` 0.2.2. Its root JavaScript package metadata
  still says 0.9.1; treat the Git tag and commit as source authority and do not
  repair Underlay from this repository.
- `@inflatable-cookie/underlay/server/config-stack` is exported by the tagged
  package, so the shared config generator does not need a relative source import.
- Contact Patch's root tasks retain explicit sibling Underlay conformance and
  template checks. That tooling relationship is not permission to resolve
  application dependencies from the sibling checkout.
- The repo's managed local-dependency status was empty at dispatch: no Effigy
  dependency link is currently registered.
- `effigy doctor` completed without findings during dispatch orientation.
- Contact Patch's Book product lane is active but has no open PR or worker
  worktree at dispatch. If that changes and the dependency files overlap, stop
  rather than racing locks or manifests.
- Report after manifest/source/lock convergence, then after the validation
  board. Stop if either phase changes the plan.

## Suggested Next Move

Read `g15.071`, then inventory every active Underlay/Poodle manifest, lock,
TypeScript path mapping, and generator import from the selected Contact Patch
worktree. Make the source migration as one coherent batch, prove one tagged
Underlay revision and one registry Poodle 0.2.2 identity, and only then address
compatibility fallout.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Contact Patch worktree supplied by the launcher. Record its actual root and
   branch; do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean, reset, stash, or discard it. A manual
   fallback requires Contact Patch's operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR` from `.agents.local.env`; never guess a
   temporary or repository-adjacent path.
4. Run `git fetch origin`; confirm selected
   `HEAD == origin/main == a6d2316f5c5248c1d27f7f595bfbf2b0de91127e`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `f9d4a3d5455885e4b69b22e1cfae0dd80821a36a` is an ancestor of Poodle
   `origin/main`.
5. Read Contact Patch root, Admin, Front, UI, client, and API `AGENTS.md`; its
   repo-local Effigy skill and working rules; Poodle `g15.071`; and the
   governing refs. Run `effigy tasks`, `effigy health`, and
   `effigy --json deps status` only after the worktree decision.
6. Recheck Contact Patch's registered worktrees and open PRs. Stop if another
   active lane now owns any manifest or lock in this card.

### While you work

- Keep all web/Rust manifests, locks, mappings, and imports aligned to Underlay
  `v0.9.2` and one registry Poodle 0.2.2 identity.
- Inspect effective dependencies and lock churn before compatibility edits.
- Stop on Underlay/Poodle/template/public API decisions, duplicate or local
  Poodle after regeneration, unrelated lock churn, visible-runtime
  requirements, or validation that changes the plan.
- Report meaningful chunks through the operator with changed files, actual
  validation, remaining work, and blockers.

### When the assigned runway is complete

1. Run the card's required final validation and `git diff --check`.
2. Record the exact Underlay tag/commit, registry Poodle versions/integrities,
   Bun/Cargo lock review, removed source bypasses, compatibility edits,
   validation, actual worktree, and branch in the Contact Patch PR.
3. Push the worker branch and open one PR against current Contact Patch `main`.
4. Link Poodle `g15.071`, report the PR URL and evidence to the operator, and
   do not merge or edit Poodle planning state.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment instead of formal approval. Make only
requested changes. Merge requires separate operator authority.

### Handoff closeout

Leave the Contact Patch PR evidence honest. If blocked, record the blocker and
stop. The Poodle orchestrator owns `g15.071`, generation currentness, and
next-task closeout after merge.
