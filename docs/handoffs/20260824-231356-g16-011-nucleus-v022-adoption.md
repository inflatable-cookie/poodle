---
title: Nucleus Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-24
updated: 2026-08-24
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260824-231356-g16-011-nucleus-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, nucleus]
---

## What This Thread Was Doing

Poodle 0.2.2 and its Longhorn foundation adoption are complete. This worker
moves Nucleus Desktop from Poodle 0.1.0 to the exact public 0.2.2 packages and
proves the existing local Longhorn integration resolves that same Poodle
identity. This handoff is complete on its own; no transcript is required.

## Why It Matters

Nucleus is a current Longhorn-backed product and a clean direct-app test of the
published release. Its result establishes the migration shape for other apps
that consume Poodle directly while developing against local Longhorn packages.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/nucleus`
- **Target base:** `91316dbe3068e4cb6cebd4cf6a2c14f55a7a4601`
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `a7293ecd43bb6715afea8c061c27223d39b0b1aa`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean and synchronized before this planning batch
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-011-nucleus-poodle-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Roadmap card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/011-nucleus-poodle-v022-adoption.md`
- **Allowed runway:** that one card only
- **Parallel topology:** runs beside Soundcheck `012` and Underlay Reference
  `013`; no shared mutable repository files
- **Required validation:** card validation plus Nucleus's complete headless
  `effigy qa` board; no desktop launch or native proof
- **PR:** Nucleus `main` <- worker branch; pending
- **Review state:** awaiting orchestrator review; worker must not merge

## Boundaries

- Change only Nucleus-owned Poodle declarations, the desktop lock, and bounded
  compatibility fallout named by `g16.011`.
- Keep local Longhorn dependencies unchanged and prove their Poodle peer
  converges on the direct 0.2.2 dependency.
- Do not edit Poodle or Longhorn, change Nucleus product/server/Tauri contracts,
  add a shim, launch the desktop app, or run `desktop:proof`.
- Work only in the clean launcher-provided Nucleus worktree. Do not edit either
  main checkout. Do not merge the PR.

## Important Context

- Nucleus currently declares Poodle core/Svelte `^0.1.0` in
  `apps/desktop/package.json`; `apps/desktop/bun.lock` resolves 0.1.0.
- Its Longhorn packages remain intentional local `file:` dependencies. Longhorn
  PR 9 updated `longhorn-poodle-svelte`'s Poodle peer to exact 0.2.2.
- Poodle itself must resolve from npm, not a sibling checkout.
- Report after dependency/lock convergence and after final validation. Include
  exact versions/sources, changed files, checks run, remaining work, and any
  blocker.

## Suggested Next Move

Read `g16.011`, inventory the desktop manifest and lock from the selected
worktree, pin both direct Poodle packages to exact 0.2.2, and regenerate the
lock narrowly. Inspect the effective peer graph before making any compatibility
edit. Stop if it contains two Poodle versions or a sibling Poodle source.

## Completion Protocol

### Before you start

1. This file's worker metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Nucleus worktree supplied by the launcher. Record the actual root/branch and
   do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean it or invent `/tmp`; a manual fallback needs
   Nucleus's operator-selected `AGENTS_WORKTREE_CONTAINER_DIR`.
4. Run `git fetch origin`; confirm target `HEAD == origin/main == 91316dbe`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `a7293ecd` is an ancestor of Poodle `origin/main`.
5. Read Nucleus `AGENTS.md`, working rules, Longhorn-consumer boundary,
   `g16.011`, and its governing Poodle refs. Run `effigy tasks` as orientation.

### While you work

- Keep one bounded adoption commit or a small set of meaningful commits.
- Stop on contract/API scope, duplicate Poodle identity, unrelated lock churn,
  visible-run requirements, or a Poodle/Longhorn release defect.

### When the assigned runway is complete

1. Run `effigy check:longhorn-consumer`, `effigy desktop:check`,
   `effigy desktop:build`, `effigy desktop:test`, `effigy qa`, and
   `git diff --check`; inspect the complete lock diff.
2. Push the worker branch and open a reviewable PR against Nucleus `main`.
3. Link `g16.011`; record exact registry resolution, peer convergence,
   compatibility edits, lock review, validation, and unresolved items.
4. Report the PR URL and evidence to the operator. Do not merge or edit Poodle
   roadmap state; the orchestrator owns closeout.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment rather than formal approval. Make only
requested changes on the branch. Merge requires separate operator authority.
