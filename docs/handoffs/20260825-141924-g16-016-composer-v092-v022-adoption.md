---
title: Composer Underlay 0.9.2 and Poodle 0.2.2 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Poodle orchestrator
created: 2026-08-25
updated: 2026-08-25
handoff_path: /Users/tom/Dev/projects/poodle/docs/handoffs/20260825-141924-g16-016-composer-v092-v022-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g16, composer]
---

## What This Thread Was Doing

Poodle 0.2.2 and Underlay tag `v0.9.2` are published. This worker moves
Composer's complete active web and Rust dependency graph from sibling sources
to those releases. Admin and Front also move from Poodle 0.1.x and committed
local overrides to exact public Poodle 0.2.2.

This is a bounded dependency-maintenance lane authorised by the operator. It
does not reopen Composer's paused product roadmap or choose its next product
implementation owner.

## Why It Matters

Composer is a useful full-graph proof: two Svelte applications, a shared API
client, and a Rust API all depend on Underlay. A green result must exercise the
tagged Underlay package and registry Poodle packages, not silently load sibling
source through aliases, deep imports, or local-link helpers.

## Current State

- **Worker repository:** `/Users/tom/Dev/projects/composer`
- **Target base:** `29a32c17d4a5e6f75da311c09776db08f118244a`; verified as
  current `origin/main` on 2026-08-25
- **Poodle planning repository:** `/Users/tom/Dev/projects/poodle`
- **Planning base:** `feb71719ace4167f7e4d223550d2cdfe2e889197`, pushed to
  `origin/main` before this handoff commit
- **Planning checkout:** clean, on `main`, with `HEAD == origin/main` before
  this handoff
- **Worker mode:** activated by this orchestrator handoff
- **Worker branch:** launcher-generated non-`main` branch; suggested identity
  `t3code/g16-016-composer-v092-v022`
- **Worker worktree:** use the launcher-provided registered worktree; no manual
  worktree has been pre-created
- **Worktree creation command:** none in the planning checkout; the launcher
  owns creation. Manual fallback is allowed only through Composer's
  operator-selected `AGENTS_WORKTREE_CONTAINER_DIR`.
- **Roadmap milestone:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/README.md`
- **Ready card:**
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/016-composer-poodle-v022-adoption.md`
- **Allowed runway:** `g16.016` only
- **Remaining card budget:** one repository and one PR
- **Dispatch topology:** independent of the other consumer repositories; it
  owns no Acowtancy, Compli Me, Contact Patch, or Songsprout files
- **Parallel safety:** separate repository, manifests, locks, worktree, branch,
  and PR; stop if a Poodle or Underlay source change becomes necessary
- **Canonical refs:** Poodle `g16.001`, `g16.009`, `g16.013`, and `g16.016`;
  Composer root and package `AGENTS.md`,
  `composer-docs/contracts/001-working-rules.md`, and the local Effigy task
  surface
- **Known web graph:** Admin and Front declare Poodle 0.1.x and local Poodle
  overrides. Admin, Front, and API-client declare sibling Underlay packages.
  Admin also aliases Underlay to sibling source in SvelteKit and Vite, while
  both config generators deep-import the sibling checkout.
- **Known Rust graph:** Composer API declares 19 sibling-path Underlay workspace
  dependencies and `composer-api/crates/infra/Cargo.toml` declares one direct
  sibling-path `underlay-config` dependency.
- **Model capability profile:** bounded dependency-adoption worker; pause on
  public API, Underlay, template, deployment, or dependency-policy decisions
- **Tool/runtime restrictions:** use Composer's Effigy-owned host/container
  routing. Do not launch the visible app stack. Do not treat host artifacts as
  evidence for the live runtime.
- **Required validation:** `effigy composer-api-client/validate`,
  `effigy composer-admin/validate`, `effigy composer-front/validate`,
  `effigy composer-api/validate`, `effigy validate`, `effigy qa`, and
  `git diff --check`; record any reproduced pre-existing baseline separately
- **PR base/head:** Composer `main` <- worker branch
- **PR URL:** pending
- **Review state:** awaiting orchestrator review
- **Merge authorisation:** worker must not merge

## Boundaries

- Pin every existing direct Admin/Front `@inflatable-cookie/poodle-core` and
  `@inflatable-cookie/poodle-svelte` dependency to exact `0.2.2`. Do not add a
  direct Svelte dependency where none is used.
- Remove committed Poodle overrides and every active sibling Poodle resolution.
- Move Admin, Front, and API-client Underlay dependencies to
  `git+ssh://git@github.com/inflatable-cookie/underlay.git#v0.9.2`.
- Remove Admin's sibling Underlay SvelteKit/Vite aliases. Move both config
  generators to the exported
  `@inflatable-cookie/underlay/server/config-stack` subpath. Remove Front's
  calls to the absent `scripts/ensure-local-js-links.sh`; do not recreate a
  local-link mechanism.
- Move all active Rust Underlay dependencies to
  `ssh://git@github.com/inflatable-cookie/underlay.git`, tag `v0.9.2`, preserving
  every existing feature set. Include the direct `underlay-config` declaration
  in `composer-api/crates/infra/Cargo.toml`.
- Regenerate the three Bun locks and Composer API Cargo lock narrowly. The
  final graph must contain exact Underlay commit
  `ddba26400f480638829917cf72eecc62be4b978d` and registry Poodle 0.2.2 with no
  manifest, lock, alias, deep import, or helper bypassing those sources.
- Update Composer-owned dependency-resolution documentation that still claims
  the frontends load Underlay/Poodle source from sibling checkouts. Sibling
  mounts used only by explicit workspace cross-repository QA may remain, but
  they must not affect package resolution.
- Repair only Composer-owned compatibility failures caused by the upgrade.
- Do not edit Poodle, Underlay, product APIs, containers, templates, deployment,
  or product behavior. Do not add compatibility shims, app exceptions, new
  architecture, or unrelated upgrades.
- Do not advance or rewrite Composer's paused `g02` product-planning gate. The
  operator authorised this maintenance lane separately.
- Work only in the clean launcher-provided Composer worktree. Do not edit the
  Poodle planning checkout, the operator's Composer checkout, or another
  consumer lane. Do not merge.

## Important Context

- Underlay `v0.9.2` resolves to
  `ddba26400f480638829917cf72eecc62be4b978d` and directly pins
  `@inflatable-cookie/poodle-svelte` 0.2.2. Its root JavaScript package metadata
  still says 0.9.1; treat the Git tag and commit as source authority and do not
  "fix" Underlay from this repository.
- `@inflatable-cookie/underlay/server/config-stack` is exported by the tagged
  package. The existing relative file import is unnecessary and would defeat
  the release proof.
- Composer's root task surface has exact package selectors for API-client,
  Admin, Front, and API validation. Root `validate` also runs an explicit
  sibling Underlay validation step; that tooling relationship is not permission
  to let Composer resolve its application dependencies from the sibling tree.
- Composer's product posture remains strict-paused under `g02.001`. This
  operator-authorised maintenance upgrade must leave that currentness honest
  and untouched.
- `effigy doctor` completed without findings during dispatch orientation.
- Report after manifest/source/lock convergence, then after the validation
  board. Stop if either phase changes the plan.

## Suggested Next Move

Read `g16.016`, then inventory every active Underlay/Poodle manifest, lock,
alias, generator import, and task helper from the selected Composer worktree.
Make the source migration as one coherent batch, inspect the effective graph for
one tagged Underlay revision and one registry Poodle 0.2.2 identity, and only
then address compatibility fallout.

## Completion Protocol

### Before you start

1. This file's metadata activates worker mode. Before broad reads, run
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Use the current context only when it is a clean, registered, non-`main`
   Composer worktree supplied by the launcher. Record its actual root and
   branch; do not create another because its generated name differs.
3. If the launcher supplied `main`, a dirty checkout, or an unregistered path,
   stop and report it. Never clean, reset, stash, or discard it. A manual
   fallback requires Composer's operator-selected
   `AGENTS_WORKTREE_CONTAINER_DIR` from `.agents.local.env`; never guess a
   temporary or repository-adjacent path.
4. Run `git fetch origin`; confirm selected
   `HEAD == origin/main == 29a32c17d4a5e6f75da311c09776db08f118244a`.
   Confirm this handoff exists at the absolute Poodle path and planning base
   `feb71719ace4167f7e4d223550d2cdfe2e889197` is an ancestor of Poodle
   `origin/main`.
5. Read Composer root, Admin, Front, API-client, and API `AGENTS.md`; Composer's
   working rules; Poodle `g16.016`; and the governing refs. Run `effigy tasks`
   and `effigy health` only after the worktree decision.

### While you work

- Keep all web/Rust manifests, locks, aliases, imports, and task helpers aligned
  to Underlay `v0.9.2` and one registry Poodle 0.2.2 identity.
- Inspect effective dependencies and lock churn before compatibility edits.
- Stop on Underlay/Poodle/template/public API decisions, duplicate or local
  Poodle after regeneration, unrelated lock churn, visible-runtime
  requirements, or validation that changes the plan.
- Report meaningful chunks through the operator with changed files, actual
  validation, remaining work, and blockers.

### When the assigned runway is complete

1. Run the card's required final validation and `git diff --check`.
2. Record the exact Underlay tag/commit, registry Poodle versions/integrities,
   Bun/Cargo lock review, removed source bypasses, compatibility edits,
   validation, actual worktree, and branch in the Composer PR.
3. Push the worker branch and open one PR against current Composer `main`.
4. Link Poodle `g16.016`, report the PR URL and evidence to the operator, and
   do not merge or edit Poodle planning state.

### Review and merge path

The Poodle orchestrator will review the PR independently. Shared GitHub identity
may require a canonical PR comment instead of formal approval. Make only
requested changes. Merge requires separate operator authority.

### Handoff closeout

Leave the Composer PR evidence honest. If blocked, record the blocker and stop.
The Poodle orchestrator owns `g16.016`, generation currentness, and next-task
closeout after merge.
