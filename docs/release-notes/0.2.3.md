# 0.2.3 — 2026-08-30

`0.2.3` was prepared in the repository but never tagged or published. There
is no `v0.2.3` tag, npm `latest` remains `0.2.2`, and `0.2.4` is skipped. Its
intended delta is carried into the `0.3.0` candidate notes. It adds triggerless
`ContextMenu` composition so
a consumer that already owns context invocation can open a controlled overlay
without a tab-stop button. `menuTransition` still owns open, dismiss, action,
and first-item focus.

There are no product API changes in `@inflatable-cookie/poodle-core`. It
moves so the ecosystem version set stays aligned across one tag.

## What 0.2.3 adds

- `ContextMenu` `trigger` (default `true`). `trigger={false}` omits the
  `role="button" tabindex="0"` host. The consumer supplies controlled `open`
  and `anchorPoint`.
- Svelte is the reference; React matches. Tree specimens use the overlay
  composition.
- Contract: `docs/contracts/components/context-menu.md`.

Change class: additive.

## What publishes

| Package | Version | Disposition |
| --- | --- | --- |
| `@inflatable-cookie/poodle-core` | 0.2.3 | **prepared — not published** |
| `@inflatable-cookie/poodle-svelte` | 0.2.3 | **prepared — not published** |
| `@inflatable-cookie/poodle-react` | 0.2.3 | **experimental — packed and certified, not published** |

Rust crates remain source/tag distribution and carry `0.2.3` in lockstep.
They are not published to crates.io:

- contracts: `poodle-adapter`, `poodle-events`, `poodle-headless`, `poodle-ir`,
  `poodle-layout`, `poodle-markdown`, `poodle-node`, `poodle-specs`,
  `poodle-style`, and `poodle-tokens`;
- composition: `poodle-render`;
- backends: `poodle-gpui`, `poodle-gpui-node-backend`, and
  `poodle-jetstream`.

Jetstream remains program-deferred.

## Downstream checks

- `0.2.3` is not installable from any registry: it was never published. It is
  historical repository state only — there is no `v0.2.3` tag or npm artifact
  to install, so web consumers remain on the released `0.2.2` versions until a
  later released version supersedes them.
- Overlay-only menus should set `trigger={false}` and keep invocation on the
  real target (tree row, list card, canvas).
- Default `ContextMenu` with a slotted trigger is unchanged.
- Do not request `@inflatable-cookie/poodle-react` from npm; it remains
  source-only.
