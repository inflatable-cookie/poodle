# g16.046 — Block Slider And RangeSlider Appearance

Status: implementation-complete — PR pending orchestrator review
Date: 2026-09-02
Card: `docs/roadmaps/g16/046-block-slider-and-range-slider-appearance.md`
Handoff: `docs/handoffs/20260902-004201-g16-046-block-sliders.md`
Governing refs: `docs/contracts/components/slider.md`,
`docs/contracts/components/range-slider.md`,
`docs/architecture/007-appearance-recipe-contract.md`,
`docs/architecture/012-semantic-motion-policy.md`
Branch: `feature/g16-046-block-sliders`
Worktree: `/Users/tom/.paseo/worktrees/1ugbsx1t/g16-046-block-sliders`
Planning ancestor: `7f59ae42f4917c675968819eb23a5e41dc90013c`
Rebase base: `77e19a619263f92fcb8e4cb6d5d3bd87b82b4ced` (PR #152)
Proof commit: `7e8d302c8c847b84da805c0b2f1a01c6165dd35e`
Pre-rebase proof: `eedb4a38ae4ac010aeb86e998b113b7a1d8d0a2c`

## Outcome

Slider and RangeSlider gained an opt-in horizontal `appearance="block"`:
labelled capsule, 44×44 hits, small thumbs, atomic fallback, `ltr`/`rtl`
geometry, and forced-color roles. `appearance` is orthogonal to `variant`.
Omission keeps track anatomy and the current value machines.

Vertical block is invalid in Svelte, React, shared Rust, and GPUI. Adapters
throw or panic before paint. Jetstream specimens compile only.

Native specs carry resolved visible strings. `formatVisible*` stays web-only
on the drift allowlist. Visible text never reads `ariaLabel` / `valueText`.

RangeSlider ties choose lower and clamp at the sibling. Pointer, cancel, lost
capture, disablement, and teardown share one `POINTER_END` terminal.

Native block paint puts a full-width 44px scrub surface above the focusable
hits so coincident thumbs still report capsule-relative fractions.

React packed `SliderAppearance` proofs map onto installed `src/types.ts`. The
package `types` condition is the value barrel, which is not tsc-clean.

## Falsification

Plant-and-restore ran against pre-rebase proof `eedb4a38ae4ac010aeb86e998b113b7a1d8d0a2c`.
That commit is `7e8d302c8c847b84da805c0b2f1a01c6165dd35e` on this branch after
rebase onto PR #152. Each oracle was planted, the named proof failed, then
`git checkout` restored the proof. Focused core + paired web boards were green
after restore.

| Oracle | Plant | Proof that failed |
| --- | --- | --- |
| Default is inert | default `data-appearance="track"` | Svelte omitting-appearance anatomy |
| Fit is atomic | miss-by-one still fits | `layoutSliderBlock` miss-by-one |
| Channels stay separate | visible label falls back to `ariaLabel` | Svelte `ariaLabel="Gain"` |
| Tie and identity | exact tie chooses upper | equal-value pointer tie |
| Terminal is idempotent | every `POINTER_END` commits | second pointer end inert |
| Targets are real | hit token `24px` | Svelte 44px CSS claim |
| Forced colors | selected fill `Canvas` | Highlight vs Canvas CSS gate |
| Vertical stays gated | `assertHorizontalBlockAppearance` no-op | Svelte vertical throw |

## Validation

Focused:

- `bun test packages/core/test/wave1.test.ts` — 25 pass
- paired Svelte/React Slider + RangeSlider vitest — 54 pass
- `cargo test` poodle-headless `slider` — 9 pass
- `cargo test -p poodle-render --lib slider` — 30 pass
- GPUI `block_slider_hit_rtl_and_terminal_on_the_mounted_host` — pass

Required boards on `42686549bf2b5a77f63976c18ed6d4f0743f4b50` after rebase:

| Board | Result |
| --- | --- |
| `effigy ci:web` | pass |
| `effigy ci:rust` | pass |
| `effigy ci:native` | pass |
| `effigy docs:check` | pass |
| `effigy qa` | pass |
| `git diff --check origin/main...HEAD` | pass |

`origin/main` at closeout is `f645091f1` (`docs(g16.047): close toast update
convention`). That commit only edits g16 front doors and the g16.047 card/log.
This lane did not rebase onto it.

Rebase conflict: `packages/gpui/preview/tests/headless_regressions.rs` imports.
Kept both `ToastTone` from main and `SliderAppearance` / `SliderDirection`.

g16 README, generation index, and continuation-register were not edited.

## Limits

- No vertical block. No public fit metrics. No paging change. No new motion
  role. No native AT claim. Jetstream compile-compat specimens only.
- Packed React assignability is mapped to `src/types.ts`; the value barrel is
  not compiled.
- Visual ledger cells were not moved.
