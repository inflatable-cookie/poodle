# g18.009 closeout — v0.4.0 release evidence merged (integration checkout, 2026-09-13)

## Merged outcome

- PR #266 (`g18.009: publish v0.4.0 and return the Desktop capsule`)
  merged 2026-09-13T07:16:41Z as `4c0bb57410584da944f781eb8064805521fba054`,
  now `origin/main`.
- Branch carried exactly two commits over `e40485ab17af865ad72976b010a2d00b40cfa26b`:
  `4a39055f3` (operator-authorized npm/web release-wrapper repair) and
  `3a963df73` (docs-only release certification and Desktop capsule).
  Tag `v0.4.0` still resolves to `4a39055f3`; package trees unchanged by
  the merge.
- Accepted review: independent review (comment `5651878364`), verdict
  `ready_to_merge` at the exact head `3a963df732e1ab6499c76feca6c2d660b3beb071`;
  claims re-verified against the remote tag, Actions API, npm and crates.io,
  not taken from the evidence log on trust. Exact-head `rust` + `web`
  checks green; mergeable `MERGEABLE`/`CLEAN`; non-draft throughout.

## Truthful validation

- Closeout reran no implementation or broad validation suites. The
  certificate is the worker/reviewer evidence plus plugin-owned
  exact-head merge verification: local `main` verified clean at
  `4c0bb574`, matching `origin/main`, before this batch.
- Focused documentation check `effigy docs:check` green over the
  closeout batch.
- Boundaries held: no tag, no publication, no registry mutation, no
  candidate rebuild, no workflow edit, no Desktop change, no gate bypass.
- Post-merge plugin sync first failed with `git ls-tree: fatal: not a
  tree object` against a stale integration checkout missing the merge
  object; recovered by fetching `origin/main` and retrying. No history
  was rewritten.

## Deferred

- No task-specific failure is deferred. Review notes were explicitly
  non-blocking: the 2026-09-13 operator ruling superseding the
  workflow-edit boundary is recorded in the roadmap card, evidence log
  and workflow comment, and the `web` check pending at review time has
  since passed.
- Desktop resumes retained g02.058 and PR #215 with the returned
  capsule (repin core/Svelte to `0.4.0`, finish Phase B). Poodle returns
  to Chatterbox for GPUI tranche compilation; no release successor
  auto-starts.
