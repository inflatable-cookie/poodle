# g15.070 — Composer Underlay 0.9.2 and Poodle 0.2.2 Adoption

Date: 2026-08-25
Status: complete
PR: [Composer #1](https://github.com/inflatable-cookie/loophole-composer/pull/1)
Merge: `74e5a7a79ad4b5d4b57d881520934a5c75e1d6ca`

## Result

Composer's active application graph no longer resolves Underlay or Poodle from
sibling source trees. Admin, Front, and API-client use Underlay tag `v0.9.2` at
`ddba26400f480638829917cf72eecc62be4b978d`. Admin and Front use exact registry
Poodle 0.2.2. The API's 24 Underlay packages resolve the same Git revision.

SvelteKit/Vite sibling aliases, committed Poodle overrides, stale local-link
calls, and direct source-file config imports are gone. The public-config
generators now use Underlay's published config-stack subpath. Explicit sibling
mounts remain only for the workspace's cross-repository QA.

## Validation

Independent review passed:

- `cargo check --workspace --all-features --all-targets --locked`
- `git diff --check`

Worker evidence also passed the Admin and Front Svelte checks and builds,
Front's frozen install, API-client typecheck, and host API Cargo check.

Review caught three lock problems and required narrow repairs before merge:

- Front retained stale local API-client Underlay metadata;
- the first Front repair regenerated unrelated web resolutions;
- Cargo upgraded 62 packages unrelated to Underlay.

The final Front lock repair changes only the coupled dependency identities.
Cargo changes exactly 24 Underlay packages from local 0.8.0 paths to tagged
0.9.2, with no unrelated package additions or removals.

Composer's Effigy API selector still cannot create its `/underlay` symlink or
Cargo build lock inside the repository's existing mounted-volume permission
baseline. The documentation validator also retains Composer's pre-existing
semantic-role and `WithEtag` baselines. Host checks pass; no application or QA
exception was added.

## Continuation

Composer, Contact Patch, and Songsprout are complete. Continue the coupled wave
through Acowtancy and Compli Me. Underlay Reference `g15.079` and Longhorn
product cards `073`–`076` remain parallel-ready.
