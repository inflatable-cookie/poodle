// Installed package-export assignability proof for packed `SliderVariant`.
import type { SliderVariant } from "@inflatable-cookie/poodle-react";

const block: SliderVariant = "block";
const embedded: SliderVariant = "embedded";

export const variants: SliderVariant[] = [block, embedded];
