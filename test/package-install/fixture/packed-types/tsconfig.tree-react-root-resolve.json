{
  // Installed public-root resolution. No paths map. Does not prove assignability;
  // tsc --traceResolution must land on package exports types → dist/index.d.ts.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": []
  },
  "files": ["ambient.d.ts", "tree-reorder-react-root-resolve.ts"]
}
