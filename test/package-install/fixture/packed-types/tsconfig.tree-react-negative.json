{
  // Public package-export assignability. The installed package is the only
  // resolver; the proof must not bypass its exports with a paths map.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": []
  },
  "files": ["ambient.d.ts", "tree-reorder-react-negative.tsx"]
}
